<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>H5 软键盘兼容方案</title>
      <link href="/2023/04/17/H5%20%E8%BD%AF%E9%94%AE%E7%9B%98%E5%85%BC%E5%AE%B9%E6%96%B9%E6%A1%88/"/>
      <url>/2023/04/17/H5%20%E8%BD%AF%E9%94%AE%E7%9B%98%E5%85%BC%E5%AE%B9%E6%96%B9%E6%A1%88/</url>
      
        <content type="html"><![CDATA[<p>在 App 内嵌 WebView 的聊天、评论、搜索等场景中，页面通常由三部分组成：<strong>顶部 Header（固定）</strong>、<strong>中间对话区（可滚动）</strong>、<strong>底部输入框（固定）</strong>。</p><p>理想状态下，软键盘弹出时只有中间对话区被压缩，Header 始终吸顶，输入框随键盘上推。但在 iOS 和 Android 上，实际情况往往与预期不符。本文整理了实际开发中常见的兼容问题，以及对应的处理方案。</p><span id="more"></span><h2 id="一、理想布局"><a href="#一、理想布局" class="headerlink" title="一、理想布局"></a>一、理想布局</h2><img src="/2023/04/17/H5%20%E8%BD%AF%E9%94%AE%E7%9B%98%E5%85%BC%E5%AE%B9%E6%96%B9%E6%A1%88/h5-keyboard-layout-ideal.png" class="" title="理想的聊天页面布局"><p>核心诉求可以概括为：<strong>键盘只影响中间内容区的高度，不改变 Header 和输入框的相对位置关系。</strong></p><h2 id="二、iOS"><a href="#二、iOS" class="headerlink" title="二、iOS"></a>二、iOS</h2><h3 id="问题一：fixed-定位的-Header-跟着页面上滑"><a href="#问题一：fixed-定位的-Header-跟着页面上滑" class="headerlink" title="问题一：fixed 定位的 Header 跟着页面上滑"></a>问题一：fixed 定位的 Header 跟着页面上滑</h3><h4 id="现象"><a href="#现象" class="headerlink" title="现象"></a>现象</h4><p>输入框获得焦点后，Header 虽然设置了 <code>position: fixed</code>，但键盘弹出时仍会跟着整个页面一起滑走，无法保持吸顶。</p><img src="/2023/04/17/H5%20%E8%BD%AF%E9%94%AE%E7%9B%98%E5%85%BC%E5%AE%B9%E6%96%B9%E6%A1%88/h5-keyboard-ios-problem-steps.png" class="" title="键盘弹出后 Header 随整页上滑，fixed 定位失效"><h4 id="原因"><a href="#原因" class="headerlink" title="原因"></a>原因</h4><p>2015 年 3 月 iOS 8.2 发布后，键盘弹起时 WebView <strong>不会被压缩</strong>，而是<strong>整体被顶上去</strong>。此时 <code>fixed</code> 元素不会重新计算定位，仍保持原来的相对位置，于是跟着页面一起上移。</p><h4 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h4><p><strong>方案一：客户端原生 Header（推荐）</strong></p><p>由客户端提供自定义 Header，或针对当前场景定制原生导航栏。原生 Header 不属于 WebView 内容，WebView 上推时它依然保持吸顶——这是从根源上规避问题的方式。</p><p><strong>方案二：H5 动态修正 Header 定位</strong></p><p>既然页面高度没变、只是整体上移，那么获取键盘高度后重新设置 Header 的 <code>top</code> 即可。</p><p>获取键盘高度有两种方式：</p><table><thead><tr><th>方式</th><th>原理</th><th>局限</th></tr></thead><tbody><tr><td><code>window.pageYOffset</code></td><td>页面上移的距离即键盘高度</td><td>通用性好</td></tr><tr><td><code>VisualViewport</code> API</td><td>用 WebView 高度减去可视区域高度</td><td>iOS 13+ 才支持</td></tr></tbody></table><p>推荐第一种方式。处理时机是输入框 <code>focus</code> 时——iOS 键盘弹出有约 300ms 延迟，需要 <code>setTimeout</code> 配合：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">isIOS</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="regexp">/iPad|iPhone|iPod/</span>.<span class="title function_">test</span>(navigator.<span class="property">userAgent</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">HEADER_SELECTOR</span> = <span class="string">&#x27;.page-header&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">fixHeaderPosition</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> header = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="variable constant_">HEADER_SELECTOR</span>);</span><br><span class="line">  <span class="keyword">if</span> (!header) <span class="keyword">return</span>;</span><br><span class="line">  header.<span class="property">style</span>.<span class="property">top</span> = <span class="string">`<span class="subst">$&#123;<span class="variable language_">window</span>.pageYOffset&#125;</span>px`</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">resetHeaderPosition</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">window</span>.<span class="title function_">scrollTo</span>(<span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">  <span class="keyword">const</span> header = <span class="variable language_">document</span>.<span class="title function_">querySelector</span>(<span class="variable constant_">HEADER_SELECTOR</span>);</span><br><span class="line">  <span class="keyword">if</span> (header) header.<span class="property">style</span>.<span class="property">top</span> = <span class="string">&#x27;0&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 组件内</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">onInputFocus</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!<span class="title function_">isIOS</span>()) <span class="keyword">return</span>;</span><br><span class="line">  <span class="built_in">setTimeout</span>(fixHeaderPosition, <span class="number">350</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">onInputBlur</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!<span class="title function_">isIOS</span>()) <span class="keyword">return</span>;</span><br><span class="line">  <span class="title function_">resetHeaderPosition</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>iPad 横竖屏切换</strong></p><p>iPad 翻转屏幕时，如果不重新计算 Header 定位，页面会出现错乱。监听 <code>resize</code> 让输入框失焦，聚焦时再重新计算：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="title function_">addEventListener</span>(<span class="string">&#x27;resize&#x27;</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="title function_">isIOS</span>()) &#123;</span><br><span class="line">    inputEl.<span class="title function_">blur</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>参考：<a href="https://segmentfault.com/a/1190000018959389">可能这些是你想要的 H5 软键盘兼容方案</a></p><p><strong>已知缺点</strong></p><p>切换键盘类型（如中英文切换）时，无法检测到键盘高度变化，页面会有轻微上下跳动。如需更平滑，可在 <code>focus</code> 期间监听页面滚动，实时更新 Header 的 <code>top</code>，<code>blur</code> 后恢复。</p><p><strong>避坑：不要用 scrollTo 强拉页面</strong></p><p>网上有一种做法是：键盘弹出后强制 <code>scrollTo</code> 把页面拉回来，再修改 <code>body</code> 高度：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> initialBodyHeight = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">onInputFocusBad</span>(<span class="params"></span>) &#123;</span><br><span class="line">  initialBodyHeight = <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">clientHeight</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (!<span class="title function_">isIOS</span>()) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">scrollTop</span> = <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">scrollHeight</span>;</span><br><span class="line">    <span class="variable language_">window</span>.<span class="title function_">scrollTo</span>(<span class="number">0</span>, <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">scrollTop</span> + <span class="number">1</span>);</span><br><span class="line">    <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">scrollTop</span> &gt;= <span class="number">1</span> &amp;&amp; <span class="variable language_">window</span>.<span class="title function_">scrollTo</span>(<span class="number">0</span>, <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">scrollTop</span> - <span class="number">1</span>);</span><br><span class="line">  &#125;, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> keyboardHeight = <span class="variable language_">window</span>.<span class="property">innerHeight</span> - <span class="variable language_">window</span>.<span class="property">visualViewport</span>.<span class="property">height</span>;</span><br><span class="line">    <span class="keyword">if</span> (keyboardHeight &gt; <span class="number">0</span>) &#123;</span><br><span class="line">      <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">style</span>.<span class="property">height</span> = <span class="string">`<span class="subst">$&#123;initialBodyHeight - keyboardHeight&#125;</span>px`</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, <span class="number">300</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">onInputBlurBad</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!<span class="title function_">isIOS</span>()) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">  <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">style</span>.<span class="property">height</span> = <span class="string">`<span class="subst">$&#123;initialBodyHeight&#125;</span>px`</span>;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> scrollTop = <span class="variable language_">document</span>.<span class="property">documentElement</span>.<span class="property">scrollTop</span> || <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">scrollTop</span> || <span class="number">0</span>;</span><br><span class="line">    <span class="variable language_">window</span>.<span class="title function_">scrollTo</span>(<span class="number">0</span>, <span class="title class_">Math</span>.<span class="title function_">max</span>(scrollTop - <span class="number">1</span>, <span class="number">0</span>));</span><br><span class="line">  &#125;, <span class="number">200</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种方式看似可行，但如果恰好在滑动页面时聚焦，<code>scrollTo</code> 会与 <code>-webkit-overflow-scrolling: touch</code> 冲突，导致滚动异常。处理滑动问题时，应尽量避开对该属性的影响——它能让滚动更流畅，建议保持开启：</p><figure class="highlight css"><table><tr><td class="code"><pre><span class="line">-webkit-<span class="attribute">overflow</span>-scrolling: touch;  <span class="comment">/* 手指离开后继续惯性滚动 */</span></span><br><span class="line">-webkit-<span class="attribute">overflow</span>-scrolling: auto;    <span class="comment">/* 手指离开后立即停止 */</span></span><br></pre></td></tr></table></figure><p><strong>方案三：H5 + 客户端 SDK</strong></p><p>客户端提供 SDK，实时通知前端键盘高度变化，前端据此动态设置 <code>top</code>。精度和实时性最好，但需要客户端配合。</p><h3 id="问题二：iOS-12-4-1-键盘收起后页面不恢复"><a href="#问题二：iOS-12-4-1-键盘收起后页面不恢复" class="headerlink" title="问题二：iOS 12.4.1 键盘收起后页面不恢复"></a>问题二：iOS 12.4.1 键盘收起后页面不恢复</h3><h4 id="现象-1"><a href="#现象-1" class="headerlink" title="现象"></a>现象</h4><p><code>fixed</code> 定位容器内的 <code>input</code> 聚焦时软键盘占位，失焦后键盘消失但<strong>占位未释放</strong>，导致输入框无法再次输入。</p><h4 id="解决方案-1"><a href="#解决方案-1" class="headerlink" title="解决方案"></a>解决方案</h4><p>失焦时主动滚动页面，释放占位：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="title function_">scrollTo</span>(<span class="number">0</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure><h2 id="三、Android"><a href="#三、Android" class="headerlink" title="三、Android"></a>三、Android</h2><p>Android 不会出现 Header 不固定的问题，但键盘弹出的表现分两种模式。</p><img src="/2023/04/17/H5%20%E8%BD%AF%E9%94%AE%E7%9B%98%E5%85%BC%E5%AE%B9%E6%96%B9%E6%A1%88/h5-keyboard-android-modes.png" class="" title="Android 键盘弹出的两种表现"><h3 id="正常模式"><a href="#正常模式" class="headerlink" title="正常模式"></a>正常模式</h3><p>键盘弹出时 WebView 被压缩，底部输入框自然上滑——<strong>最理想的状态，无需额外处理。</strong></p><h3 id="异常模式（华为等机型居多）"><a href="#异常模式（华为等机型居多）" class="headerlink" title="异常模式（华为等机型居多）"></a>异常模式（华为等机型居多）</h3><p>键盘弹出后页面<strong>毫无变化</strong>，WebView 没有被压缩，键盘直接覆盖在 H5 页面上，输入框被挡住。</p><p>原因尚不明确，且因为可视区没有变化，前端也<strong>无法直接获取键盘高度</strong>。</p><h3 id="解决方案-2"><a href="#解决方案-2" class="headerlink" title="解决方案"></a>解决方案</h3><p><strong>方案一：H5 按屏幕比例估算键盘高度</strong></p><p>思路比较粗糙：根据不同设备的宽高比推算键盘大概高度，给一个固定值。以下系数来自常见机型的实测经验，<strong>上线前需按目标设备微调</strong>。</p><ul><li>监听 <code>resize</code> 无效——异常机型可视区根本不改变</li><li><code>scrollIntoView</code> 也无效——系统认为屏幕大小就是可视区，不会产生滚动</li></ul><p>聚焦后等待 600ms，对比前后可视区高度，<strong>过滤掉正常机型</strong>（正常机型键盘弹出后可视区会减小，不需要处理）：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 宽高比下限 → 键盘高度系数（值越小，估算键盘越高）</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">KEYBOARD_RATIO_MAP</span> = [</span><br><span class="line">  [<span class="number">1.9</span>, <span class="number">1.835</span>],</span><br><span class="line">  [<span class="number">1.8</span>, <span class="number">1.89</span>],</span><br><span class="line">  [<span class="number">1.7</span>, <span class="number">1.945</span>],</span><br><span class="line">  [<span class="number">1.6</span>, <span class="number">2.0</span>],</span><br><span class="line">  [<span class="number">1.5</span>, <span class="number">2.055</span>],</span><br><span class="line">  [<span class="number">1.4</span>, <span class="number">2.11</span>],</span><br><span class="line">  [<span class="number">1.3</span>, <span class="number">2.165</span>],</span><br><span class="line">  [<span class="number">1.2</span>, <span class="number">2.22</span>],</span><br><span class="line">  [<span class="number">1.1</span>, <span class="number">2.275</span>],</span><br><span class="line">  [<span class="number">1.0</span>, <span class="number">2.33</span>],</span><br><span class="line">  [<span class="number">0.9</span>, <span class="number">2.4</span>],</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">getViewportSize</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">width</span>: <span class="variable language_">document</span>.<span class="property">documentElement</span>.<span class="property">clientWidth</span>,</span><br><span class="line">    <span class="attr">height</span>: <span class="variable language_">document</span>.<span class="property">documentElement</span>.<span class="property">clientHeight</span>,</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">estimateKeyboardHeight</span>(<span class="params">screenHeight, width, height</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> aspectRatio = height / width;</span><br><span class="line">  <span class="keyword">if</span> (aspectRatio &gt; <span class="number">2</span>) <span class="keyword">return</span> screenHeight / <span class="number">1.78</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> ratio = <span class="variable constant_">KEYBOARD_RATIO_MAP</span>.<span class="title function_">find</span>(<span class="function">(<span class="params">[min]</span>) =&gt;</span> aspectRatio &gt;= min)?.[<span class="number">1</span>] ?? <span class="number">2.6</span>;</span><br><span class="line">  <span class="keyword">return</span> screenHeight / ratio;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> cachedScreenHeight = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">onInputFocus</span>(<span class="params"></span>) &#123;</span><br><span class="line">  cachedScreenHeight = <span class="title function_">getViewportSize</span>().<span class="property">height</span>;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    inputEl.<span class="title function_">scrollIntoView</span>(<span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 过滤正常机型：可视区高度有变化说明 WebView 已被压缩</span></span><br><span class="line">    <span class="keyword">const</span> &#123; width, height &#125; = <span class="title function_">getViewportSize</span>();</span><br><span class="line">    <span class="keyword">if</span> (cachedScreenHeight !== height) &#123;</span><br><span class="line">      cachedScreenHeight = <span class="number">0</span>;</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> inputTop = inputBarEl.<span class="title function_">getBoundingClientRect</span>().<span class="property">top</span>;</span><br><span class="line">    <span class="keyword">const</span> keyboardHeight = <span class="title function_">estimateKeyboardHeight</span>(cachedScreenHeight, width, height);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (inputTop - keyboardHeight &lt; <span class="number">0</span>) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">style</span>.<span class="property">height</span> = <span class="string">`<span class="subst">$&#123;cachedScreenHeight - (inputTop - keyboardHeight)&#125;</span>px`</span>;</span><br><span class="line">  &#125;, <span class="number">600</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">onInputBlur</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (cachedScreenHeight === <span class="number">0</span>) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">  <span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">style</span>.<span class="property">height</span> = <span class="string">`<span class="subst">$&#123;cachedScreenHeight&#125;</span>px`</span>;</span><br><span class="line">  cachedScreenHeight = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>失焦时重置页面高度即可。上面示例用 <code>inputEl</code>、<code>inputBarEl</code> 代指输入框及其容器，Vue 项目中可对应 <code>$refs.input</code>、<code>$refs.inputBar</code>。</p><p><strong>缺点</strong>：实现有投机成分，不够严谨，但能兼容大多数异常机型。</p><p><strong>方案二：H5 + 客户端 SDK</strong></p><p>与 iOS 方案三相同，由客户端实时通知键盘高度，前端动态调整布局——最理想的长期方案。</p><h2 id="四、方案对比"><a href="#四、方案对比" class="headerlink" title="四、方案对比"></a>四、方案对比</h2><table><thead><tr><th>方案</th><th>适用平台</th><th>精度</th><th>客户端成本</th><th>推荐度</th></tr></thead><tbody><tr><td>原生 Header</td><td>iOS</td><td>完美</td><td>中</td><td>⭐⭐⭐⭐⭐</td></tr><tr><td>H5 修正 <code>top</code></td><td>iOS</td><td>较好</td><td>无</td><td>⭐⭐⭐⭐</td></tr><tr><td>H5 估算键盘高度</td><td>Android 异常机型</td><td>一般</td><td>无</td><td>⭐⭐⭐</td></tr><tr><td>客户端 SDK</td><td>iOS &#x2F; Android</td><td>最好</td><td>高</td><td>⭐⭐⭐⭐⭐</td></tr></tbody></table><h2 id="五、总结"><a href="#五、总结" class="headerlink" title="五、总结"></a>五、总结</h2><p>H5 软键盘兼容没有银弹，核心思路是：</p><ol><li><strong>能交给客户端的尽量交给客户端</strong>——原生 Header 或键盘高度 SDK 是最稳的方案</li><li><strong>iOS 的关键是补偿 <code>pageYOffset</code></strong>——键盘顶起整个页面后，用 <code>top</code> 把 fixed 元素拉回来</li><li><strong>Android 异常机型只能曲线救国</strong>——按屏幕比例估算，先过滤正常机型再处理</li><li><strong>避开 <code>scrollTo</code> + <code>-webkit-overflow-scrolling: touch</code> 的组合</strong>——会引发滚动冲突</li></ol><p>如果业务对体验要求高，建议推动客户端提供键盘高度 SDK，前端统一监听、统一适配，一劳永逸。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><ul><li><a href="http://www.alloyteam.com/2020/02/14265/">AlloyTeam：移动端 H5 软键盘兼容</a></li><li><a href="https://segmentfault.com/a/1190000018959389">SegmentFault：可能这些是你想要的 H5 软键盘兼容方案</a></li><li><a href="https://juejin.cn/post/7148986270369185799">掘金：7 种解决 iOS 软键盘 fixed 吸顶失效的方案</a></li><li><a href="https://juejin.cn/post/6961757804491178014">掘金：移动端软键盘问题及解决方法</a></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> 前端, 移动端, H5 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>webpack 热更新原理</title>
      <link href="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/"/>
      <url>/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a><strong>前言</strong></h2><p>随着前端技术的不断发展，构建工具与我们的开发已密不可分，但是无论选择哪款构建工具，我们都或多或少的使用过他们热更新的能力，今天就以 webpack 为例，来看 webpack 热更新的原理。</p><p>要实现热更新的能力，首先 webpack 要具备文件监听的能力，其次也要具备模块构建的能力，所以我们就从无到有的看 webpack 热更新的原理。</p><span id="more"></span><h2 id="一、webpack-编译原理"><a href="#一、webpack-编译原理" class="headerlink" title="一、webpack 编译原理"></a><strong>一、webpack 编译原理</strong></h2><p>先创建一个项目，看在不引入构建工具的情况下会出现什么问题。</p><h3 id="1、新建项目"><a href="#1、新建项目" class="headerlink" title="1、新建项目"></a><strong>1、新建项目</strong></h3><h4 id="1-1、新建项目"><a href="#1-1、新建项目" class="headerlink" title="1.1、新建项目"></a>1.1、<strong>新建项目</strong></h4><p>新建项目</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">mkdir</span> webpack-hmr-demo</span><br><span class="line"><span class="built_in">cd</span> webpack-hmr-demo</span><br></pre></td></tr></table></figure><p> 项目结构</p><figure class="highlight tree"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line"><span class="keyword">├──</span> <span class="string">index.html</span></span><br><span class="line"><span class="keyword">└──</span> <span class="title">src</span></span><br><span class="line">    <span class="keyword">├──</span> <span class="string">App.vue</span></span><br><span class="line">    <span class="keyword">├──</span> <span class="title">assets</span></span><br><span class="line">    <span class="keyword">│</span>   <span class="keyword">├──</span> <span class="title">css</span></span><br><span class="line">    <span class="keyword">│</span>   <span class="keyword">│</span>   <span class="keyword">└──</span> <span class="string">index.css</span></span><br><span class="line">    <span class="keyword">│</span>   <span class="keyword">└──</span> <span class="title">js</span></span><br><span class="line">    <span class="keyword">│</span>       <span class="keyword">└──</span> <span class="string">index.js</span></span><br><span class="line">    <span class="keyword">└──</span> <span class="string">main.js</span></span><br></pre></td></tr></table></figure><p>main.js</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createApp &#125; <span class="keyword">from</span> <span class="string">&quot;vue&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="title class_">App</span> <span class="keyword">from</span> <span class="string">&quot;./App.vue&quot;</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;./assets/css/index.css&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> app = <span class="title function_">createApp</span>(<span class="title class_">App</span>);</span><br><span class="line">app.<span class="title function_">mount</span>(<span class="string">&quot;#app&quot;</span>);</span><br></pre></td></tr></table></figure><p>App.vue</p><figure class="highlight vue"><table><tr><td class="code"><pre><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">template</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">div</span>&gt;</span>&#123;&#123; msg &#125;&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> /&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">button</span> @<span class="attr">click</span>=<span class="string">&quot;handleClick&quot;</span>&gt;</span>点击<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;/<span class="name">template</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">  <span class="keyword">import</span> &#123; defineComponent, ref &#125; <span class="keyword">from</span> <span class="string">&quot;vue&quot;</span>;</span></span><br><span class="line"><span class="language-javascript">  </span></span><br><span class="line"><span class="language-javascript">  <span class="keyword">import</span> &#123; modification &#125; <span class="keyword">from</span> <span class="string">&quot;./assets/js/index&quot;</span>;</span></span><br><span class="line"><span class="language-javascript">  </span></span><br><span class="line"><span class="language-javascript">  <span class="keyword">export</span> <span class="keyword">default</span> <span class="title function_">defineComponent</span>(&#123;</span></span><br><span class="line"><span class="language-javascript">    <span class="attr">name</span>: <span class="string">&quot;App&quot;</span>,</span></span><br><span class="line"><span class="language-javascript">    <span class="title function_">setup</span>(<span class="params"></span>) &#123;</span></span><br><span class="line"><span class="language-javascript">      <span class="keyword">const</span> msg = <span class="title function_">ref</span>(<span class="string">&quot;hello world&quot;</span>);</span></span><br><span class="line"><span class="language-javascript">      </span></span><br><span class="line"><span class="language-javascript">      <span class="keyword">function</span> <span class="title function_">handleClick</span>(<span class="params"></span>) &#123;</span></span><br><span class="line"><span class="language-javascript">        msg.<span class="property">value</span> = <span class="title function_">modification</span>(msg.<span class="property">value</span>);</span></span><br><span class="line"><span class="language-javascript">      &#125;</span></span><br><span class="line"><span class="language-javascript">      </span></span><br><span class="line"><span class="language-javascript">      <span class="keyword">return</span> &#123;</span></span><br><span class="line"><span class="language-javascript">        msg,</span></span><br><span class="line"><span class="language-javascript">        handleClick,</span></span><br><span class="line"><span class="language-javascript">      &#125;;</span></span><br><span class="line"><span class="language-javascript">    &#125;,</span></span><br><span class="line"><span class="language-javascript">  &#125;);</span></span><br><span class="line"><span class="language-javascript"></span><span class="language-xml"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span></span><br></pre></td></tr></table></figure><p>assets&#x2F;js&#x2F;index.js</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">modification</span>(<span class="params">str</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;str&#125;</span>!!!`</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>assets&#x2F;css&#x2F;index.css</p><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#app</span> &#123;</span><br><span class="line">  <span class="attribute">font-size</span>: <span class="number">25px</span>;</span><br><span class="line">  <span class="attribute">font-weight</span>: <span class="number">600</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>之后在 index.html 中引入 main.js 文件，理想状态下，通过浏览器应该是能直接访问我们写的页面的，但是通过浏览器打开后，会发现报如下错误：</p><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6htwhrq36j217206gt9s.jpg"></p><h4 id="1-2、问题分析"><a href="#1-2、问题分析" class="headerlink" title="1.2、问题分析"></a>1.2、问题分析</h4><p>JavaScript 有两种源文件，一种是脚本，另一种是模块。二者的区别是脚本只有语句，模块除了语句，还有 import 声明或 export 声明。</p><p>main.js 文件属于模块，而 script 标签的 type 属性默认是 text&#x2F;javascript ，所以浏览器默认会将它作为脚本进行解析，所以此处需要把 type 修改为 module ，将 main.js 作为模块引入。</p><p>但是修改后依然报错。</p><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu1dei63j217e09atbs.jpg"></p><p>这是因为脚本被跨域策略给拦截了，我们使用的协议是 file、而跨域请求只支持这些协议：http、data、chrome、chrome-extension、chrome-untrusted、https，这里我们需要一个服务来作为资源的提供方，最简单的方式就是通过 VSCode 安装一个插件 Live Server。</p><p>跨域解决了但是项目依然跑不起来，因为没有引入 vue 的模块，即使在 node_modules 下安装了浏览器也找不到，因为它没有对应的查找规则，并且在项目中我们也不能需要什么就通过 cdn 的方式引入或直接把对应模块下载到本地，这个在后期模块的维护管理方面都会带来问题。</p><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu20xtksj216y0643zw.jpg"></p><p>所以这个时候，我们就需要使用构建工具来帮我们对文件做处理让浏览器能认识。</p><h3 id="2、使用"><a href="#2、使用" class="headerlink" title="2、使用"></a>2、使用</h3><h4 id="2-1、安装依赖"><a href="#2-1、安装依赖" class="headerlink" title="2.1、安装依赖"></a><strong>2.1、安装依赖</strong></h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yarn init -y</span><br><span class="line"></span><br><span class="line">yarn add webpack webpack-cli --save-dev</span><br><span class="line">yarn add @babel/core @babel/preset-env babel-loader --save-dev</span><br><span class="line">yarn add vue vue-loader vue-template-compiler --save-dev</span><br><span class="line">yarn add html-webpack-plugin --save-dev</span><br><span class="line">yarn add css-loader style-loader --save-dev </span><br></pre></td></tr></table></figure><h4 id="2-2、项目配置"><a href="#2-2、项目配置" class="headerlink" title="2.2、项目配置"></a><strong>2.2、项目配置</strong></h4><p>配置 build&#x2F;webpack.dev.js 文件</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">&quot;path&quot;</span>);</span><br><span class="line"><span class="keyword">const</span> &#123; <span class="title class_">CleanWebpackPlugin</span> &#125; = <span class="built_in">require</span>(<span class="string">&quot;clean-webpack-plugin&quot;</span>);</span><br><span class="line"><span class="keyword">const</span> &#123; <span class="title class_">VueLoaderPlugin</span> &#125; = <span class="built_in">require</span>(<span class="string">&quot;vue-loader&quot;</span>);</span><br><span class="line"><span class="keyword">const</span> <span class="title class_">HtmlWebpackPlugin</span> = <span class="built_in">require</span>(<span class="string">&quot;html-webpack-plugin&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123;</span><br><span class="line">  <span class="attr">mode</span>: <span class="string">&quot;development&quot;</span>,</span><br><span class="line">  <span class="attr">entry</span>: path.<span class="title function_">join</span>(__dirname, <span class="string">&quot;../src/main.js&quot;</span>),</span><br><span class="line">  <span class="attr">output</span>: &#123;</span><br><span class="line">    <span class="attr">filename</span>: <span class="string">&quot;bundle.js&quot;</span>,</span><br><span class="line">    <span class="attr">path</span>: path.<span class="title function_">join</span>(__dirname, <span class="string">&quot;../dist&quot;</span>),</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">module</span>: &#123;</span><br><span class="line">    <span class="attr">rules</span>: [</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="attr">test</span>: <span class="regexp">/\.js$/</span>,</span><br><span class="line">        <span class="attr">exclude</span>: <span class="string">&quot;/node_modules/&quot;</span>,</span><br><span class="line">        <span class="attr">use</span>: &#123;</span><br><span class="line">          <span class="attr">loader</span>: <span class="string">&quot;babel-loader&quot;</span>,</span><br><span class="line">          <span class="attr">options</span>: &#123;</span><br><span class="line">            <span class="attr">presets</span>: [<span class="string">&quot;@babel/preset-env&quot;</span>],</span><br><span class="line">          &#125;,</span><br><span class="line">        &#125;,</span><br><span class="line">      &#125;,</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="attr">test</span>: <span class="regexp">/\.vue$/</span>,</span><br><span class="line">        <span class="attr">loader</span>: <span class="string">&quot;vue-loader&quot;</span>,</span><br><span class="line">      &#125;,</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="attr">test</span>: <span class="regexp">/\.css$/</span>,</span><br><span class="line">        <span class="attr">use</span>: [<span class="string">&quot;style-loader&quot;</span>, <span class="string">&quot;css-loader&quot;</span>],</span><br><span class="line">      &#125;,</span><br><span class="line">    ],</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">plugins</span>: [</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">CleanWebpackPlugin</span>(),</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">VueLoaderPlugin</span>(),</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">HtmlWebpackPlugin</span>(&#123;</span><br><span class="line">      <span class="attr">template</span>: <span class="string">&quot;./index.html&quot;</span>,</span><br><span class="line">    &#125;),</span><br><span class="line">  ],</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>配置编译命令</p><figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;webpack --config build/webpack.dev.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h4 id="2-3、运行"><a href="#2-3、运行" class="headerlink" title="2.3、运行"></a><strong>2.3、运行</strong></h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yarn build</span><br></pre></td></tr></table></figure><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu2iwvi7j215k0ds751.jpg"></p><h3 id="3、编译结果"><a href="#3、编译结果" class="headerlink" title="3、编译结果"></a><strong>3、编译结果</strong></h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="string">&quot;use strict&quot;</span>;</span><br><span class="line">  <span class="keyword">var</span> __webpack_modules__ = ([])</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">__webpack_require__</span>(<span class="params">moduleId</span>) &#123;&#125;</span><br><span class="line">  </span><br><span class="line">  <span class="comment">//...</span></span><br><span class="line">  </span><br><span class="line">  <span class="keyword">var</span> __webpack_exports__ = &#123;&#125;;</span><br><span class="line">  </span><br><span class="line">  (<span class="function">() =&gt;</span> &#123;</span><br><span class="line">__webpack_require__.<span class="title function_">r</span>(__webpack_exports__);</span><br><span class="line"><span class="comment">/* harmony import */</span> <span class="keyword">var</span> vue__WEBPACK_IMPORTED_MODULE_0__ = <span class="title function_">__webpack_require__</span>(<span class="comment">/*! vue */</span> <span class="string">&quot;./node_modules/vue/dist/vue.runtime.esm-bundler.js&quot;</span>);</span><br><span class="line"><span class="comment">/* harmony import */</span> <span class="keyword">var</span> _App_vue__WEBPACK_IMPORTED_MODULE_1__ = <span class="title function_">__webpack_require__</span>(<span class="comment">/*! ./App.vue */</span> <span class="string">&quot;./src/App.vue&quot;</span>);</span><br><span class="line"><span class="comment">/* harmony import */</span> <span class="keyword">var</span> _assets_css_index_css__WEBPACK_IMPORTED_MODULE_2__ = <span class="title function_">__webpack_require__</span>(<span class="comment">/*! ./assets/css/index.css */</span> <span class="string">&quot;./src/assets/css/index.css&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> app = (<span class="number">0</span>,vue__WEBPACK_IMPORTED_MODULE_0__.<span class="property">createApp</span>)(_App_vue__WEBPACK_IMPORTED_MODULE_1__[<span class="string">&quot;default&quot;</span>]);</span><br><span class="line">  &#125;)();</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure><ul><li>打包后的结果是一个IIFE（匿名闭包）</li><li><strong>webpack_modules</strong> 用来记录当前依赖的所有模块</li><li><strong>webpack_require</strong> 用来加载模块</li><li>最后一段 IIFE 对应的就是我们开头写好的 main.js 文件</li></ul><h3 id="4、实现原理"><a href="#4、实现原理" class="headerlink" title="4、实现原理"></a><strong>4、实现原理</strong></h3><h4 id="4-1、执行过程"><a href="#4-1、执行过程" class="headerlink" title="4.1、执行过程"></a>4.1、执行过程</h4><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu304qxtj21du0ocdhb.jpg"></p><h4 id="4-2、原理分析"><a href="#4-2、原理分析" class="headerlink" title="4.2、原理分析"></a>4.2、原理分析</h4><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu3kzveej214m0kijsv.jpg"></p><h2 id="二、文件监听"><a href="#二、文件监听" class="headerlink" title="二、文件监听"></a><strong>二、文件监听</strong></h2><p>经过构建工具的处理，浏览器已经能运行我们写好的代码了，但是每次修改完看渲染效果都需要手动去编译，这就很影响开发效率。</p><p>webpack 提供了监听文件变化，变化后就自动编译的策略。</p><h3 id="1、使用"><a href="#1、使用" class="headerlink" title="1、使用"></a><strong>1、使用</strong></h3><h4 id="1-1、安装依赖"><a href="#1-1、安装依赖" class="headerlink" title="1.1、安装依赖"></a><strong>1.1、安装依赖</strong></h4><p>同上</p><h4 id="1-2、项目配置"><a href="#1-2、项目配置" class="headerlink" title="1.2、项目配置"></a><strong>1.2、项目配置</strong></h4><p>在配置 webpack.config.js 中设置 watch: true 或者启动 webpack 命令时带上 –watch 参数：</p><figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;webpack --config build/webpack.dev.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;watch&quot;</span><span class="punctuation">:</span> <span class="string">&quot;webpack --config build/webpack.dev.js --watch&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h4 id="1-3、运行"><a href="#1-3、运行" class="headerlink" title="1.3、运行"></a><strong>1.3、运行</strong></h4><p>运行后每次修改完保存后 webpack 都会自动帮我们去编译。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yarn watch</span><br></pre></td></tr></table></figure><h3 id="2、编译结果"><a href="#2、编译结果" class="headerlink" title="2、编译结果"></a><strong>2、编译结果</strong></h3><p>同上</p><h3 id="3、实现原理"><a href="#3、实现原理" class="headerlink" title="3、实现原理"></a><strong>3、实现原理</strong></h3><h4 id="3-1、执行过程"><a href="#3-1、执行过程" class="headerlink" title="3.1、执行过程"></a>3.1、执行过程</h4><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu48vwxlj21bs0ladh9.jpg"></p><p>与一般编译不同的是，在红色区域会自动监听文件变化。</p><h4 id="3-2、原理分析"><a href="#3-2、原理分析" class="headerlink" title="3.2、原理分析"></a>3.2、原理分析</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (watch) &#123;</span><br><span class="line">  compiler.<span class="title function_">watch</span>(watchOptions, callback);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  compiler.<span class="title function_">run</span>(<span class="function">(<span class="params">err, stats</span>) =&gt;</span> &#123;</span><br><span class="line">    compiler.<span class="title function_">close</span>(<span class="function"><span class="params">err2</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="title function_">callback</span>(err || err2, stats);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在未开启文件监听的功能时，webpack 构建走的是 compiler.run 方法，当开启文件监听的功能后，webpack 在进行构建时会走 compiler.watch 方法。</p><p>而且他们构建后的内容是完全相同的，唯一不同的是开启文件监听会监听文件变化自动构建。所以这里我们主要关注两个问题</p><ul><li>webpack 怎么做到的监听</li><li>监听到变化后的处理机制</li></ul><blockquote><p>📌  webpack 怎么做到的监听</p></blockquote><p>Webpack5 是通过 node 的 fs 模块创建监听器来实现文件监听的。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// node_modules/watchpack/lib/watchEventSource.js</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">DirectWatcher</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">filePath</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">filePath</span> = filePath;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">watchers</span> = <span class="keyword">new</span> <span class="title class_">Set</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">watcher</span> = <span class="literal">undefined</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">const</span> watcher = fs.<span class="title function_">watch</span>(filePath);</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">watcher</span> = watcher;</span><br><span class="line">      watcher.<span class="title function_">on</span>(<span class="string">&quot;change&quot;</span>, <span class="function">(<span class="params">type, filename</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">const</span> w <span class="keyword">of</span> <span class="variable language_">this</span>.<span class="property">watchers</span>) &#123;</span><br><span class="line">          w.<span class="title function_">emit</span>(<span class="string">&quot;change&quot;</span>, type, filename);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;);</span><br><span class="line">      watcher.<span class="title function_">on</span>(<span class="string">&quot;error&quot;</span>, <span class="function"><span class="params">error</span> =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">const</span> w <span class="keyword">of</span> <span class="variable language_">this</span>.<span class="property">watchers</span>) &#123;</span><br><span class="line">          w.<span class="title function_">emit</span>(<span class="string">&quot;error&quot;</span>, error);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">      process.<span class="title function_">nextTick</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">const</span> w <span class="keyword">of</span> <span class="variable language_">this</span>.<span class="property">watchers</span>) &#123;</span><br><span class="line">          w.<span class="title function_">emit</span>(<span class="string">&quot;error&quot;</span>, err);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    watcherCount++;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 webpack 5 以前，mac 上文件监听上报有问题，要依赖了第三方包 chokidar，webpack 5 已重构。</p><blockquote><p>📌  监听到变化后的处理机制</p></blockquote><p>webpack 在监听到文件变化后并不会立刻去构建，它有一套自己的处理机制，主要通过 wathcOptions 的 poll 和 aggregateTimeout 属性来控制，poll 属性是控制多长时间去轮询看文件是否变化，aggregateTimeout 属性是经过 poll 属性的轮询检测到文件变化后需要多久才去执行构建。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">module</span>.<span class="property">export</span> = &#123;</span><br><span class="line">  <span class="attr">watch</span>: <span class="literal">true</span>,</span><br><span class="line">  <span class="attr">wathcOptions</span>: &#123; <span class="comment">// 只有开启监听模式时，watchOptions才有意义</span></span><br><span class="line">    <span class="attr">ignored</span>: <span class="regexp">/node_modules/</span>,</span><br><span class="line">    <span class="attr">aggregateTimeout</span>: <span class="number">300</span>, <span class="comment">// 监听到变化发生后会等300ms再去执行</span></span><br><span class="line">    <span class="attr">poll</span>: <span class="number">1000</span> <span class="comment">// 判断文件是否发生变化是通过不停询问系统指定文件有没有变化实现的</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="三、热重载-live-reload"><a href="#三、热重载-live-reload" class="headerlink" title="三、热重载 live reload"></a><strong>三、热重载</strong> <strong>live</strong> <strong>reload</strong></h2><p>文件监听的方式虽然能自动帮我们编译代码，但是每次更新都需要手动刷新浏览器，这个体验是非常不友好的，所以 webpack 利用 webpack-dev-server 实现了不需要手动刷新浏览器，编译后自动刷新浏览器的方式。</p><h3 id="1、使用-1"><a href="#1、使用-1" class="headerlink" title="1、使用"></a><strong>1、使用</strong></h3><h4 id="1-1、安装依赖-1"><a href="#1-1、安装依赖-1" class="headerlink" title="1.1、安装依赖"></a>1.1、安装依赖</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yarn add -D webpack-dev-server</span><br></pre></td></tr></table></figure><h4 id="1-2、项目配置-1"><a href="#1-2、项目配置-1" class="headerlink" title="1.2、项目配置"></a>1.2、项目配置</h4><figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;webpack --config build/webpack.dev.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;watch&quot;</span><span class="punctuation">:</span> <span class="string">&quot;webpack --config build/webpack.dev.js --watch&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;dev&quot;</span><span class="punctuation">:</span> <span class="string">&quot;webpack-dev-server --config build/webpack.dev.js --open&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br></pre></td></tr></table></figure><p>因为 webpack5 默认会开启热更新，所以这里先手动关闭它。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="attr">devServer</span>: &#123;</span><br><span class="line">  <span class="attr">hot</span>: <span class="literal">false</span> <span class="comment">// 为 false 时文件发生变化会刷新浏览器</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="1-3、运行-1"><a href="#1-3、运行-1" class="headerlink" title="1.3、运行"></a>1.3、运行</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yarn dev</span><br></pre></td></tr></table></figure><p>修改完保存代码浏览器就会自动刷新重新加载代码。</p><h3 id="2、编译结果-1"><a href="#2、编译结果-1" class="headerlink" title="2、编译结果"></a>2、<strong>编译结果</strong></h3><p>在原来构建的基础上多了两个模块。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">__webpack_require__</span>(<span class="string">&quot;./node_modules/webpack-dev-server/client/index.js?protocol=ws%3A&amp;hostname=0.0.0.0&amp;port=8080&amp;pathname=%2Fws&amp;logging=info&amp;reconnect=10&quot;</span>);</span><br><span class="line"><span class="comment">/******/</span>         </span><br><span class="line"><span class="title function_">__webpack_require__</span>(<span class="string">&quot;./node_modules/webpack/hot/dev-server.js&quot;</span>);</span><br></pre></td></tr></table></figure><h3 id="3、实现原理-1"><a href="#3、实现原理-1" class="headerlink" title="3、实现原理"></a><strong>3、实现原理</strong></h3><h4 id="3-1、执行过程-1"><a href="#3-1、执行过程-1" class="headerlink" title="3.1、执行过程"></a>3.1、执行过程</h4><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu537c7wj21ik0mhdij.jpg"></p><h4 id="3-2、原理分析-1"><a href="#3-2、原理分析-1" class="headerlink" title="3.2、原理分析"></a>3.2、原理分析</h4><p>这里我们需要关注三个问题：</p><ul><li>热重载和文件监听相比，构建的差异是什么？</li><li>浏览器和服务端怎么做的通讯？具体在传递什么信息？</li><li>浏览器怎么做到的更新？</li></ul><p>热重载和文件监听相比，构建的差异是什么？</p><p>相比较文件监听，热重载会通过 webpack-dev-server 调用 express 起了一个 node 服务。</p><p>然后创建 <em>Compiler</em> <em>实例，之后也会走</em> <em>Compiler.watch</em> <em>方法执行构建。</em></p><p><em>不同的是编译后的内容没有输出到磁盘，而是输出到了内存</em>，提供一个地址去让你访问。</p><img src="https://cdn.nlark.com/yuque/0/2022/jpeg/10367031/1659883287850-6fc6c9b9-8b40-48be-9cf9-f118a3acae7b.jpeg" referrerpolicy="no-referrer" alt="img"><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> express = <span class="built_in">require</span>(<span class="string">&quot;express&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">/** <span class="doctag">@typedef</span> &#123;<span class="type">import(&quot;webpack&quot;).Compiler</span>&#125; Compiler */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Server</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">options = &#123;&#125;, compiler</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">compiler</span> = <span class="comment">/** <span class="doctag">@type</span> &#123;<span class="type">Compiler | MultiCompiler</span>&#125; */</span> (compiler);</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="title function_">setupApp</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="comment">/** <span class="doctag">@type</span> &#123;<span class="type">import(&quot;express&quot;).Application | undefined</span>&#125;*/</span></span><br><span class="line">    <span class="comment">// eslint-disable-next-line new-cap</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">app</span> = <span class="keyword">new</span> <span class="comment">/** <span class="doctag">@type</span> &#123;<span class="type">any</span>&#125; */</span> (express)();</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="title function_">setupDevMiddleware</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> webpackDevMiddleware = <span class="built_in">require</span>(<span class="string">&quot;webpack-dev-middleware&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// middleware for serving webpack bundle</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">middleware</span> = <span class="title function_">webpackDevMiddleware</span>(</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">compiler</span>,</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">devMiddleware</span></span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">start</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> listenOptions = <span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">ipc</span></span><br><span class="line">      ? &#123; <span class="attr">path</span>: <span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">ipc</span> &#125;</span><br><span class="line">      : &#123; <span class="attr">host</span>: <span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">host</span>, <span class="attr">port</span>: <span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">port</span> &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">await</span> <span class="comment">/** <span class="doctag">@type</span> &#123;<span class="type">Promise&lt;void&gt;</span>&#125; */</span> (</span><br><span class="line">      <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="function">(<span class="params">resolve</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="comment">/** <span class="doctag">@type</span> &#123;<span class="type">import(&quot;http&quot;).Server</span>&#125; */</span></span><br><span class="line">        (<span class="variable language_">this</span>.<span class="property">server</span>).<span class="title function_">listen</span>(listenOptions, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">          <span class="title function_">resolve</span>();</span><br><span class="line">        &#125;);</span><br><span class="line">      &#125;)</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line"> </span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">initialize</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">setupHooks</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">setupApp</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">setupHostHeaderCheck</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">setupDevMiddleware</span>();</span><br><span class="line">    <span class="comment">// Should be after `webpack-dev-middleware`, otherwise other middlewares might rewrite response</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">setupBuiltInRoutes</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">setupWatchFiles</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">setupWatchStaticFiles</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">setupMiddlewares</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">createServer</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>浏览器和服务端怎么做的通讯？具体在传递什么信息？</p><p>访问 webpack 提供的地址，打开控制台如下，可以看到浏览器和服务器之间有建立 websocket 通信的信息，但是我们开发的时候代码里并没有做这些额外的事情。我们就需要去看 webpack 是在哪里做的这些事。</p><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu5zn09xj21a60u0q5y.jpg"></p><p>通过探寻我们可以在发现 webpack 在开启本地服务前对 webpack 的配置入口做了修改。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 添加 client socket 代码</span></span><br><span class="line">additionalEntries.<span class="title function_">push</span>(<span class="string">`<span class="subst">$&#123;<span class="built_in">require</span>.resolve(<span class="string">&quot;../client/index.js&quot;</span>)&#125;</span>?<span class="subst">$&#123;webSocketURLStr&#125;</span>`</span>);</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 添加 webpack dev-server 代码</span></span><br><span class="line"><span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">hot</span> === <span class="string">&quot;only&quot;</span>) &#123;</span><br><span class="line">  additionalEntries.<span class="title function_">push</span>(<span class="built_in">require</span>.<span class="title function_">resolve</span>(<span class="string">&quot;webpack/hot/only-dev-server&quot;</span>));</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">options</span>.<span class="property">hot</span>) &#123;</span><br><span class="line">  additionalEntries.<span class="title function_">push</span>(<span class="built_in">require</span>.<span class="title function_">resolve</span>(<span class="string">&quot;webpack/hot/dev-server&quot;</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> webpack = compiler.<span class="property">webpack</span> || <span class="built_in">require</span>(<span class="string">&quot;webpack&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 引入一些需要额外打包的内容</span></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">typeof</span> webpack.<span class="property">EntryPlugin</span> !== <span class="string">&quot;undefined&quot;</span>) &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> additionalEntry <span class="keyword">of</span> additionalEntries) &#123;</span><br><span class="line">    <span class="keyword">new</span> webpack.<span class="title class_">EntryPlugin</span>(compiler.<span class="property">context</span>, additionalEntry, &#123;</span><br><span class="line">      <span class="attr">name</span>: <span class="literal">undefined</span>,</span><br><span class="line">    &#125;).<span class="title function_">apply</span>(compiler);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里 webpack 帮我们的入口又引入了两个额外的 module 来打包，其中引入的第一个 module 是与服务端建立 websocket 连接用的，第二个 module 就是客户端在接收到文件变化的通知后做处理用的。</p><p>所以浏览器之所以能感应到本地文件变化是本地服务主动推送的缘故。具体是服务端在监听到文件变化后会向客户端发送 hash 事件和 ok 的事件。hash 事件主要返回一个 hash 值，用来对每次的编译结果做标识，ok 事件是通知浏览器进行热重载用的。</p><p>hash 事件是啥具体下面热更新会说</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">sendStats</span>(<span class="params">clients, stats, force</span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="title function_">sendMessage</span>(clients, <span class="string">&quot;hash&quot;</span>, stats.<span class="property">hash</span>);</span><br><span class="line">  <span class="variable language_">this</span>.<span class="title function_">sendMessage</span>(clients, <span class="string">&quot;ok&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>浏览器怎么做到的更新？</p><p>当检测到需要更新时就调用 reloadApp 刷新浏览器请求最新的资源，其中关键的代码是 <em>window</em>.location.reload()。这就是为什么浏览器会自动刷新的缘故。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">reloadApp</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (liveReload &amp;&amp; allowToLiveReload) &#123;</span><br><span class="line">    <span class="keyword">var</span> rootWindow = self; <span class="comment">// use parent window for reload (in case we&#x27;re in an iframe with no valid src)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> intervalId = self.<span class="built_in">setInterval</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (rootWindow.<span class="property">location</span>.<span class="property">protocol</span> !== <span class="string">&quot;about:&quot;</span>) &#123;</span><br><span class="line">        <span class="comment">// reload immediately if protocol is valid</span></span><br><span class="line">        <span class="title function_">applyReload</span>(rootWindow, intervalId);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        rootWindow = rootWindow.<span class="property">parent</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (rootWindow.<span class="property">parent</span> === rootWindow) &#123;</span><br><span class="line">          <span class="comment">// if parent equals current window we&#x27;ve reached the root which would continue forever, so trigger a reload anyways</span></span><br><span class="line">          <span class="title function_">applyReload</span>(rootWindow, intervalId);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">applyReload</span>(<span class="params">rootWindow, intervalId</span>) &#123;</span><br><span class="line">    <span class="built_in">clearInterval</span>(intervalId);</span><br><span class="line">    log.<span class="title function_">info</span>(<span class="string">&quot;App updated. Reloading...&quot;</span>);</span><br><span class="line">    rootWindow.<span class="property">location</span>.<span class="title function_">reload</span>();</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><h2 id="四、热更新-HMR（热替换）"><a href="#四、热更新-HMR（热替换）" class="headerlink" title="四、热更新 HMR（热替换）"></a><strong>四、热更新 HMR（热替换）</strong></h2><p>上面介绍的热重载虽然在一定程度上提高了开发效率，满足了我们的基本开发诉求，但是还有一个痛点就是不能在接收变化后保留之前的状态做局部更新，这样在测试一些功能的时候，比如输入框输入一些数据后才能做点击的测试，那我们每次更新完就必须重新输一遍再开始测试，这是非常低效的，热更新就很好的解决了这个问题。</p><h3 id="1、使用-2"><a href="#1、使用-2" class="headerlink" title="1、使用"></a><strong>1、使用</strong></h3><h4 id="1-1、安装依赖-2"><a href="#1-1、安装依赖-2" class="headerlink" title="1.1、安装依赖"></a>1.1、安装依赖</h4><p>同上</p><h4 id="1-2、项目配置-2"><a href="#1-2、项目配置-2" class="headerlink" title="1.2、项目配置"></a>1.2、项目配置</h4><p>将 hot 的值修改为 true 或去掉该配置（默认值 true）。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="attr">devServer</span>: &#123;</span><br><span class="line">  <span class="attr">hot</span>: <span class="literal">true</span> <span class="comment">// 为 false 时文件发生变化会刷新浏览器</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="1-3、运行-2"><a href="#1-3、运行-2" class="headerlink" title="1.3、运行"></a>1.3、运行</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yarn dev</span><br></pre></td></tr></table></figure><h3 id="2、编译结果-2"><a href="#2、编译结果-2" class="headerlink" title="2、编译结果"></a>2、<strong>编译结果</strong></h3><p>同上</p><h3 id="3、实现原理-2"><a href="#3、实现原理-2" class="headerlink" title="3、实现原理"></a><strong>3、实现原理</strong></h3><h4 id="3-1、执行过程-2"><a href="#3-1、执行过程-2" class="headerlink" title="3.1、执行过程"></a>3.1、执行过程</h4><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu537c7wj21ik0mhdij.jpg"></p><h4 id="3-2、原理分析-2"><a href="#3-2、原理分析-2" class="headerlink" title="3.2、原理分析"></a>3.2、原理分析</h4><p>这里主要关注两个问题是：</p><ul><li>和热重载相比，热更新编译的差异是什么？</li><li>浏览器在监听到文件变化后做了什么事情？</li></ul><p>和热重载相比，热更新编译的差异是什么？</p><p>实例化 HotModuleReplacementPlugin 插件，并监听对应的 hooks。</p><p>webpack4 需要手动引入</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> plugin = <span class="keyword">new</span> webpack.<span class="title class_">HotModuleReplacementPlugin</span>();</span><br><span class="line"></span><br><span class="line">plugin.<span class="title function_">apply</span>(compiler);</span><br></pre></td></tr></table></figure><p>首先会定义 module.hot 相关 api 的 dependency 以及 template</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">HotModuleReplacementPlugin</span> &#123;</span><br><span class="line">  <span class="title function_">apply</span>(<span class="params">compiler</span>) &#123;</span><br><span class="line">    compiler.<span class="property">hooks</span>.<span class="property">compilation</span>.<span class="title function_">tap</span>(</span><br><span class="line">      <span class="string">&quot;HotModuleReplacementPlugin&quot;</span>,</span><br><span class="line">      <span class="function">(<span class="params">compilation, &#123; normalModuleFactory &#125;</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="comment">//#region module.hot.* API</span></span><br><span class="line">        compilation.<span class="property">dependencyFactories</span>.<span class="title function_">set</span>(</span><br><span class="line">          <span class="title class_">ModuleHotAcceptDependency</span>,</span><br><span class="line">          normalModuleFactory</span><br><span class="line">        );</span><br><span class="line">        compilation.<span class="property">dependencyTemplates</span>.<span class="title function_">set</span>(</span><br><span class="line">          <span class="title class_">ModuleHotAcceptDependency</span>,</span><br><span class="line">          <span class="keyword">new</span> <span class="title class_">ModuleHotAcceptDependency</span>.<span class="title class_">Template</span>()</span><br><span class="line">        );</span><br><span class="line">        compilation.<span class="property">dependencyFactories</span>.<span class="title function_">set</span>(</span><br><span class="line">          <span class="title class_">ModuleHotDeclineDependency</span>,</span><br><span class="line">          normalModuleFactory</span><br><span class="line">        );</span><br><span class="line">        compilation.<span class="property">dependencyTemplates</span>.<span class="title function_">set</span>(</span><br><span class="line">          <span class="title class_">ModuleHotDeclineDependency</span>,</span><br><span class="line">          <span class="keyword">new</span> <span class="title class_">ModuleHotDeclineDependency</span>.<span class="title class_">Template</span>()</span><br><span class="line">        );</span><br><span class="line">      &#125;</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后监听parser阶段，对module.hot等api进行解析，例如：</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">parser.<span class="property">hooks</span>.<span class="property">call</span></span><br><span class="line">  .<span class="title function_">for</span>(<span class="string">&quot;module.hot.accept&quot;</span>)</span><br><span class="line">  .<span class="title function_">tap</span>(</span><br><span class="line">  <span class="string">&quot;HotModuleReplacementPlugin&quot;</span>,</span><br><span class="line">  <span class="title function_">createAcceptHandler</span>(parser, <span class="title class_">ModuleHotAcceptDependency</span>)</span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>因此module.hot等api经过parser后会变为相应的dependency。在code generate时，调用对应的template生成新的代码</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 转换前</span></span><br><span class="line"><span class="keyword">if</span> (<span class="variable language_">module</span>.<span class="property">hot</span>) &#123;</span><br><span class="line">  <span class="variable language_">module</span>.<span class="property">hot</span>.<span class="title function_">accept</span>([<span class="string">&#x27;./moduleB.js&#x27;</span>], <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;======&gt;  accept B&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转换后</span></span><br><span class="line"><span class="keyword">if</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">  <span class="variable language_">module</span>.<span class="property">hot</span>.<span class="title function_">accept</span>([<span class="comment">/*! ./moduleB.js */</span> <span class="string">&quot;./src/moduleB.js&quot;</span>], <span class="function"><span class="params">__WEBPACK_OUTDATED_DEPENDENCIES__</span> =&gt;</span> &#123; </span><br><span class="line">    <span class="comment">/* harmony import */</span> _moduleB__WEBPACK_IMPORTED_MODULE_0__ = <span class="title function_">__webpack_require__</span>(<span class="comment">/*! ./moduleB.js */</span> <span class="string">&quot;./src/moduleB.js&quot;</span>);</span><br><span class="line">    (<span class="function">() =&gt;</span> &#123;</span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;======&gt;  accept B&#x27;</span>)</span><br><span class="line">    &#125;)(__WEBPACK_OUTDATED_DEPENDENCIES__); </span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>浏览器在监听到文件变化后做了什么事情？</p><p>要知道浏览器在监听到文件变化后做了什么事情要先回到服务端监听到文件变化给客户端发送 hash 事件和 ok 的事件的时候，我们先看 hash 事件具体做了什么。</p><p>刚打开页面时</p><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu74019yj21ik0aujtd.jpg"></p><p>修改代码保存后</p><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu7c4rzlj21ik0dtq5k.jpg"></p><p>观察可以发现修改完代码后，浏览器新加载的文件后缀是上次的 hash，所以 Hash 值代表每一次编译的标识，上次输出的 Hash 值会作为本次编译新生成的文件标识。</p><p>知道了 hash 事件的概念后，我们再回到服务端向浏览器发送 hash 事件和 ok 事件，浏览器会调用 reloadApp 方法。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// node_modules/webpack-dev-server/client/index.js</span></span><br><span class="line"><span class="keyword">var</span> onSocketMessage = &#123;</span><br><span class="line">  <span class="attr">hash</span>: <span class="keyword">function</span> <span class="title function_">hash</span>(<span class="params">_hash</span>) &#123;</span><br><span class="line">    status.<span class="property">previousHash</span> = status.<span class="property">currentHash</span>;</span><br><span class="line">    status.<span class="property">currentHash</span> = _hash;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">ok</span>: <span class="keyword">function</span> <span class="title function_">ok</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">sendMessage</span>(<span class="string">&quot;Ok&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (options.<span class="property">overlay</span>) &#123;</span><br><span class="line">      <span class="title function_">hide</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">reloadApp</span>(options, status);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">reloadApp</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (hot &amp;&amp; allowToHot) &#123;</span><br><span class="line">    log.<span class="title function_">info</span>(<span class="string">&quot;App hot update...&quot;</span>);</span><br><span class="line">    hotEmitter.<span class="title function_">emit</span>(<span class="string">&quot;webpackHotUpdate&quot;</span>, status.<span class="property">currentHash</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> self !== <span class="string">&quot;undefined&quot;</span> &amp;&amp; self.<span class="property">window</span>) &#123;</span><br><span class="line">      <span class="comment">// broadcast update to window</span></span><br><span class="line">      self.<span class="title function_">postMessage</span>(<span class="string">&quot;webpackHotUpdate&quot;</span>.<span class="title function_">concat</span>(status.<span class="property">currentHash</span>), <span class="string">&quot;*&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (liveReload &amp;&amp; allowToLiveReload) &#123;</span><br><span class="line">    <span class="variable language_">window</span>.<span class="property">location</span>.<span class="title function_">reload</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接着触发 webpackHotUpdate 事件，经过一系列的检查后根据上次的 hash 值发送 fetch 请求获取需要更新的模块名。</p><img src="https://cdn.nlark.com/yuque/0/2022/png/10367031/1659883436219-7d07db68-e11c-4f4b-aba6-390a822e09c2.png" referrerpolicy="no-referrer" alt=""><p>再通过 JSONP 的方式请求这个新的模块，并执行相关模块的代码，这就起到了局部更新的作用。</p><h2 id="五、总结"><a href="#五、总结" class="headerlink" title="五、总结"></a><strong>五、总结</strong></h2><p>webpack compiler 对文件进行打包，打包好后把编译好的文件传输给 bundleServer， bundleServer 就以服务器的方式让浏览器访问。（1-2-A-B）</p><p>文件发生变化后，会再经过 webpack compiler 进行编译，编译后将代码发送给 HMR Server，HMR Server 知道哪些模块发生了改变，然后 HMR server 会以 websocket 的方式通知 HRM Runtime，HRM Runtime 通过 JSONP 的方式获取更新的内容，最后 HMR runtime 会根据返回的新模块代码做进一步处理，可能是刷新页面，也可能是对模块进行热更新。（1-2-3-4）</p><p><img src="/2021/12/25/webpack%20%E7%83%AD%E6%9B%B4%E6%96%B0%E5%8E%9F%E7%90%86/e6c9d24ely1h6hu537c7wj21ik0mhdij.jpg"></p><h2 id="六、思考"><a href="#六、思考" class="headerlink" title="六、思考"></a>六、思考</h2><h3 id="1、构建工具热更新能力对比"><a href="#1、构建工具热更新能力对比" class="headerlink" title="1、构建工具热更新能力对比"></a>1、构建工具热更新能力对比</h3><p>gulp：支持热重载，热更新目前没发现支持特别好的包，需要自己实现。</p><table><thead><tr><th>工具</th><th>webpack</th><th>vite</th></tr></thead><tbody><tr><td>配置</td><td>简单</td><td>简单</td></tr><tr><td>实现原理</td><td>webpack4 文件监听用的 chokidar，webpack5 用的 fs.watchwebpack4 依赖 vue-hot-reload-api 实现模块热替换，webpack5 自己实现基于bundle进行构建</td><td>vite2 文件监听用的 chokidarvite2 依赖 vue-hot-reload-api 实现模块热替换基于bundleless进行构建</td></tr><tr><td>速度</td><td>冷启动时间长热更新能力受项目体积的影响</td><td>冷启动时间短热更新能力不受项目体积的影响</td></tr><tr><td>使用场景</td><td>对构建内容有严格要求且构建结果与平台架构关系紧密</td><td>对构建内容无严格要求</td></tr></tbody></table><h2 id="七、项目地址"><a href="#七、项目地址" class="headerlink" title="七、项目地址"></a><strong>七、项目地址</strong></h2><p><a href="https://github.com/Yin-Hongwei/webpack-hmr-demo">https://github.com/Yin-Hongwei/webpack-hmr-demo</a></p>]]></content>
      
      
      
        <tags>
            
            <tag> webpack </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vue watcher 分类</title>
      <link href="/2021/12/04/Vue%20watcher%20%E5%88%86%E7%B1%BB/"/>
      <url>/2021/12/04/Vue%20watcher%20%E5%88%86%E7%B1%BB/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>知道 Vue 响应式原理的都知道，每个组件都对应有一个自己的 watcher 实例，但是除了组件对应的 watcher 实例，computed 和 watch 也都分别对应有自己的 watcher 实例，所以下面我们就针对这三种 watcher 做一下研究，知道了底层实现，也能更好的让我们在使用中做出更好的抉择。</p><span id="more"></span><p>Watcher 的初始化发生在 Vue 实例初始化阶段的 <code>initState</code> 函数中。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Vue</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">_init</span> = <span class="keyword">function</span> (<span class="params">options?: <span class="built_in">Object</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="attr">vm</span>: <span class="title class_">Component</span> = <span class="variable language_">this</span></span><br><span class="line"><span class="comment">// ..</span></span><br><span class="line">  <span class="title function_">initLifecycle</span>(vm)</span><br><span class="line">  <span class="title function_">initEvents</span>(vm)</span><br><span class="line">  <span class="title function_">initRender</span>(vm)</span><br><span class="line">  <span class="title function_">callHook</span>(vm, <span class="string">&#x27;beforeCreate&#x27;</span>)</span><br><span class="line">  <span class="title function_">initInjections</span>(vm) <span class="comment">// resolve injections before data/props</span></span><br><span class="line">  <span class="title function_">initState</span>(vm)</span><br><span class="line">  <span class="title function_">initProvide</span>(vm) <span class="comment">// resolve provide after data/props</span></span><br><span class="line">  <span class="title function_">callHook</span>(vm, <span class="string">&#x27;created&#x27;</span>)</span><br><span class="line"><span class="comment">// ..</span></span><br><span class="line">  <span class="keyword">if</span> (vm.<span class="property">$options</span>.<span class="property">el</span>) &#123;</span><br><span class="line">    vm.$mount(vm.<span class="property">$options</span>.<span class="property">el</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">initState</span> (<span class="params">vm: Component</span>) &#123;</span><br><span class="line">  vm.<span class="property">_watchers</span> = []</span><br><span class="line">  <span class="keyword">const</span> opts = vm.<span class="property">$options</span></span><br><span class="line">  <span class="keyword">if</span> (opts.<span class="property">props</span>) <span class="title function_">initProps</span>(vm, opts.<span class="property">props</span>)</span><br><span class="line">  <span class="keyword">if</span> (opts.<span class="property">methods</span>) <span class="title function_">initMethods</span>(vm, opts.<span class="property">methods</span>)</span><br><span class="line">  <span class="keyword">if</span> (opts.<span class="property">data</span>) &#123;</span><br><span class="line">    <span class="title function_">initData</span>(vm)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="title function_">observe</span>(vm.<span class="property">_data</span> = &#123;&#125;, <span class="literal">true</span> <span class="comment">/* asRootData */</span>)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (opts.<span class="property">computed</span>) <span class="title function_">initComputed</span>(vm, opts.<span class="property">computed</span>)</span><br><span class="line">  <span class="keyword">if</span> (opts.<span class="property">watch</span> &amp;&amp; opts.<span class="property">watch</span> !== nativeWatch) &#123;</span><br><span class="line">    <span class="title function_">initWatch</span>(vm, opts.<span class="property">watch</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里先创建了 computed 和 watch 的 watcher 实例，组件的 watcher 实例是在执行 vm.$mount 后创建的，所以这三种 watcher 执行顺序为  computed watcher &#x3D;&gt; user watcher &#x3D;&gt; render watcher。</p><h2 id="一、computed-watcher"><a href="#一、computed-watcher" class="headerlink" title="一、computed watcher"></a>一、computed watcher</h2><h3 id="1、数据劫持"><a href="#1、数据劫持" class="headerlink" title="1、数据劫持"></a>1、数据劫持</h3><p>计算属性的初始化是发生在 Vue 实例初始化阶段的 <code>initState</code> 函数中。先遍历 <code>computed</code> 对象，为每一个计算属性创建一个 <strong>computed watcher</strong>。然后判断如果该计算属性不是 <code>vm</code> 的属性，则调用 <code>defineComputed(vm, key, userDef)</code>，否则判断计算属性是否已经被 <code>data</code> 或 <code>prop</code> 占用，如果占用则在开发环境报相应警告。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">initComputed</span> (<span class="params">vm: Component, computed: <span class="built_in">Object</span></span>) &#123;</span><br><span class="line">  <span class="comment">// $flow-disable-line</span></span><br><span class="line">  <span class="keyword">const</span> watchers = vm.<span class="property">_computedWatchers</span> = <span class="title class_">Object</span>.<span class="title function_">create</span>(<span class="literal">null</span>)</span><br><span class="line">  <span class="comment">// computed properties are just getters during SSR</span></span><br><span class="line">  <span class="keyword">const</span> isSSR = <span class="title function_">isServerRendering</span>()</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> key <span class="keyword">in</span> computed) &#123;</span><br><span class="line">    <span class="keyword">const</span> userDef = computed[key]</span><br><span class="line">    <span class="keyword">const</span> getter = <span class="keyword">typeof</span> userDef === <span class="string">&#x27;function&#x27;</span> ? userDef : userDef.<span class="property">get</span></span><br><span class="line">    <span class="keyword">if</span> (process.<span class="property">env</span>.<span class="property">NODE_ENV</span> !== <span class="string">&#x27;production&#x27;</span> &amp;&amp; getter == <span class="literal">null</span>) &#123;</span><br><span class="line">      <span class="title function_">warn</span>(</span><br><span class="line">        <span class="string">`Getter is missing for computed property &quot;<span class="subst">$&#123;key&#125;</span>&quot;.`</span>,</span><br><span class="line">        vm</span><br><span class="line">      )</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!isSSR) &#123;</span><br><span class="line">      <span class="comment">// create internal watcher for the computed property.</span></span><br><span class="line">      watchers[key] = <span class="keyword">new</span> <span class="title class_">Watcher</span>(</span><br><span class="line">        vm,</span><br><span class="line">        getter || noop,</span><br><span class="line">        noop,</span><br><span class="line">        computedWatcherOptions <span class="comment">// computedWatcherOptions = &#123; computed: true &#125;</span></span><br><span class="line">      )</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// component-defined computed properties are already defined on the</span></span><br><span class="line">    <span class="comment">// component prototype. We only need to define computed properties defined</span></span><br><span class="line">    <span class="comment">// at instantiation here.</span></span><br><span class="line">    <span class="keyword">if</span> (!(key <span class="keyword">in</span> vm)) &#123;</span><br><span class="line">      <span class="title function_">defineComputed</span>(vm, key, userDef)</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (process.<span class="property">env</span>.<span class="property">NODE_ENV</span> !== <span class="string">&#x27;production&#x27;</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (key <span class="keyword">in</span> vm.<span class="property">$data</span>) &#123;</span><br><span class="line">        <span class="title function_">warn</span>(<span class="string">`The computed property &quot;<span class="subst">$&#123;key&#125;</span>&quot; is already defined in data.`</span>, vm)</span><br><span class="line">      &#125; <span class="keyword">else</span> <span class="keyword">if</span> (vm.<span class="property">$options</span>.<span class="property">props</span> &amp;&amp; key <span class="keyword">in</span> vm.<span class="property">$options</span>.<span class="property">props</span>) &#123;</span><br><span class="line">        <span class="title function_">warn</span>(<span class="string">`The computed property &quot;<span class="subst">$&#123;key&#125;</span>&quot; is already defined as a prop.`</span>, vm)</span><br><span class="line">      &#125; <span class="keyword">else</span> <span class="keyword">if</span> (vm.<span class="property">$options</span>.<span class="property">methods</span> &amp;&amp; key <span class="keyword">in</span> vm.<span class="property">$options</span>.<span class="property">methods</span>) &#123;</span><br><span class="line">        <span class="title function_">warn</span>(<span class="string">`The computed property &quot;<span class="subst">$&#123;key&#125;</span>&quot; is already defined as a method.`</span>, vm)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>利用 <code>Object.defineProperty</code> 给每一个计算属性添加 getter 和 setter。setter 通常是计算属性是一个对象，并且拥有 <code>set</code> 方法的时候才有，否则是一个空函数。在平时的开发场景中，计算属性有 setter 的情况比较少。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">defineComputed</span> (<span class="params"></span></span><br><span class="line"><span class="params">  target: any,</span></span><br><span class="line"><span class="params">  key: string,</span></span><br><span class="line"><span class="params">  userDef: <span class="built_in">Object</span> | <span class="built_in">Function</span></span></span><br><span class="line"><span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> shouldCache = !<span class="title function_">isServerRendering</span>()</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> userDef === <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">    sharedPropertyDefinition.<span class="property">get</span> = shouldCache</span><br><span class="line">      ? <span class="title function_">createComputedGetter</span>(key)</span><br><span class="line">      : userDef</span><br><span class="line">    sharedPropertyDefinition.<span class="property">set</span> = noop</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    sharedPropertyDefinition.<span class="property">get</span> = userDef.<span class="property">get</span></span><br><span class="line">      ? shouldCache &amp;&amp; userDef.<span class="property">cache</span> !== <span class="literal">false</span></span><br><span class="line">        ? <span class="title function_">createComputedGetter</span>(key)</span><br><span class="line">        : userDef.<span class="property">get</span></span><br><span class="line">      : noop</span><br><span class="line">    sharedPropertyDefinition.<span class="property">set</span> = userDef.<span class="property">set</span></span><br><span class="line">      ? userDef.<span class="property">set</span></span><br><span class="line">      : noop</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (process.<span class="property">env</span>.<span class="property">NODE_ENV</span> !== <span class="string">&#x27;production&#x27;</span> &amp;&amp;</span><br><span class="line">      sharedPropertyDefinition.<span class="property">set</span> === noop) &#123;</span><br><span class="line">    sharedPropertyDefinition.<span class="property">set</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">      <span class="title function_">warn</span>(</span><br><span class="line">        <span class="string">`Computed property &quot;<span class="subst">$&#123;key&#125;</span>&quot; was assigned to but it has no setter.`</span>,</span><br><span class="line">        <span class="variable language_">this</span></span><br><span class="line">      )</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(target, key, sharedPropertyDefinition)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createComputedGetter</span> (<span class="params">key</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">function</span> <span class="title function_">computedGetter</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> watcher = <span class="variable language_">this</span>.<span class="property">_computedWatchers</span> &amp;&amp; <span class="variable language_">this</span>.<span class="property">_computedWatchers</span>[key]</span><br><span class="line">    <span class="keyword">if</span> (watcher) &#123;</span><br><span class="line">      <span class="keyword">if</span> (watcher.<span class="property">dirty</span>) &#123;</span><br><span class="line">        watcher.<span class="title function_">evaluate</span>()</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (<span class="title class_">Dep</span>.<span class="property">target</span>) &#123;</span><br><span class="line">        watcher.<span class="title function_">depend</span>()</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> watcher.<span class="property">value</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2、依赖收集"><a href="#2、依赖收集" class="headerlink" title="2、依赖收集"></a>2、依赖收集</h3><p>当调用 <code>render</code> 函数访问到计算属性的时候，就触发了计算属性的 <code>getter</code> 进行求值，将 <code>dirty</code> 设为 <code>false</code>，用于<strong>缓存</strong>。 <code>getter</code> 会触发依赖的属性去收集 <strong>render watcher</strong> 和此时的 <strong>computed watcher</strong> 。</p><h3 id="3、依赖更新"><a href="#3、依赖更新" class="headerlink" title="3、依赖更新"></a>3、依赖更新</h3><p>一旦我们对计算属性依赖的数据做修改，则会触发 setter ，通知所有订阅它变化的 <code>watcher</code> 更新，包括 <strong>render watcher</strong> 和 <strong>computed watcher</strong> 。对于 <strong>computed watcher</strong> 就是将 <code>dirty</code> 设为 <code>true</code>。对于 <strong>render watcher</strong> 会执行 <code>Watcher</code> 实例的 <code>run</code> 方法重新执行组件的<code>render</code>函数。这样在渲染组件的过程中又会触发计算属性，然后继续对计算属性进行求值，使得依赖的属性继续收集 <strong>computed watcher</strong>。</p><h2 id="二、user-watcher"><a href="#二、user-watcher" class="headerlink" title="二、user watcher"></a>二、user watcher</h2><h3 id="1、数据劫持-1"><a href="#1、数据劫持-1" class="headerlink" title="1、数据劫持"></a>1、数据劫持</h3><p>侦听属性的初始化也是发生在 Vue 的实例初始化阶段的 <code>initState</code> 函数中。遍历 <code>watch</code> 对象，拿到每一个 <code>handler</code>，因为 Vue 是支持 <code>watch</code> 的同一个 <code>key</code> 对应多个 <code>handler</code>，所以如果 <code>handler</code> 是一个数组，则遍历这个数组，每一项调用 <code>createWatcher</code> 方法，否则直接调用 <code>createWatcher</code>。然后创建一个 <strong>user watcher</strong>。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">initWatch</span> (<span class="params">vm: Component, watch: <span class="built_in">Object</span></span>) &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> key <span class="keyword">in</span> watch) &#123;</span><br><span class="line">    <span class="keyword">const</span> handler = watch[key]</span><br><span class="line">    <span class="keyword">if</span> (<span class="title class_">Array</span>.<span class="title function_">isArray</span>(handler)) &#123;</span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; handler.<span class="property">length</span>; i++) &#123;</span><br><span class="line">        <span class="title function_">createWatcher</span>(vm, key, handler[i])</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="title function_">createWatcher</span>(vm, key, handler)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createWatcher</span> (<span class="params"></span></span><br><span class="line"><span class="params">  vm: Component,</span></span><br><span class="line"><span class="params">  expOrFn: string | <span class="built_in">Function</span>,</span></span><br><span class="line"><span class="params">  handler: any,</span></span><br><span class="line"><span class="params">  options?: <span class="built_in">Object</span></span></span><br><span class="line"><span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="title function_">isPlainObject</span>(handler)) &#123;</span><br><span class="line">    options = handler</span><br><span class="line">    handler = handler.<span class="property">handler</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> handler === <span class="string">&#x27;string&#x27;</span>) &#123;</span><br><span class="line">    handler = vm[handler]</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> vm.$watch(expOrFn, handler, options)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Vue</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">$watch</span> = <span class="keyword">function</span> (<span class="params"></span></span><br><span class="line"><span class="params">  expOrFn: string | <span class="built_in">Function</span>,</span></span><br><span class="line"><span class="params">  cb: any,</span></span><br><span class="line"><span class="params">  options?: <span class="built_in">Object</span></span></span><br><span class="line"><span class="params"></span>): <span class="title class_">Function</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="attr">vm</span>: <span class="title class_">Component</span> = <span class="variable language_">this</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="title function_">isPlainObject</span>(cb)) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">createWatcher</span>(vm, expOrFn, cb, options)</span><br><span class="line">  &#125;</span><br><span class="line">  options = options || &#123;&#125;</span><br><span class="line">  options.<span class="property">user</span> = <span class="literal">true</span></span><br><span class="line">  <span class="keyword">const</span> watcher = <span class="keyword">new</span> <span class="title class_">Watcher</span>(vm, expOrFn, cb, options)</span><br><span class="line">  <span class="keyword">if</span> (options.<span class="property">immediate</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> info = <span class="string">`callback for immediate watcher &quot;<span class="subst">$&#123;watcher.expression&#125;</span>&quot;`</span></span><br><span class="line">    <span class="title function_">pushTarget</span>()</span><br><span class="line">    <span class="title function_">invokeWithErrorHandling</span>(cb, vm, [watcher.<span class="property">value</span>], vm, info)</span><br><span class="line">    <span class="title function_">popTarget</span>()</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">function</span> <span class="title function_">unwatchFn</span> (<span class="params"></span>) &#123;</span><br><span class="line">    watcher.<span class="title function_">teardown</span>()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2、依赖收集-1"><a href="#2、依赖收集-1" class="headerlink" title="2、依赖收集"></a>2、依赖收集</h3><p>当初始化试图或更新试图访问属性的 getter 方法时，会触发属性去收集 <strong>render watcher</strong> 和对应的的 <strong>user watcher</strong> 。</p><h3 id="3、依赖更新-1"><a href="#3、依赖更新-1" class="headerlink" title="3、依赖更新"></a>3、依赖更新</h3><p>一旦我们对数据做修改，则会触发 setter ，通知所有订阅它变化的 <code>watcher</code> 更新，包括 <strong>render watcher</strong> 和 <strong>user watcher</strong> 。</p><h4 id="4、其他特性"><a href="#4、其他特性" class="headerlink" title="4、其他特性"></a>4、其他特性</h4><h5 id="4-1、deep"><a href="#4-1、deep" class="headerlink" title="4.1、deep"></a>4.1、deep</h5><p>如果我们想对一下对象做深度观测的时候，需要设置这个属性为 true。</p><p>这个时候是不会 log 任何数据的，因为我们是 watch 了 <code>a</code> 对象，只触发了 <code>a</code> 的 getter，并没有触发 <code>a.b</code> 的 getter，所以并没有订阅它的变化，导致我们对 <code>vm.a.b = 2</code> 赋值的时候，虽然触发了 setter，但没有可通知的对象，所以也并不会触发 watch 的回调函数了。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> vm = <span class="keyword">new</span> <span class="title class_">Vue</span>(&#123;</span><br><span class="line">  <span class="title function_">data</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="attr">a</span>: &#123;</span><br><span class="line">      <span class="attr">b</span>: <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">watch</span>: &#123;</span><br><span class="line">    <span class="attr">a</span>: &#123;</span><br><span class="line">      <span class="title function_">handler</span>(<span class="params">newVal</span>) &#123;</span><br><span class="line">        <span class="variable language_">console</span>.<span class="title function_">log</span>(newVal)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line">vm.<span class="property">a</span>.<span class="property">b</span> = <span class="number">2</span></span><br></pre></td></tr></table></figure><p>而我们只需要对代码做稍稍修改，就可以观测到这个变化了</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="attr">watch</span>: &#123;</span><br><span class="line">  <span class="attr">a</span>: &#123;</span><br><span class="line">    <span class="attr">deep</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="title function_">handler</span>(<span class="params">newVal</span>) &#123;</span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">log</span>(newVal)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样在 <code>watcher</code> 执行 <code>get</code> 求值的过程中有一段逻辑，traverse 实际上就是对一个对象做深层递归遍历，因为遍历过程中就是对一个子对象的访问，会触发它们的 getter 过程，这样就可以收集到依赖，也就是订阅它们变化的 watcher。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="title function_">get</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> value = <span class="variable language_">this</span>.<span class="property">getter</span>.<span class="title function_">call</span>(vm, vm)</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">deep</span>) &#123;</span><br><span class="line">    <span class="title function_">traverse</span>(value)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="三、render-watcher"><a href="#三、render-watcher" class="headerlink" title="三、render watcher"></a>三、render watcher</h2><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">mountComponent</span> (<span class="params"></span></span><br><span class="line"><span class="params">  vm: Component,</span></span><br><span class="line"><span class="params">  el: ?Element,</span></span><br><span class="line"><span class="params">  hydrating?: boolean</span></span><br><span class="line"><span class="params"></span>): <span class="title class_">Component</span> &#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  <span class="keyword">new</span> <span class="title class_">Watcher</span>(vm, updateComponent, noop, &#123;</span><br><span class="line">    <span class="title function_">before</span> () &#123;</span><br><span class="line">      <span class="keyword">if</span> (vm.<span class="property">_isMounted</span> &amp;&amp; !vm.<span class="property">_isDestroyed</span>) &#123;</span><br><span class="line">        <span class="title function_">callHook</span>(vm, <span class="string">&#x27;beforeUpdate&#x27;</span>)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;, <span class="literal">true</span> <span class="comment">/* isRenderWatcher */</span>)</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>详情见 <a href="https://yin-hongwei.github.io/2018/08/01/vue%E5%93%8D%E5%BA%94%E5%BC%8F%E5%8E%9F%E7%90%86/#%E4%BA%8C%E3%80%81%E5%8E%9F%E7%90%86">Vue 响应式原理</a></p><h2 id="四、问题"><a href="#四、问题" class="headerlink" title="四、问题"></a>四、问题</h2><h3 id="1、computed-和-watch-的区别"><a href="#1、computed-和-watch-的区别" class="headerlink" title="1、computed 和 watch 的区别"></a>1、computed 和 watch 的区别</h3><ul><li><strong>computed：</strong> 依赖的属性值发生变化才会重新计算 computed 的值；</li><li><strong>watch：</strong> 监听的数据发生变化就会执行回调进行后续操作；</li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> Vue </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>浏览器工作原理</title>
      <link href="/2021/06/01/%E6%B5%8F%E8%A7%88%E5%99%A8%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/"/>
      <url>/2021/06/01/%E6%B5%8F%E8%A7%88%E5%99%A8%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/</url>
      
        <content type="html"><![CDATA[<p>从地址栏输入 URL 到页面显示，浏览器依次完成 <strong>URL 校验 → DNS 解析 → 网络请求 → HTML&#x2F;CSS&#x2F;JS 解析 → 渲染树构建 → 布局 → 绘制 → 合成</strong>。理解这条 <strong>关键渲染路径（Critical Rendering Path）</strong>，是掌握浏览器内部机制的基础。</p><span id="more"></span><p>本文按时间顺序梳理各阶段的核心机制，重点放在 DOM&#x2F;CSSOM 构建与渲染管线。下图按 <strong>客户端 → 网络层 → 解析层 → 渲染层</strong> 四阶段展示完整流程：</p><img src="/2021/06/01/%E6%B5%8F%E8%A7%88%E5%99%A8%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/browser-pipeline.svg" class="" title="从 URL 到屏幕显示的完整流程（客户端 → 网络层 → 解析层 → 渲染层）"><h2 id="一、客户端：URL-输入与校验"><a href="#一、客户端：URL-输入与校验" class="headerlink" title="一、客户端：URL 输入与校验"></a>一、客户端：URL 输入与校验</h2><p>用户在地址栏输入 URL、点击链接或提交表单，都属于 <strong>导航（Navigation）</strong> 的触发动作。这一步发生在浏览器内部，<strong>尚未进入网络请求</strong>——浏览器首先要对 URL 做校验和解析，确认可以安全、合法地发起导航后，才会进入 DNS 和网络阶段。</p><h3 id="1、URL-解析"><a href="#1、URL-解析" class="headerlink" title="1、URL 解析"></a>1、URL 解析</h3><p>浏览器按 <a href="https://url.spec.whatwg.org/">URL 标准</a> 将输入字符串解析为结构化对象，主要包含：</p><table><thead><tr><th>部分</th><th>示例</th><th>说明</th></tr></thead><tbody><tr><td>scheme（协议）</td><td><code>https</code></td><td>决定使用何种加载机制</td></tr><tr><td>host（主机）</td><td><code>example.com</code></td><td>域名或 IP</td></tr><tr><td>port（端口）</td><td><code>443</code></td><td>可省略，使用协议默认端口</td></tr><tr><td>path（路径）</td><td><code>/docs/page</code></td><td>资源路径</td></tr><tr><td>query（查询）</td><td><code>?id=1</code></td><td>查询参数</td></tr><tr><td>fragment（片段）</td><td><code>#section</code></td><td>仅用于页面内定位，<strong>不会发送给服务器</strong></td></tr></tbody></table><p>用户可能只输入 <code>example.com</code>，浏览器会自动补全为 <code>https://example.com/</code>（具体策略因浏览器和 HSTS 配置而异）。</p><h3 id="2、校验与安全检查"><a href="#2、校验与安全检查" class="headerlink" title="2、校验与安全检查"></a>2、校验与安全检查</h3><p>解析完成后，浏览器还会做一系列检查，<strong>任一环节失败都可能中止导航</strong>：</p><ul><li><strong>协议合法性</strong>：<code>javascript:</code>、<code>data:</code> 等特殊 scheme 走不同处理路径，不会发起普通 HTTP 请求</li><li><strong>HSTS 升级</strong>：若域名在 HSTS 预加载列表中，强制将 <code>http://</code> 升级为 <code>https://</code></li><li><strong>混合内容拦截</strong>：HTTPS 页面中的不安全 HTTP 子资源会被阻止或警告</li><li><strong>Safe Browsing</strong>：对照恶意网站&#x2F;钓鱼名单（如 Google Safe Browsing）</li><li><strong>CSP &#x2F; 权限策略</strong>：部分导航可能受 Content-Security-Policy 约束</li></ul><p>只有校验通过后，浏览器才会创建网络请求，进入 DNS 解析阶段。</p><h2 id="二、域名解析（DNS）"><a href="#二、域名解析（DNS）" class="headerlink" title="二、域名解析（DNS）"></a>二、域名解析（DNS）</h2><p>域名解析就是查找域名对应 IP 地址的过程。浏览器不会每次都走完整 DNS 查询，而是按层级逐级查找缓存：</p><table><thead><tr><th>顺序</th><th>查找位置</th><th>说明</th></tr></thead><tbody><tr><td>1</td><td>浏览器 DNS 缓存</td><td>命中则直接返回，有时效限制</td></tr><tr><td>2</td><td>操作系统 DNS 缓存</td><td>浏览器未命中时由 OS 提供</td></tr><tr><td>3</td><td>本地 hosts 文件</td><td>手动配置的域名映射</td></tr><tr><td>4</td><td>本地 DNS 服务器（递归解析）</td><td>通常由运营商或路由器分配</td></tr></tbody></table><p>本地 DNS 服务器采用 <strong>迭代查询</strong>：依次向根域名服务器 → 顶级域名服务器 → 权威域名服务器询问，每一级只返回「下一级该问谁」，<strong>不会直接返回最终 IP</strong>（除非它自己已有缓存）。</p><img src="/2021/06/01/%E6%B5%8F%E8%A7%88%E5%99%A8%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/browser-dns.svg" class="" title="DNS 迭代查询过程"><p>解析成功后，结果会缓存到本地 DNS 服务器；TTL 过期后需重新查询。解析失败则页面无法访问。</p><h2 id="三、网络通讯"><a href="#三、网络通讯" class="headerlink" title="三、网络通讯"></a>三、网络通讯</h2><p>获取 IP 后，浏览器与服务器建立连接并收发 HTTP 报文。</p><h3 id="1、建立连接"><a href="#1、建立连接" class="headerlink" title="1、建立连接"></a>1、建立连接</h3><ol><li><strong>TCP 三次握手</strong>：客户端与服务器建立可靠传输通道</li><li><strong>TLS 握手</strong>（HTTPS）：协商加密算法、验证证书、交换密钥（HTTP 明文传输则跳过此步）</li></ol><p>现代浏览器在 HTTPS 场景下还会启用 <strong>TLS 1.3</strong>，通常 1-RTT 即可完成握手；支持 <strong>0-RTT</strong> 的站点可在复连时进一步减少延迟。</p><h3 id="2、发送与接收"><a href="#2、发送与接收" class="headerlink" title="2、发送与接收"></a>2、发送与接收</h3><p>浏览器构造 HTTP 请求报文（方法、路径、Header、Body），经 TCP 封装后依次经过传输层 → 网络层 → 数据链路层 → 物理层到达服务器。</p><p>服务器处理请求后返回 HTTP 响应报文（状态码、Header、HTML&#x2F;CSS&#x2F;JS 等资源）。部分服务器会先发 <strong>103 Early Hints</strong>，在 HTML 生成完毕前提示浏览器预加载 CSS&#x2F;JS，缩短等待时间。</p><h3 id="3、连接复用与协议演进"><a href="#3、连接复用与协议演进" class="headerlink" title="3、连接复用与协议演进"></a>3、连接复用与协议演进</h3><table><thead><tr><th>协议</th><th>特点</th></tr></thead><tbody><tr><td>HTTP&#x2F;1.1</td><td>默认 Keep-Alive，同一 TCP 连接复用多个请求</td></tr><tr><td>HTTP&#x2F;2</td><td>多路复用、Header 压缩、Server Push（已逐步弃用 Push）</td></tr><tr><td>HTTP&#x2F;3</td><td>基于 QUIC（UDP），消除队头阻塞，移动网络更稳定</td></tr></tbody></table><blockquote><p>只有在连接关闭或超时后，才会通过 TCP 四次挥手断开连接。</p></blockquote><h3 id="4、资源加载优先级"><a href="#4、资源加载优先级" class="headerlink" title="4、资源加载优先级"></a>4、资源加载优先级</h3><p>浏览器会根据资源类型和位置自动分配 <strong>fetch priority</strong>：</p><ul><li>HTML 文档：最高</li><li><code>&lt;head&gt;</code> 中的 CSS、同步 <code>&lt;script&gt;</code>：高</li><li><code>&lt;img fetchpriority=&quot;high&quot;&gt;</code>、LCP 图片：可手动提升</li><li><code>async</code>&#x2F;<code>defer</code> 脚本、预加载资源：中低</li><li><code>lazy</code> 图片、低优先级 prefetch：低</li></ul><h2 id="四、解析-HTML"><a href="#四、解析-HTML" class="headerlink" title="四、解析 HTML"></a>四、解析 HTML</h2><img src="/2021/06/01/%E6%B5%8F%E8%A7%88%E5%99%A8%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/browser-html-parser.svg" class="" title="HTML 词法分析与 DOM 树构建"><p>HTML 解析分为 <strong>词法分析</strong>（生成 token）和 <strong>语法分析</strong>（构建 DOM 树）两个阶段。HTML 解析器不会等待整个文档下载完毕——<strong>流式解析</strong>会在收到部分字节后就开始构建 DOM，这也是首屏能逐步渲染的基础。</p><h3 id="1、词法分析"><a href="#1、词法分析" class="headerlink" title="1、词法分析"></a>1、词法分析</h3><p>词法分析将字符流解析成 token。HTML 语法容错性强，无法使用常规编译器式解析器，浏览器采用基于 <strong>状态机</strong> 的自定义词法分析器。</p><blockquote><p><strong>词法分析器（Tokenizer）</strong>：负责将输入内容分解成一个个有效标记（token）。</p></blockquote><p>以 <code>&lt;p class=&quot;a&quot;&gt;text&lt;/p&gt;</code> 为例，生成的 token 序列大致为：</p><table><thead><tr><th>Token 类型</th><th>内容</th></tr></thead><tbody><tr><td>标签开始</td><td><code>&lt;p</code></td></tr><tr><td>属性</td><td><code>class=&quot;a&quot;</code></td></tr><tr><td>标签结束</td><td><code>&gt;</code></td></tr><tr><td>文本</td><td><code>text</code></td></tr><tr><td>结束标签</td><td><code>&lt;/p&gt;</code></td></tr></tbody></table><p>状态机从 <strong>数据状态（Data state）</strong> 出发：</p><ul><li>遇到普通字符 → 进入文本节点</li><li>遇到 <code>&lt;</code> → 进入 <strong>标签状态</strong><ul><li>下一个字符是 <code>!</code> → 可能是注释或 CDATA</li><li>下一个字符是字母 → 开始标签，持续接收直到 <code>&gt;</code></li><li>下一个字符是 <code>/</code> → 结束标签，持续接收直到 <code>&gt;</code>，然后回到数据状态</li></ul></li></ul><p>本质上，状态机把每种 token 的「特征字符链」拆成独立状态，再串联成一张状态转移图。</p><h3 id="2、语法分析（构建-DOM-树）"><a href="#2、语法分析（构建-DOM-树）" class="headerlink" title="2、语法分析（构建 DOM 树）"></a>2、语法分析（构建 DOM 树）</h3><blockquote><p><strong>树构建器（Tree Builder）</strong>：根据 HTML 语法规则分析 token 序列，构建 DOM 树。</p></blockquote><p>语法分析的过程就是构建 DOM 树的过程。解析器在消费 token 的同时创建 <code>Document</code> 对象，以它为根节点不断插入子元素。规范定义了每个 token 对应的 DOM 元素；解析器还会维护一个 <strong>开放元素栈</strong> 来跟踪嵌套关系，并纠正标签嵌套错误。</p><p>栈的操作规则（Tree Builder 消费的是 <strong>完整 token</strong>，不是词法分析阶段的中间态）：</p><ul><li>栈顶元素即当前正在构建的节点</li><li>遇到 <strong>StartTag token</strong>（含标签名与全部属性）→ 创建元素、写入属性、挂到父节点后 <strong>入栈</strong></li><li>遇到 <strong>Character token</strong>（文本）→ 插入为栈顶元素的子节点（<strong>不入栈</strong>；相邻文本会合并）</li><li>遇到 <strong>Comment token</strong> → 作为栈顶元素的子节点</li><li>遇到 <strong>EndTag token</strong> → <strong>出栈</strong>并校验是否与栈顶标签匹配</li></ul><h3 id="3、浏览器的容错机制"><a href="#3、浏览器的容错机制" class="headerlink" title="3、浏览器的容错机制"></a>3、浏览器的容错机制</h3><p>浏览 HTML 页面时几乎不会看到「语法无效」的错误提示——浏览器会按 <a href="https://html.spec.whatwg.org/multipage/parsing.html">HTML 解析算法</a> 自动修正无效内容并继续解析。例如未闭合的标签、错误的嵌套顺序等，都会被容错处理。</p><h3 id="4、遇到-script-标签"><a href="#4、遇到-script-标签" class="headerlink" title="4、遇到 script 标签"></a>4、遇到 script 标签</h3><p>HTML 解析器遇到 <code>&lt;script&gt;</code> 时会 <strong>暂停 DOM 构建</strong>，触发 JavaScript 的下载与执行（详见下一节）。默认情况下，这会阻塞后续 HTML 的解析。</p><h2 id="五、解析-JavaScript"><a href="#五、解析-JavaScript" class="headerlink" title="五、解析 JavaScript"></a>五、解析 JavaScript</h2><p>HTML 解析器遇到 <code>&lt;script&gt;</code> 时，会触发 JavaScript 的 <strong>下载 → 解析 → 编译 → 执行</strong>。其中解析阶段与 HTML、CSS 类似，同样分为 <strong>词法分析</strong> 和 <strong>语法分析</strong>；编译与执行在 <strong>主线程</strong> 完成。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">下载脚本 → 词法分析（Token）→ 语法分析（AST）→ 编译（字节码）→ 执行 → 恢复 HTML 解析</span><br></pre></td></tr></table></figure><h3 id="1、词法分析（Tokenize）"><a href="#1、词法分析（Tokenize）" class="headerlink" title="1、词法分析（Tokenize）"></a>1、词法分析（Tokenize）</h3><p>词法分析器（Lexer &#x2F; Scanner）将 JS 源码字符流切分为一个个 <strong>Token</strong>，不关心语法结构，只识别「词」的边界和类型。</p><p>以 <code>const sum = a + 1;</code> 为例：</p><table><thead><tr><th>Token 类型</th><th>值</th></tr></thead><tbody><tr><td>Keyword</td><td><code>const</code></td></tr><tr><td>Identifier</td><td><code>sum</code></td></tr><tr><td>Punctuator</td><td><code>=</code></td></tr><tr><td>Identifier</td><td><code>a</code></td></tr><tr><td>Punctuator</td><td><code>+</code></td></tr><tr><td>Numeric</td><td><code>1</code></td></tr><tr><td>Punctuator</td><td><code>;</code></td></tr></tbody></table><p>常见 Token 类型包括：<strong>关键字</strong>（<code>if</code>、<code>return</code>、<code>async</code>）、<strong>标识符</strong>、<strong>字面量</strong>（数字、字符串、正则）、<strong>运算符</strong>、<strong>分隔符</strong>。</p><p>词法分析还会处理：</p><ul><li><strong>自动分号插入（ASI）</strong> 的前置判断</li><li><strong>模板字符串</strong>、<strong>正则字面量</strong> 等上下文相关 token</li><li><strong>注释</strong> 直接丢弃，不参与后续分析</li></ul><h3 id="2、语法分析（Parse-→-AST）"><a href="#2、语法分析（Parse-→-AST）" class="headerlink" title="2、语法分析（Parse → AST）"></a>2、语法分析（Parse → AST）</h3><p>语法分析器（Parser）根据 <a href="https://tc39.es/ecma262/">ECMAScript 语法规范</a>，将 Token 流组装成 <strong>抽象语法树（AST，Abstract Syntax Tree）</strong>。每个 AST 节点对应一种语法结构，例如变量声明、函数调用、if 语句等。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Program</span><br><span class="line">└── VariableDeclaration (const sum)</span><br><span class="line">    └── VariableDeclarator</span><br><span class="line">        ├── Identifier: sum</span><br><span class="line">        └── BinaryExpression (+)</span><br><span class="line">            ├── Identifier: a</span><br><span class="line">            └── Literal: 1</span><br></pre></td></tr></table></figure><p>若源码不符合语法规则，Parser 在此阶段抛出 <strong><code>SyntaxError</code></strong>，脚本不会进入执行阶段——这与 HTML 的容错解析不同，JS 语法错误是硬失败。</p><p>解析完成后，引擎还会做 <strong>早期错误（Early Error）</strong> 检查，例如：</p><ul><li><code>let</code> 重复声明</li><li><code>return</code> 出现在非函数顶层</li><li>严格模式下的非法 <code>with</code> 语句</li></ul><h3 id="3、编译与执行"><a href="#3、编译与执行" class="headerlink" title="3、编译与执行"></a>3、编译与执行</h3><p>现代引擎（如 V8）不会直接把 AST 交给解释器逐行运行，典型流水线为：</p><ol><li><strong>Parser</strong> 生成 AST</li><li><strong>Ignition</strong> 将 AST 编译为 <strong>字节码（Bytecode）</strong> 并执行</li><li><strong>TurboFan</strong> 对热点代码做 JIT 优化，生成高效机器码</li></ol><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">源码 → AST → 字节码 →（热点）优化机器码</span><br></pre></td></tr></table></figure><p>执行期间可以读写 DOM、触发重排重绘、注册事件监听等——都在主线程上进行，因此长时间运行的 JS 会阻塞页面响应。</p><h3 id="4、script-标签与-HTML-解析的阻塞"><a href="#4、script-标签与-HTML-解析的阻塞" class="headerlink" title="4、script 标签与 HTML 解析的阻塞"></a>4、script 标签与 HTML 解析的阻塞</h3><p>默认情况下，普通 <code>&lt;script&gt;</code> 或 <code>&lt;script src=&quot;...&quot;&gt;</code> 会：</p><ol><li><strong>阻塞 HTML 解析</strong>——解析器暂停，等待脚本下载</li><li><strong>阻塞 DOM 构建</strong>——脚本执行期间不能继续插入节点</li><li><strong>可能阻塞渲染</strong>——若脚本修改 DOM 或读取布局属性</li></ol><p>CSS 也会阻塞脚本执行：浏览器需先完成样式表解析，才能执行依赖样式的 JavaScript（避免 JS 读到错误的样式计算结果）。</p><h3 id="5、async-与-defer"><a href="#5、async-与-defer" class="headerlink" title="5、async 与 defer"></a>5、async 与 defer</h3><p>通过 <code>async</code> 和 <code>defer</code> 属性，可以改变脚本的 <strong>下载时机</strong> 和 <strong>执行时机</strong>，从而减少对 HTML 解析的阻塞：</p><table><thead><tr><th>属性</th><th>下载</th><th>HTML 解析</th><th>执行时机</th><th>执行顺序</th></tr></thead><tbody><tr><td>默认（无属性）</td><td>同步，阻塞解析</td><td>暂停</td><td>下载完立即解析并执行</td><td>按文档顺序</td></tr><tr><td><code>defer</code></td><td>异步，不阻塞解析</td><td>继续</td><td>DOM 解析完成后、<code>DOMContentLoaded</code> 前</td><td>按文档顺序</td></tr><tr><td><code>async</code></td><td>异步，不阻塞解析</td><td>继续</td><td>下载完立即解析并执行</td><td><strong>不保证顺序</strong></td></tr><tr><td><code>type=&quot;module&quot;</code></td><td>异步（类似 defer）</td><td>继续</td><td>DOM 解析完成后</td><td>按文档顺序，支持 ES Module</td></tr></tbody></table><blockquote><p>注意：<code>async</code> 和 <code>defer</code> 只影响 <strong>何时下载、何时执行</strong>，不影响 JS 本身的词法&#x2F;语法分析过程——一旦开始执行，解析流程相同。</p></blockquote><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 默认：下载与执行期间阻塞 HTML 解析 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;/critical.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- defer：异步下载，DOM 解析完成后按顺序执行 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">defer</span> <span class="attr">src</span>=<span class="string">&quot;/app.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">defer</span> <span class="attr">src</span>=<span class="string">&quot;/app-chunk.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- async：异步下载，下载完立即执行，不保证顺序 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">async</span> <span class="attr">src</span>=<span class="string">&quot;/analytics.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure><p><strong>执行时序对比</strong>（简化）：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">默认 script:  [HTML]──停──[下载 → 解析 → 执行 JS]──[继续 HTML]──[DOMContentLoaded]</span><br><span class="line">defer:        [HTML 持续解析 + JS 异步下载]──[解析 → 执行 JS]──[DOMContentLoaded]</span><br><span class="line">async:        [HTML 持续解析]──[JS 下载完即解析执行，时机不确定]</span><br></pre></td></tr></table></figure><h3 id="6、其他加载方式"><a href="#6、其他加载方式" class="headerlink" title="6、其他加载方式"></a>6、其他加载方式</h3><ul><li><strong><code>type=&quot;module&quot;</code></strong>：默认 defer 语义，自动严格模式，支持 <code>import</code>&#x2F;<code>export</code></li><li><strong>动态 <code>import()</code></strong>：运行时按需加载，不阻塞 HTML 解析</li><li><strong><code>document.createElement(&#39;script&#39;)</code></strong>：动态插入的脚本默认等效于 <code>async</code></li></ul><h2 id="六、解析-CSS"><a href="#六、解析-CSS" class="headerlink" title="六、解析 CSS"></a>六、解析 CSS</h2><p>构建 DOM 树的过程中遇到 CSS 资源会同步解析。CSS 同样经过词法分析和语法分析，最终生成 <strong>CSSOM 树</strong>（CSS Object Model）。</p><img src="/2021/06/01/%E6%B5%8F%E8%A7%88%E5%99%A8%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/browser-critical-path.svg" class="" title="DOM 树与 CSSOM 树合并为渲染树"><p>样式来源主要有三种：</p><ul><li><code>&lt;link&gt;</code> 引用的外部 CSS 文件</li><li><code>&lt;style&gt;</code> 标签内的 CSS</li><li>元素 <code>style</code> 属性中的内联 CSS</li></ul><h3 id="1、词法分析-1"><a href="#1、词法分析-1" class="headerlink" title="1、词法分析"></a>1、词法分析</h3><p>浏览器使用 <a href="https://en.wikipedia.org/wiki/Flex_lexical_analyser">Flex</a> 等工具将 CSS 字符流解析为 token。</p><h3 id="2、语法分析（构建-CSSOM-树）"><a href="#2、语法分析（构建-CSSOM-树）" class="headerlink" title="2、语法分析（构建 CSSOM 树）"></a>2、语法分析（构建 CSSOM 树）</h3><p>解析器将 token 组装成 <strong>StyleSheet 对象</strong>，每个对象包含多条 CSS 规则；每条规则包含一个选择器和一组属性声明。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">StyleSheet</span><br><span class="line">├── CSSRule &#123; selector: &quot;body&quot;,     properties: &#123; margin: 0 &#125; &#125;</span><br><span class="line">├── CSSRule &#123; selector: &quot;.title&quot;,   properties: &#123; font-size: 24px &#125; &#125;</span><br><span class="line">└── CSSRule &#123; selector: &quot;#header&quot;,  properties: &#123; display: flex &#125; &#125;</span><br></pre></td></tr></table></figure><p>可在 DevTools Console 中通过 <code>document.styleSheets</code> 查看解析结果。</p><h3 id="3、生成选择器索引（Rule-Map）"><a href="#3、生成选择器索引（Rule-Map）" class="headerlink" title="3、生成选择器索引（Rule Map）"></a>3、生成选择器索引（Rule Map）</h3><p>为加速样式匹配，浏览器将 CSS 规则按 <strong>最右侧选择器</strong> 的类型（id、class、标签、伪类）分别存入哈希表：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">CompactRuleMap m_idRules;</span><br><span class="line">CompactRuleMap m_classRules;</span><br><span class="line">CompactRuleMap m_tagRules;</span><br><span class="line">CompactRuleMap m_shadowPseudoElementRules;</span><br></pre></td></tr></table></figure><p>匹配时先按 id → class → 伪元素 → 标签的顺序快速取出候选规则，再逐条检查复合选择器左侧是否也匹配当前元素。</p><h3 id="4、CSS-阻塞渲染"><a href="#4、CSS-阻塞渲染" class="headerlink" title="4、CSS 阻塞渲染"></a>4、CSS 阻塞渲染</h3><p>CSS 是 <strong>渲染阻塞资源</strong>：浏览器必须等 CSSOM 构建完成，才能生成渲染树并进行首次绘制。<code>@import</code> 会串行触发额外请求，进一步延迟 CSSOM 的构建。</p><h2 id="七、构建渲染树"><a href="#七、构建渲染树" class="headerlink" title="七、构建渲染树"></a>七、构建渲染树</h2><h3 id="1、渲染树的概念"><a href="#1、渲染树的概念" class="headerlink" title="1、渲染树的概念"></a>1、渲染树的概念</h3><p>DOM 树描述文档结构，CSSOM 树描述样式规则；<strong>渲染树（Render Tree）</strong> 则是二者的交集——只包含需要显示的节点及其计算后的样式。</p><p>Firefox 称渲染树中的节点为 <strong>Frame</strong>，WebKit&#x2F;Blink 称为 <strong>RenderObject</strong>。每个 RenderObject 对应一个矩形区域，其类型由 <code>display</code> 等 CSS 属性决定：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">RenderObject* <span class="title function_">RenderObject::createObject</span><span class="params">(Node* node, RenderStyle* style)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">switch</span> (style-&gt;display()) &#123;</span><br><span class="line">        <span class="keyword">case</span> NONE:            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> INLINE:          o = new RenderInline(node);  <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> BLOCK:           o = new RenderBlock(node);   <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> INLINE_BLOCK:    o = new RenderBlock(node);   <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> LIST_ITEM:       o = new RenderListItem(node); <span class="keyword">break</span>;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> o;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2、构建过程"><a href="#2、构建过程" class="headerlink" title="2、构建过程"></a>2、构建过程</h3><p>从 DOM 根节点 <code>document</code> 开始 <strong>深度优先遍历</strong>，对每个可见节点：</p><ol><li>在 Rule Map 中匹配适用的 CSS 规则</li><li>计算最终样式（含继承、层叠、默认值）</li><li>创建对应的 RenderObject 加入渲染树</li></ol><h4 id="2-1-计算样式"><a href="#2-1-计算样式" class="headerlink" title="2.1 计算样式"></a>2.1 计算样式</h4><p><strong>1）选择器匹配</strong></p><p>对每个节点，按 id → class → 伪元素 → 标签 → 通配符的顺序取出候选规则。若复合选择器最右侧匹配，再递归检查左侧选择器是否也匹配。</p><p><strong>2）设置 style</strong></p><p>找到匹配规则后，计算各 CSS 属性的最终值：</p><ul><li><strong>继承属性</strong>：沿 DOM 树向上查找祖先元素的值，找不到则用初始值</li><li><strong>层叠合并</strong>：多个规则命中同一节点时，按优先级（!important &gt; 内联 &gt; ID &gt; class &gt; 标签 &gt; 通配符）合并</li><li><strong>样式结构共享</strong>：浏览器将常用样式组合缓存为共享对象，减少内存占用</li></ul><p><strong>3）调整 style</strong></p><p>部分属性会触发其他属性的自动调整，例如 <code>position: absolute/fixed</code> 或 <code>float</code> 的元素会被计算为 <code>display: block</code>。</p><h4 id="2-2-渲染对象与-DOM-元素的关系"><a href="#2-2-渲染对象与-DOM-元素的关系" class="headerlink" title="2.2 渲染对象与 DOM 元素的关系"></a>2.2 渲染对象与 DOM 元素的关系</h4><ul><li><strong>多对一</strong>：多个 DOM 节点可能不产生 RenderObject（如 <code>&lt;head&gt;</code>、<code>display: none</code> 的元素）；<code>visibility: hidden</code> 的元素仍在渲染树中，只是不可见</li><li><strong>一对多</strong>：复杂元素可能对应多个 RenderObject，如 <code>&lt;select&gt;</code> 有显示区、下拉列表、按钮三个渲染对象；文本换行时每一行也会新增 RenderObject</li><li><strong>位置不同</strong>：浮动和绝对定位元素的 RenderObject 会脱离正常文档流，在原位置留下占位框架</li></ul><h2 id="八、布局（Layout-Reflow）"><a href="#八、布局（Layout-Reflow）" class="headerlink" title="八、布局（Layout &#x2F; Reflow）"></a>八、布局（Layout &#x2F; Reflow）</h2><p>渲染树构建完成后，浏览器需要为每个 RenderObject 计算 <strong>几何信息</strong>（位置、尺寸），这个过程称为 <strong>布局</strong> 或 <strong>重排（Reflow）</strong>。</p><p>布局从根 RenderObject（对应 <code>&lt;html&gt;</code> 元素）开始，递归遍历渲染树，为每个节点计算坐标和大小。</p><p>HTML 默认采用 <strong>流式布局（Flow Layout）</strong>：大多数情况下只需一次从上到下、从左到右的遍历即可确定几何信息——靠后的元素通常不影响靠前元素的位置。例外情况包括表格布局、Flex&#x2F;Grid 中的 <code>auto</code> 尺寸等，可能需要多轮计算。</p><p>坐标系以根 RenderObject 为原点，左上角为 <code>(0, 0)</code>，尺寸等于 <strong>视口（Viewport）</strong> 大小。</p><h3 id="触发重排的操作"><a href="#触发重排的操作" class="headerlink" title="触发重排的操作"></a>触发重排的操作</h3><table><thead><tr><th>类型</th><th>示例</th></tr></thead><tbody><tr><td>DOM 变更</td><td>增删节点、修改文本</td></tr><tr><td>几何样式</td><td><code>width</code>、<code>height</code>、<code>margin</code>、<code>padding</code>、<code>border</code></td></tr><tr><td>布局上下文</td><td><code>display</code>、<code>position</code>、<code>float</code></td></tr><tr><td>读取布局属性</td><td><code>offsetWidth</code>、<code>getBoundingClientRect()</code> 强制同步布局</td></tr></tbody></table><p>频繁交替读写布局属性会导致 <strong>布局抖动（Layout Thrashing）</strong>——浏览器被迫在读写之间反复同步布局计算。</p><h2 id="九、绘制与合成"><a href="#九、绘制与合成" class="headerlink" title="九、绘制与合成"></a>九、绘制与合成</h2><img src="/2021/06/01/%E6%B5%8F%E8%A7%88%E5%99%A8%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/browser-compositing.svg" class="" title="主线程与合成线程的分工"><h3 id="1、绘制（Paint）"><a href="#1、绘制（Paint）" class="headerlink" title="1、绘制（Paint）"></a>1、绘制（Paint）</h3><p>布局完成后，浏览器遍历渲染树，将各节点绘制为位图或绘制指令列表。绘制按 <strong>绘制顺序（Paint Order）</strong> 进行，受 <code>z-index</code>、<code>stacking context</code> 等影响。</p><p>绘制通常分多个阶段：背景 → 边框 → 内容 → 阴影等，最终生成 <strong>绘制记录（Paint Record）</strong>。</p><h3 id="2、合成（Composite）"><a href="#2、合成（Composite）" class="headerlink" title="2、合成（Composite）"></a>2、合成（Composite）</h3><p>现代浏览器会将页面拆分为多个 <strong>合成层（Compositing Layer）</strong>，由 <strong>合成线程（Compositor Thread）</strong> 在 GPU 上完成最终合成。</p><p>以下情况可能触发独立合成层：</p><ul><li><code>transform</code>、<code>opacity</code> 动画</li><li><code>will-change: transform</code> 提示</li><li><code>&lt;video&gt;</code>、<code>&lt;canvas&gt;</code>、<code>&lt;iframe&gt;</code> 等元素</li><li>3D transform（<code>translateZ(0)</code>）</li></ul><p>只改变合成层属性时，可跳过 Layout 和 Paint，直接 Composite。</p><h2 id="十、完整流程回顾"><a href="#十、完整流程回顾" class="headerlink" title="十、完整流程回顾"></a>十、完整流程回顾</h2><ol><li><strong>客户端</strong>：用户输入 URL，浏览器解析并校验（协议、HSTS、安全检查）</li><li><strong>DNS 解析</strong>：浏览器缓存 → OS 缓存 → hosts → 本地 DNS 迭代查询，获取服务器 IP</li><li><strong>TCP 三次握手</strong>建立连接；HTTPS 还需 TLS 握手</li><li>浏览器发送 <strong>HTTP 请求</strong>；可选 103 Early Hints 提前预加载</li><li>服务器返回 <strong>HTTP 响应</strong>（HTML 及关联资源）</li><li>浏览器 <strong>流式解析 HTML</strong>，构建 DOM 树</li><li>并行 <strong>解析 CSS</strong> 构建 CSSOM 树；遇到 <strong>JavaScript</strong> 下载并执行（<code>async</code>&#x2F;<code>defer</code> 可改变阻塞行为）</li><li>合并 DOM 与 CSSOM，构建 <strong>渲染树</strong>（排除非视觉节点和 <code>display: none</code> 元素）</li><li><strong>布局</strong>：计算每个节点的几何信息</li><li><strong>绘制</strong>：生成绘制指令</li><li><strong>合成</strong>：GPU 将各层合成为最终画面，显示到屏幕</li></ol><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><ul><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/Performance/Critical_rendering_path">Critical rendering path（MDN）</a></li><li><a href="https://www.html5rocks.com/zh/tutorials/internals/howbrowserswork/">How Browsers Work（HTML5 Rocks）</a></li><li><a href="https://developer.mozilla.org/zh-CN/docs/Web/Performance/How_browsers_work">浏览器的工作原理（MDN）</a></li><li><a href="https://web.dev/articles/efficiently-load-third-party-javascript">Script tags: async and defer（web.dev）</a></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> 浏览器, 前端 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>前端数据存储方案</title>
      <link href="/2020/07/22/%E5%89%8D%E7%AB%AF%E6%95%B0%E6%8D%AE%E5%AD%98%E5%82%A8%E6%96%B9%E6%A1%88/"/>
      <url>/2020/07/22/%E5%89%8D%E7%AB%AF%E6%95%B0%E6%8D%AE%E5%AD%98%E5%82%A8%E6%96%B9%E6%A1%88/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>前端数据存储方案主要有 Cookie、Web Storage、IndexedDB 三种。下图为 Chrome 浏览器的存储方案，虽然支持 Web SQL 存储，但是 W3C Web应用工作组于2010年11月停止了对该规范的研究，所以不提倡使用，下面就以上三种存储方案做一下介绍。</p><span id="more"></span><p><img src="/2020/07/22/%E5%89%8D%E7%AB%AF%E6%95%B0%E6%8D%AE%E5%AD%98%E5%82%A8%E6%96%B9%E6%A1%88/007S8ZIlly1gh06exiu2nj31nw0u0n2h.jpg"></p><br/><h2 id="一、Cookie"><a href="#一、Cookie" class="headerlink" title="一、Cookie"></a>一、Cookie</h2><h3 id="1、概念"><a href="#1、概念" class="headerlink" title="1、概念"></a>1、概念</h3><p>Cookie 指某些网站为了辨别用户身份而储存在用户本地终端上的数据。每次请求都会把本地的 Cookie 发到服务器，让服务器知道当前用户的状态。</p><p>Cookie 有域的概念，不能跨域操作。</p><h3 id="2、适用场景"><a href="#2、适用场景" class="headerlink" title="2、适用场景"></a>2、适用场景</h3><p>适合客户端存储较少的数据，用于标识状态（用户登录状态、购物车等）。</p><h3 id="3、特点"><a href="#3、特点" class="headerlink" title="3、特点"></a>3、特点</h3><p><strong>1）数据存放位置</strong>：客户端浏览器缓存中。</p><p><strong>2）数据存储大小</strong>：Cookie 存储的数据不能超过 4 KB，浏览器支持的 Cookie 是30～50个，IE6 20个。</p><p><strong>3）数据有效期</strong>：cookie 支持设置数据过期时间。</p><p><strong>4）数据作用域</strong>：cookie 在所有同源窗口中共享。</p><h3 id="4、缺点"><a href="#4、缺点" class="headerlink" title="4、缺点"></a>4、缺点</h3><p>Cookie 容量小（4K）；不安全，容易被拦截暴露数据。</p><h3 id="5、API"><a href="#5、API" class="headerlink" title="5、API"></a>5、API</h3><p><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Document/cookie">https://developer.mozilla.org/zh-CN/docs/Web/API/Document/cookie</a></p><h3 id="6、HTTP-请求头字段相关"><a href="#6、HTTP-请求头字段相关" class="headerlink" title="6、HTTP 请求头字段相关"></a>6、HTTP 请求头字段相关</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1）Expires：cookie最长有效期</span><br><span class="line">2）Max-Age：在 cookie 失效之前需要经过的秒数。（当Expires和Max-Age同时存在时，以 Max-Age 为准）</span><br><span class="line">3）Domain：指定 cookie 可以送达的主机名。</span><br><span class="line">4）Path：指定一个 URL 路径，这个路径必须出现在要请求的资源的路径中才可以发送 Cookie 首部</span><br><span class="line">5）Secure：一个带有安全属性的 cookie 只有在请求使用SSL和HTTPS协议的时候才会被发送到服务器。</span><br><span class="line">6）HttpOnly: 设置了 HttpOnly 属性的 cookie 不能使用 JavaScript 经由 Document.cookie 属性、XMLHttpRequest 和 Request APIs 进行访问，以防范跨站脚本攻击（XSS）。</span><br></pre></td></tr></table></figure><br/><h2 id="二、Web-Storage"><a href="#二、Web-Storage" class="headerlink" title="二、Web Storage"></a>二、Web Storage</h2><h3 id="1、概念-1"><a href="#1、概念-1" class="headerlink" title="1、概念"></a>1、概念</h3><p>Web Storage 有 sessionStorage 和 localStorage 两种存储机制。</p><p>sessionStorage 为每一个给定的源维持一个独立的存储区域，该存储区域在页面会话期间可用；localStorage 在浏览器关闭，然后重新打开后数据仍然存在，可用作持久存储数据。</p><h3 id="2、适用场景-1"><a href="#2、适用场景-1" class="headerlink" title="2、适用场景"></a>2、适用场景</h3><p>适合客户端存储较少的数据，且数字存储时会转成字符串。</p><h3 id="3、特点-1"><a href="#3、特点-1" class="headerlink" title="3、特点"></a>3、特点</h3><p><strong>1）数据存放位置</strong>：sessionStorage 和 localStorage 的数据仅在本地保存，不会自动发送给服务器。</p><p><strong>2）数据存储大小</strong>：5M。</p><p><strong>3）数据有效期</strong>：sessionStorage 仅在当前浏览器窗口关闭之前有效；localStorage 始终有效，窗口或浏览器关闭也一直保存。</p><p><strong>4）数据作用域</strong>：sessionStorage 即使同一页面，不同的浏览器窗口中也不共享；localstorage 在所有同源窗口中都共享。</p><p><strong>5）同步</strong>：操作数据是同步的。</p><h3 id="4、缺点-1"><a href="#4、缺点-1" class="headerlink" title="4、缺点"></a>4、缺点</h3><p>不能用于存储重要信息，本地储存数据都容易被篡改，容易受到 XSS 攻击。</p><h3 id="5、API-1"><a href="#5、API-1" class="headerlink" title="5、API"></a>5、API</h3><p><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/Web_Storage_API/">https://developer.mozilla.org/zh-CN/docs/Web/API/Web_Storage_API/</a></p><br/><h2 id="三、IndexedDB"><a href="#三、IndexedDB" class="headerlink" title="三、IndexedDB"></a>三、IndexedDB</h2><h3 id="1、概念-2"><a href="#1、概念-2" class="headerlink" title="1、概念"></a>1、概念</h3><p>IndexedDB 是一种在用户浏览器中持久存储数据的方法，用于客户端存储大量结构化数据(包括文件&#x2F; 二进制大型对象(blobs)。</p><h3 id="2、适用场景-2"><a href="#2、适用场景-2" class="headerlink" title="2、适用场景"></a>2、适用场景</h3><p>适合客户端存储大量结构化数据。</p><h3 id="3、特点-2"><a href="#3、特点-2" class="headerlink" title="3、特点"></a>3、特点</h3><p><strong>1）键值对储存：</strong> IndexedDB 内部采用对象仓库（object store）存放数据。所有类型的数据都可以直接存入，包括字符串、JavaScript 对象、二进制数据（ArrayBuffer 对象和 Blob 对象）。对象仓库中，数据以”键值对”的形式保存，每一个数据记录都有对应的主键，主键是独一无二的，不能有重复，否则会抛出一个错误。</p><p><strong>2）数据存储大小</strong>： 取决于硬盘大小，<strong>全局限制</strong>为可用磁盘空间的50％。如果超过此范围，则会发起称为<strong>源回收</strong>的过程，删除整个源的数据，直到存储量再次低于限制。删除源数据没有只删一部分的说法——因为这样可能会导致不一致的问题。<strong>组限制</strong>——这被定义为全局限制的20％，但它至少有10 MB，最大为2GB。</p><p><strong>3）异步：</strong> IndexedDB 操作时不会锁死浏览器，用户依然可以进行其他操作，这与 LocalStorage 形成对比，后者的操作是同步的。异步设计是为了防止大量数据的读写，拖慢网页的表现。</p><p><strong>4）支持事务：</strong> IndexedDB 支持事务（transaction），这意味着一系列操作步骤之中，只要有一步失败，整个事务就都取消，数据库回滚到事务发生之前的状态，不存在只改写一部分数据的情况。</p><p><strong>5）同源限制：</strong> IndexedDB 受到同源限制，每一个数据库对应创建它的域名。网页只能访问自身域名下的数据库，而不能访问跨域的数据库。</p><h3 id="4、缺点-2"><a href="#4、缺点-2" class="headerlink" title="4、缺点"></a>4、缺点</h3><p>不能设置数据存储的范围，数据太多时就比较占内存。</p><h3 id="5、API-2"><a href="#5、API-2" class="headerlink" title="5、API"></a>5、API</h3><p><a href="https://developer.mozilla.org/zh-CN/docs/Web/API/IndexedDB_API/">https://developer.mozilla.org/zh-CN/docs/Web/API/IndexedDB_API/</a></p><h3 id="6、第三方类库"><a href="#6、第三方类库" class="headerlink" title="6、第三方类库"></a>6、第三方类库</h3><ul><li><a href="https://localforage.github.io/localForage/">localForage</a>：一个简单名称的Polyfill：客户端数据存储的值语法，它在后台使用IndexedDB，但在不支持IndexedDB的浏览器中回退到WebSQL或localStorage。</li><li><a href="http://www.dexie.org/">Dexie.js</a>：IndexedDB的包装器，通过简单的语法，可以更快地进行代码开发。</li><li><a href="https://pouchdb.com/">PouchDB</a>：使用IndexedDB在浏览器中实现CouchDB的客户端。</li><li><a href="https://github.com/jakearchibald/idb">idb</a>：一个微小的（〜1.15k）库，主要反映了IndexedDB的API，但小的改进，使一个很大的区别的可用性。</li></ul><br/><h2 id="参考资源"><a href="#参考资源" class="headerlink" title="参考资源"></a>参考资源</h2><p><a href="http://www.ruanyifeng.com/blog/2018/07/indexeddb.html">http://www.ruanyifeng.com/blog/2018/07/indexeddb.html</a></p><br/>]]></content>
      
      
      
        <tags>
            
            <tag> Web 前端 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>前端模块化</title>
      <link href="/2020/03/18/%E5%89%8D%E7%AB%AF%E6%A8%A1%E5%9D%97%E5%8C%96/"/>
      <url>/2020/03/18/%E5%89%8D%E7%AB%AF%E6%A8%A1%E5%9D%97%E5%8C%96/</url>
      
        <content type="html"><![CDATA[<h2 id="一、模块化进化史"><a href="#一、模块化进化史" class="headerlink" title="一、模块化进化史"></a>一、模块化进化史</h2><p>对于 js 的编写，不能像其他语言一样很好的支持模块化，很多程序会比较臃肿的丢在一个文件里，要把这个程序拆分开使用，两个文件之前变量的传递就是个问题。</p><span id="more"></span><p>模块化的的第一个阶段，是使用的全局模式。假如一个文件要使用另一个文件中的变量，就需要将这个变量定为全局变量，挂到 window  对象上面，这样虽然解决了变量共享的问题，但是这样不仅污染全局对象，而且很容易引起命名冲突覆盖之前的变量。于是，前端模块化的又摸索开始了。</p><p>到了第二个阶段，人们开始用对象分装这些数据，这样做避免了命名冲突，但是这些数据可以通过对象直接访问到，即没有做到数据私有化。使得数据不安全。</p><p>到了第三个阶段，立即执行函数的引入，很好的解决了数据是私有的问题，但是当前这个模块依赖另一个模块时就不好处理了。</p><p>到了第四个阶段，开始出现引入依赖的思想，诸如 commonjs、AMD、ES6 等等规范，他们都是将一个文件作为一个模块来看待，但是浏览器并不识别他们，所以中间就需要一个东西来翻译一下，让浏览器看的懂，即将他们转成浏览器能读懂的 js ，这个时候 webpack 就出现了，它就充当了这个翻译的角色。</p><p>下面看一个 demo 感受一下。这个 demo 要实现的主体效果是我一个文件里定义了变量，在另一个文件里引用它输出。</p><h2 id="二、传统开发"><a href="#二、传统开发" class="headerlink" title="二、传统开发"></a>二、传统开发</h2><p>先看没有使用 webpack 的场景。首先在你的工程下创建一个 index.html 文件，在 js 目录下再分别创建 a.js 和 b.js 文件。</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;en&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;UTF-8&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;viewport&quot;</span> <span class="attr">content</span>=<span class="string">&quot;width=device-width, initial-scale=1.0&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Document<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;./js/a.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;./js/b.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>1）定义变量</p><p>a.js 文件中定义一个变量，要想让令一个文件中能访问到，就要把它挂到全局。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">window</span>.<span class="property">msg</span> = <span class="string">&#x27;yin&#x27;</span>;</span><br><span class="line">&#125;)()</span><br></pre></td></tr></table></figure><p>2）引用变量</p><p>b.js 文件中输出这个变量。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;msg: &#x27;</span>, msg);</span><br></pre></td></tr></table></figure><p>3）查看结果</p><p>打开浏览器控制台可以看到如下效果：</p><p><img src="/2020/03/18/%E5%89%8D%E7%AB%AF%E6%A8%A1%E5%9D%97%E5%8C%96/00831rSTly1gcyk86igfdj31bu05cwev.jpg"></p><h2 id="三、模块化开发"><a href="#三、模块化开发" class="headerlink" title="三、模块化开发"></a>三、模块化开发</h2><p>1）导出变量</p><p>使用 exports 导出变量</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> msg = <span class="string">&#x27;yin&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123;<span class="attr">msg</span>: msg&#125;;</span><br></pre></td></tr></table></figure><p>2）引入变量</p><p>在另一个文件中，以 require 的方式导入数据</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> msg = <span class="built_in">require</span>(<span class="string">&#x27;./a.js&#x27;</span>).<span class="property">msg</span>;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;msg: &#x27;</span>, msg);</span><br></pre></td></tr></table></figure><p>3）运行</p><p>在控制台运行 b.js 文件</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node b.js</span><br></pre></td></tr></table></figure><p>可以看到控制台输出内容，但是到了浏览器中刷新的时候，会发现报错</p><p><img src="/2020/03/18/%E5%89%8D%E7%AB%AF%E6%A8%A1%E5%9D%97%E5%8C%96/00831rSTly1gcykdh2e5oj31by068q42.jpg"></p><p>这是意料之中的事情，因为浏览器本身不认识 node，要让它认识就需要拿 webpack 来编译一下。所以用 npm 先全局装一下 webpack</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install webpack -g</span><br><span class="line"></span><br><span class="line">npm install webpack-cli -g</span><br></pre></td></tr></table></figure><p>这里将编译后的内容放到 dist 目录下，命名为 bundle.js，所以 html 文件中重新改一下引用方式。</p><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;en&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;UTF-8&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;viewport&quot;</span> <span class="attr">content</span>=<span class="string">&quot;width=device-width, initial-scale=1.0&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Document<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;./dist/bundle.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>最后编译一下，刷新浏览器，内容就又正常显示了。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">webpack ./js/b.js -o ./dist/bundle.js</span><br></pre></td></tr></table></figure><h2 id="四、优化"><a href="#四、优化" class="headerlink" title="四、优化"></a>四、优化</h2><p>1）配置 package.json 文件</p><p>对于编程来说，每次编译跑这么长代码很不优雅，webpack 给我们提供了很多配置，其中可以对指令进行配置，首先运行 <strong>npm init</strong>  安装 package.json 文件，然后局部安装 webpack，并在 scripts 中配置 <strong>“build”: “node_modules&#x2F;.bin&#x2F;webpack”</strong> 这一行，如下：</p><figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;webpack-demo&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;version&quot;</span><span class="punctuation">:</span> <span class="string">&quot;1.0.0&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;description&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;main&quot;</span><span class="punctuation">:</span> <span class="string">&quot;index.js&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;scripts&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;test&quot;</span><span class="punctuation">:</span> <span class="string">&quot;echo \&quot;Error: no test specified\&quot; &amp;&amp; exit 1&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="string">&quot;node_modules/.bin/webpack&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;keywords&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;author&quot;</span><span class="punctuation">:</span> <span class="string">&quot;&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;license&quot;</span><span class="punctuation">:</span> <span class="string">&quot;ISC&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;devDependencies&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;webpack&quot;</span><span class="punctuation">:</span> <span class="string">&quot;^4.42.0&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;webpack-cli&quot;</span><span class="punctuation">:</span> <span class="string">&quot;^3.3.11&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>2）配置 webpack.config.js 文件</p><p>这里写程序编译的出口和入口。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">&#x27;path&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span> = &#123;</span><br><span class="line">    <span class="attr">entry</span>: <span class="string">&#x27;./js/b.js&#x27;</span>,</span><br><span class="line">    <span class="attr">output</span>: &#123;</span><br><span class="line">        <span class="attr">filename</span>: <span class="string">&#x27;bundle.js&#x27;</span>,</span><br><span class="line">        <span class="attr">path</span>: path.<span class="title function_">resolve</span>(__dirname, <span class="string">&#x27;dist&#x27;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>3）运行</p><p>每次编译只需要运行<strong>npm run build</strong> 即可，刷新浏览器，控制台内容也正常输出。最终项目结构如下：</p><p><img src="/2020/03/18/%E5%89%8D%E7%AB%AF%E6%A8%A1%E5%9D%97%E5%8C%96/00831rSTly1gcyn60zyucj314a0iy0w9.jpg"></p>]]></content>
      
      
      
        <tags>
            
            <tag> webpack </tag>
            
            <tag> 模块化 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Git 工作原理</title>
      <link href="/2019/11/05/Git%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/"/>
      <url>/2019/11/05/Git%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/</url>
      
        <content type="html"><![CDATA[<h2 id="一、Git-是什么"><a href="#一、Git-是什么" class="headerlink" title="一、Git 是什么"></a>一、Git 是什么</h2><p>Git 是目前世界上最先进的分布式版本控制工具之一，下面我将通过数据的流向来介绍它。</p><p><img src="/2019/11/05/Git%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/00831rSTly1gcixg7jnb0j30wk09gdg2.jpg"></p><span id="more"></span><h2 id="二、从工作区到远程仓库"><a href="#二、从工作区到远程仓库" class="headerlink" title="二、从工作区到远程仓库"></a>二、从工作区到远程仓库</h2><h3 id="1、Workspace（工作区）"><a href="#1、Workspace（工作区）" class="headerlink" title="1、Workspace（工作区）"></a>1、Workspace（工作区）</h3><p>如果要对项目进行版本管理，在开始一个工程前，先执行 <code>git init</code> 命令来对工程进行初始化。 </p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git init</span><br></pre></td></tr></table></figure><p><code>git init</code> 命令会在该项目下生成一个 <code>.git</code> 的目录（如下），用来保存版本信息。<code>.git</code> 目录的上一级就是工作区。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">branches/</span><br><span class="line">config</span><br><span class="line">description</span><br><span class="line">HEAD</span><br><span class="line">hooks/</span><br><span class="line">info/</span><br><span class="line">objects/</span><br><span class="line">refs/</span><br></pre></td></tr></table></figure><h3 id="2、Index-Stage（暂存区）"><a href="#2、Index-Stage（暂存区）" class="headerlink" title="2、Index &#x2F; Stage（暂存区）"></a>2、Index &#x2F; Stage（暂存区）</h3><p>在 <code>.git</code> 目录下，有一个 index 文件，这个地方叫做暂存区。它实际上就是一个包含文件索引的目录树，记录了文件名、文件的状态信息。</p><p><img src="/2019/11/05/Git%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86/00831rSTly1gcj8zz5bixj30iz09x0tj.jpg"></p><p>当编写了一些代码后，我们可以执行 <code>git add</code> 命令，将当前项目所有变动的文件写入到对象库中的一个新的对象中，位于 “.git&#x2F;objects” 目录下。并记录在暂存区。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git add .</span><br></pre></td></tr></table></figure><p>如果要查看暂存区的状态。可以用如下命令，它会显示当前改动过的文件的状态。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git status</span><br><span class="line">// modified：已修改</span><br><span class="line">// staged：已暂存。可以看到你要提交的东西有哪些</span><br><span class="line">// committed：已提交</span><br></pre></td></tr></table></figure><h3 id="3、Repository（本地仓库或版本库）"><a href="#3、Repository（本地仓库或版本库）" class="headerlink" title="3、Repository（本地仓库或版本库）"></a>3、Repository（本地仓库或版本库）</h3><p>将改动后的文件存于暂存区后，就可以将现在的信息提交到版本库，即暂存区的目录树写到对象库中，其中HEAD指向最新放入仓库的版本。提交命令如下，</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git commit -m &quot;[XX]&quot;</span><br></pre></td></tr></table></figure><p>并将当前项目作为一个快照保存起来。项目的历史就是由不同时期的快照构成的。Git 可以使项目回到历史的任何一个快照。要切到不同的点可以用如下命令：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git checkout</span><br><span class="line">或</span><br><span class="line">git reset</span><br><span class="line">或</span><br><span class="line">git revert</span><br></pre></td></tr></table></figure><h3 id="4、Remote（远程仓库）"><a href="#4、Remote（远程仓库）" class="headerlink" title="4、Remote（远程仓库）"></a>4、Remote（远程仓库）</h3><p>但是要实现对代码更好的管理，是要将它存在远程仓库，所以当版本库存放了已提交的代码后，就可以将其推到远程仓库了。要将代码推到远程仓库，首先要知道要向远程仓库的哪里提交，即要知道分支，而分支（branch）就是指向某个快照的指针，分支名就是指针名。分支使得用户可以为快照起别名。而且，分支会自动更新，如果当前分支有新的快照，指针就会自动指向它。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git push</span><br></pre></td></tr></table></figure><p>上面命令用于将本地分支的更新推送到远程仓库。</p><p>而要查看快照可以用下面命令</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git log</span><br></pre></td></tr></table></figure><h2 id="三、从远程仓库到工作区"><a href="#三、从远程仓库到工作区" class="headerlink" title="三、从远程仓库到工作区"></a>三、从远程仓库到工作区</h2><p>从远程主机克隆一个版本库，要用到如下命令。该命令会在本地主机生成一个目录，与远程主机的版本库同名。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git clone &lt;版本库的网址&gt;</span><br></pre></td></tr></table></figure><p>当远程主机的版本库有了更新（新的 commit），需要将这些更新取回本地，这时就要用到<code>git fetch</code>命令。默认情况下，<code>git fetch</code>取回所有分支（branch）的更新。如果只想取回特定分支的更新，可以指定分支名。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git fetch &lt;远程主机名&gt; &lt;分支名&gt;</span><br></pre></td></tr></table></figure><p>取回远程主机的更新以后，可以在它的基础上，使用<code>git checkout</code>命令创建一个新的分支。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git checkout -b newBrach // 在当前分支上新建一个分支</span><br></pre></td></tr></table></figure><p>此外，也可以使用<code>git merge</code>命令或者<code>git rebase</code>命令，在本地分支上合并远程分支。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git merge origin/master</span><br><span class="line"></span><br><span class="line">git rebase origin/master</span><br></pre></td></tr></table></figure><p>或者可以用个更快的命令，直接<code>git pull</code>命令取回远程主机某个分支的更新，再与本地的指定分支合并。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git pull &lt;远程主机名&gt; &lt;远程分支名&gt;:&lt;本地分支名&gt; // 取回远程主机名的分支，与本地的分支合并</span><br></pre></td></tr></table></figure><p>等同于先做 <code>git fetch</code>，再做 <code>git merge</code>。</p><h2 id="四、Git-常用命令"><a href="#四、Git-常用命令" class="headerlink" title="四、Git 常用命令"></a>四、Git 常用命令</h2><p><strong>扩展阅读</strong></p><p>Git Book - <a href="https://git-scm.com/book/zh/">https://git-scm.com/book/zh/</a><br>git 简明指南 - <a href="http://rogerdudler.github.io/git-guide/index.zh.html">http://rogerdudler.github.io/git-guide/index.zh.html</a><br>常用 Git 命令清单 - <a href="http://www.ruanyifeng.com/blog/2015/12/git-cheat-sheet.html">http://www.ruanyifeng.com/blog/2015/12/git-cheat-sheet.html</a><br>猴子都能懂的 GIT 入门 - <a href="http://backlogtool.com/git-guide/cn/">http://backlogtool.com/git-guide/cn/</a><br>Git 教程 - <a href="http://www.liaoxuefeng.com/wiki/0013739516305929606dd18361248578c67b8067c8c017b000">http://www.liaoxuefeng.com/wiki/0013739516305929606dd18361248578c67b8067c8c017b000</a></p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p><a href="https://wsgzao.github.io/post/git/">https://wsgzao.github.io/post/git/</a></p>]]></content>
      
      
      
        <tags>
            
            <tag> Git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hexo 主题制作</title>
      <link href="/2019/07/29/Hexo%20%E4%B8%BB%E9%A2%98%E5%88%B6%E4%BD%9C/"/>
      <url>/2019/07/29/Hexo%20%E4%B8%BB%E9%A2%98%E5%88%B6%E4%BD%9C/</url>
      
        <content type="html"><![CDATA[<p>用过默认主题，也试过别人写的主题，最后还是想有一套自己的。于是做了 <a href="https://github.com/Yin-Hongwei/hexo-theme-Yin">Yin</a> 主题。本文记录从零搭建 Hexo 主题的完整流程：<strong>目录结构 → 数据来源 → 各页面实现 → 发布到官方主题库</strong>。</p><span id="more"></span><h2 id="一、前置知识"><a href="#一、前置知识" class="headerlink" title="一、前置知识"></a>一、前置知识</h2><p>制作 Hexo 主题前，建议先了解以下三块：</p><table><thead><tr><th>类别</th><th>技术选型</th><th>作用</th></tr></thead><tbody><tr><td>模板引擎</td><td><a href="https://pugjs.org/api/getting-started.html">Pug</a></td><td>通过 <code>extends</code> &#x2F; <code>include</code> &#x2F; <code>mixin</code> 复用页面结构</td></tr><tr><td>CSS 预处理</td><td><a href="http://stylus-lang.com/">Stylus</a></td><td>变量、嵌套、模块化样式管理</td></tr><tr><td>Hexo 文档</td><td><a href="https://hexo.io/docs/">官方文档</a></td><td>配置项、变量、辅助函数</td></tr></tbody></table><p>核心思路：<strong>layout 写结构，source 写样式</strong>，相同部分抽成模块，各页面按需组合。</p><h2 id="二、项目创建"><a href="#二、项目创建" class="headerlink" title="二、项目创建"></a>二、项目创建</h2><p>可以用脚手架生成，也可以手动创建。手动方式如下：</p><h3 id="1、创建主题目录"><a href="#1、创建主题目录" class="headerlink" title="1、创建主题目录"></a>1、创建主题目录</h3><p>在站点根目录的 <code>themes/</code> 下新建文件夹（如 <code>hexo-theme-Yin</code>），并在站点根目录的 <code>_config.yml</code> 中切换主题：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">theme:</span> <span class="string">hexo-theme-Yin</span>  <span class="comment"># 替换默认的 landscape</span></span><br></pre></td></tr></table></figure><h3 id="2、最小目录结构"><a href="#2、最小目录结构" class="headerlink" title="2、最小目录结构"></a>2、最小目录结构</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">themes/hexo-theme-Yin/</span><br><span class="line">├── _config.yml   <span class="comment"># 主题配置（菜单、评论、打赏等）</span></span><br><span class="line">├── layout/       <span class="comment"># 页面模板</span></span><br><span class="line">└── <span class="built_in">source</span>/       <span class="comment"># 静态资源（CSS、JS、字体等）</span></span><br></pre></td></tr></table></figure><p>需要实现的页面通常包括：<strong>首页、文章详情、归档、标签、关于</strong>。这些页面共享头部、底部和 <code>&lt;head&gt;</code> 内容，应拆成独立模块，避免重复编写。</p><h3 id="3、搭建基础布局"><a href="#3、搭建基础布局" class="headerlink" title="3、搭建基础布局"></a>3、搭建基础布局</h3><p>在 <code>layout/</code> 下创建 <code>index.pug</code>（首页），在 <code>layout/includes/</code> 下创建 <code>layout.pug</code>（全局布局）。<code>layout.pug</code> 只负责骨架，具体模块交给子文件处理：</p><img src="/2019/07/29/Hexo%20%E4%B8%BB%E9%A2%98%E5%88%B6%E4%BD%9C/00831rSTly1gdaoh2wd4jj31cq0o4790.jpg" class="" title="主题 layout 模块划分示意"><p>首页继承全局布局：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">extends includes/layout.pug</span><br></pre></td></tr></table></figure><p>本地预览：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo s</span><br><span class="line"><span class="comment"># 访问 http://localhost:4000/</span></span><br></pre></td></tr></table></figure><p>样式写在 <code>source/</code> 目录下，Stylus 的变量和嵌套能很好地管理全局样式。</p><h2 id="三、页面数据来源"><a href="#三、页面数据来源" class="headerlink" title="三、页面数据来源"></a>三、页面数据来源</h2><p>模板渲染时，数据来自三个配置文件和 Hexo 内置对象。</p><h3 id="1、站点配置（根目录-config-yml）"><a href="#1、站点配置（根目录-config-yml）" class="headerlink" title="1、站点配置（根目录 _config.yml）"></a>1、站点配置（根目录 <code>_config.yml</code>）</h3><p>控制整站行为，常用字段：</p><table><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>title</code> &#x2F; <code>subtitle</code></td><td>站点标题与副标题</td></tr><tr><td><code>url</code></td><td>站点地址，影响 <code>url_for()</code> 生成路径</td></tr><tr><td><code>permalink</code></td><td>文章 URL 规则</td></tr><tr><td><code>theme</code></td><td>当前使用的主题名</td></tr></tbody></table><h3 id="2、Hexo-内置对象"><a href="#2、Hexo-内置对象" class="headerlink" title="2、Hexo 内置对象"></a>2、Hexo 内置对象</h3><p>Hexo 在渲染时注入 <a href="https://hexo.io/docs/variables.html">变量</a> 和 <a href="https://hexo.io/docs/helpers.html">辅助函数</a>。</p><p><strong>常用变量：</strong></p><table><thead><tr><th>变量</th><th>作用域</th><th>说明</th></tr></thead><tbody><tr><td><code>site</code></td><td>全局</td><td>全站文章、页面、分类、标签集合</td></tr><tr><td><code>page</code></td><td>当前页</td><td>当前页的文章列表或单页信息</td></tr><tr><td><code>post</code></td><td>文章页</td><td>单篇文章详情，含 <code>tags</code>、<code>categories</code>、<code>published</code></td></tr><tr><td><code>theme</code></td><td>全局</td><td>主题 <code>_config.yml</code> 中的配置项</td></tr></tbody></table><p><code>site</code> 对象结构示意：</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">site = &#123;</span><br><span class="line">  <span class="attr">posts</span>: [],       <span class="comment">// 文章对象数组</span></span><br><span class="line">  <span class="attr">pages</span>: [],       <span class="comment">// 页面对象数组</span></span><br><span class="line">  <span class="attr">categories</span>: [],  <span class="comment">// 分类对象数组</span></span><br><span class="line">  <span class="attr">tags</span>: []         <span class="comment">// 标签对象数组</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>常用辅助函数：</strong></p><table><thead><tr><th>类别</th><th>函数</th><th>用途</th></tr></thead><tbody><tr><td>页面判断</td><td><code>is_home()</code>、<code>is_post()</code>、<code>is_tag()</code>、<code>is_category()</code>、<code>is_archive()</code></td><td>判断当前页面类型</td></tr><tr><td>时间处理</td><td><code>date()</code>、<code>time()</code></td><td>格式化日期时间</td></tr><tr><td>列表生成</td><td><code>list_categories()</code>、<code>list_tags()</code>、<code>tagcloud()</code></td><td>生成分类&#x2F;标签列表和标签云</td></tr><tr><td>文章目录</td><td><code>toc()</code></td><td>根据标题生成目录</td></tr><tr><td>分页</td><td><code>paginator()</code></td><td>生成分页导航</td></tr></tbody></table><h3 id="3、主题配置（themes-config-yml）"><a href="#3、主题配置（themes-config-yml）" class="headerlink" title="3、主题配置（themes/&lt;theme&gt;/_config.yml）"></a>3、主题配置（<code>themes/&lt;theme&gt;/_config.yml</code>）</h3><p>存放主题专属选项，通过 <code>theme.xxx</code> 在模板中读取。典型配置：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">menu:</span></span><br><span class="line">  <span class="attr">Home:</span> <span class="string">/</span></span><br><span class="line">  <span class="attr">Archives:</span> <span class="string">/archives</span></span><br><span class="line">  <span class="attr">Tags:</span> <span class="string">/tags</span></span><br><span class="line">  <span class="attr">About:</span> <span class="string">/about</span></span><br><span class="line"></span><br><span class="line"><span class="attr">reward:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">wechat:</span> <span class="string">/img/weixin.jpg</span></span><br></pre></td></tr></table></figure><h2 id="四、各页面实现"><a href="#四、各页面实现" class="headerlink" title="四、各页面实现"></a>四、各页面实现</h2><p><code>layout.pug</code> 是各页面的公共骨架，内部再拆分为 head、header、footer 等子模块。</p><h3 id="1、全局布局（layout-pug）"><a href="#1、全局布局（layout-pug）" class="headerlink" title="1、全局布局（layout.pug）"></a>1、全局布局（layout.pug）</h3><p><strong>头部（header.pug）</strong></p><p>利用 <code>url_for()</code> 将相对路径转为绝对路径，<code>theme.menu</code> 读取主题配置中的导航菜单：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">header#header</span><br><span class="line">  nav#navbar</span><br><span class="line">    span.nav-left</span><br><span class="line">      div.tou-img</span><br><span class="line">        img(src=&quot;/img/tou.jpg&quot;)</span><br><span class="line">      a#site-name(href=url_for(&#x27;/&#x27;))= config.title</span><br><span class="line">    i.fa.fa-bars.toggle-menu.nav-right(aria-hidden=&quot;true&quot;)</span><br><span class="line">    span.nav-right.menus</span><br><span class="line">      each url, label in theme.menu</span><br><span class="line">        a.site-page(href=url)= label</span><br></pre></td></tr></table></figure><p><strong>内容区</strong></p><p>中间内容因页面而异，在 <code>layout.pug</code> 中预留 <code>block content</code>，由子模板填充。</p><p><strong>尾部（footer.pug）</strong></p><p>放置版权、备案等声明信息。</p><h3 id="2、首页（index-pug）"><a href="#2、首页（index-pug）" class="headerlink" title="2、首页（index.pug）"></a>2、首页（index.pug）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">extends includes/layout.pug</span><br><span class="line"></span><br><span class="line">block content</span><br><span class="line">  include includes/recent-posts.pug</span><br><span class="line">  include includes/partial/pagination.pug</span><br></pre></td></tr></table></figure><p>首页主要做两件事：<strong>文章列表</strong>和<strong>分页</strong>。文章数据来自 <code>page</code> 变量，分页用 <code>paginator()</code> 辅助函数：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-</span><br><span class="line">  var options = &#123;</span><br><span class="line">    prev_text: &#x27;&lt;i class=&quot;fa fa-chevron-left&quot;&gt;&lt;/i&gt;&#x27;,</span><br><span class="line">    next_text: &#x27;&lt;i class=&quot;fa fa-chevron-right&quot;&gt;&lt;/i&gt;&#x27;,</span><br><span class="line">    mid_size: 1</span><br><span class="line">  &#125;</span><br><span class="line">nav#pagination</span><br><span class="line">  if !is_post()</span><br><span class="line">    .pagination</span><br><span class="line">      != paginator(options)</span><br></pre></td></tr></table></figure><h3 id="3、文章详情页（post-pug）"><a href="#3、文章详情页（post-pug）" class="headerlink" title="3、文章详情页（post.pug）"></a>3、文章详情页（post.pug）</h3><p>详情页侧重阅读体验，可扩展打赏、上下篇导航和评论等功能。</p><h4 id="微信打赏"><a href="#微信打赏" class="headerlink" title="微信打赏"></a>微信打赏</h4><p>在主题 <code>_config.yml</code> 中配置：</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">reward:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">wechat:</span> <span class="string">/img/weixin.jpg</span></span><br></pre></td></tr></table></figure><p>在 <code>post.pug</code> 中引用：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if theme.reward.enable</span><br><span class="line">  .qr-code</span><br><span class="line">    img.qrcode-img(src=theme.reward.wechat)</span><br><span class="line">    .qrcode-desc 微信打赏</span><br></pre></td></tr></table></figure><h4 id="上下篇导航"><a href="#上下篇导航" class="headerlink" title="上下篇导航"></a>上下篇导航</h4><p>与首页分页不同，详情页只需上一篇 &#x2F; 下一篇链接，可在分页模块中做兼容：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if page.prev</span><br><span class="line">  .prev-post.pull-left</span><br><span class="line">    a(href=url_for(page.prev.path))</span><br><span class="line">      i.fa.fa-chevron-left</span><br><span class="line">      span= page.prev.title</span><br><span class="line">if page.next</span><br><span class="line">  .next-post.pull-right</span><br><span class="line">    a(href=url_for(page.next.path))</span><br><span class="line">      span= page.next.title</span><br><span class="line">      i.fa.fa-chevron-right</span><br></pre></td></tr></table></figure><h4 id="评论"><a href="#评论" class="headerlink" title="评论"></a>评论</h4><p>借助第三方服务，在主题 <code>_config.yml</code> 中配置后在 <code>comments/</code> 目录引用即可。常见方案：</p><p><strong>Gitalk</strong>（<a href="https://gitalk.github.io/">gitalk.github.io</a>）</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">gitalk:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">id:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line">  <span class="attr">owner:</span> <span class="string">your-github-username</span></span><br><span class="line">  <span class="attr">admin:</span> <span class="string">your-github-username</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">your-repo-name</span></span><br><span class="line">  <span class="attr">client_id:</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line">  <span class="attr">client_secret:</span> <span class="string">&#x27;&#x27;</span></span><br></pre></td></tr></table></figure><p><strong>Disqus</strong>（<a href="https://disqus.com/">disqus.com</a>）</p><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">disqus:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">shortname:</span> <span class="string">your-shortname</span></span><br><span class="line">  <span class="attr">count:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure><h3 id="4、关于页"><a href="#4、关于页" class="headerlink" title="4、关于页"></a>4、关于页</h3><p>在站点 <code>source/about/</code> 下创建 <code>index.md</code>，写入内容后 Hexo 自动生成页面，再用主题样式调整排版即可。</p><h3 id="5、归档页"><a href="#5、归档页" class="headerlink" title="5、归档页"></a>5、归档页</h3><p>按年份分组展示文章时间线，用 Pug mixin 实现：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mixin articleSort(posts)</span><br><span class="line">  .article-sort</span><br><span class="line">    - var year</span><br><span class="line">    - posts.each(function (article) &#123;</span><br><span class="line">      - var tempYear = date(article.date, &#x27;YYYY&#x27;)</span><br><span class="line">      if tempYear !== year</span><br><span class="line">        - year = tempYear</span><br><span class="line">        .article-item.year= year</span><br><span class="line">      .article-item</span><br><span class="line">        time.article-item__time= date(article.date)</span><br><span class="line">        a.article-item__title(href=url_for(article.path))= article.title || &#x27;No Title&#x27;</span><br><span class="line">    - &#125;)</span><br></pre></td></tr></table></figure><h3 id="6、标签页"><a href="#6、标签页" class="headerlink" title="6、标签页"></a>6、标签页</h3><p>利用 <code>list_tags()</code> 生成标签云，在模板中按 <code>page.type</code> 判断渲染：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if page.type === &#x27;tags&#x27;</span><br><span class="line">  article#tag</span><br><span class="line">    h1.tag-title 标签</span><br><span class="line">    hr</span><br><span class="line">    .tag-cloud-tags!= list_tags()</span><br></pre></td></tr></table></figure><h2 id="五、发布到官方主题库"><a href="#五、发布到官方主题库" class="headerlink" title="五、发布到官方主题库"></a>五、发布到官方主题库</h2><p>主题完成后，可向 <a href="https://github.com/hexojs/site">hexojs&#x2F;site</a> 提交，收录到 <a href="https://hexo.io/themes/">Hexo 官方主题列表</a>。</p><ol><li>Fork <a href="https://github.com/hexojs/site">hexojs&#x2F;site</a> 仓库</li><li>编辑 <code>source/_data/theme.yml</code>，添加主题信息：</li></ol><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Yin</span></span><br><span class="line">  <span class="attr">description:</span> <span class="string">A</span> <span class="string">simple</span> <span class="string">&amp;</span> <span class="string">beautiful</span> <span class="string">&amp;</span> <span class="string">fast</span> <span class="string">theme</span> <span class="string">for</span> <span class="string">Hexo</span></span><br><span class="line">  <span class="attr">link:</span> <span class="string">https://github.com/Yin-Hongwei/hexo-theme-Yin</span></span><br><span class="line">  <span class="attr">preview:</span> <span class="string">https://yin-hongwei.github.io/</span></span><br><span class="line">  <span class="attr">tags:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">simple</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">beautiful</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">fast</span></span><br></pre></td></tr></table></figure><ol start="3"><li>在 <code>source/theme/screenshots/</code> 放置 800×500 的预览图，文件名与主题名一致</li><li>提交到自己 fork 的仓库，向官方仓库发起 Pull Request</li></ol><p>审核合并后，主题就会出现在官方主题列表中。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Hexo, 博客, 前端 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vue + SpringBoot + MyBatis 音乐网站</title>
      <link href="/2019/03/04/music/"/>
      <url>/2019/03/04/music/</url>
      
        <content type="html"><![CDATA[<h2 id="一、前言"><a href="#一、前言" class="headerlink" title="一、前言"></a>一、前言</h2><p>本文梳理音乐网站从设计到落地的完整思路，重点讲清<strong>数据如何从数据库一步步流到前端页面</strong>。先建立整体流程感，再逐个击破接口、组件等细节——无论是照着做一遍，还是迁移到其他项目，都会顺手得多。</p><p>项目源码：<a href="https://github.com/Yin-Hongwei/music-website">github.com&#x2F;Yin-Hongwei&#x2F;music-website</a></p><p><img src="/2019/03/04/music/007mxWTUgy1icr7mwfp2sj32c00ikqv6.jpg" alt="音乐网站效果预览"></p><span id="more"></span><h2 id="二、项目架构"><a href="#二、项目架构" class="headerlink" title="二、项目架构"></a>二、项目架构</h2><p>仓库分为三个子项目：<strong>前台</strong> <code>music-client</code>、<strong>管理端</strong> <code>music-manage</code>、<strong>后端 API</strong> <code>music-server</code>。前台与管理端均为 Vue 应用，通过 Axios 调用 Spring Boot 接口；后端按 Controller → Service → Mapper 分层访问 MySQL。结合下图，后文将从数据库到页面，沿这条链路逐层拆解。</p><img src="/2019/03/04/music/architecture.svg" class="" title="音乐网站项目架构图"><br/><h2 id="三、开发环境"><a href="#三、开发环境" class="headerlink" title="三、开发环境"></a>三、开发环境</h2><p>本节只列<strong>从零起步</strong>所需的基本开发环境，先把后端、数据库和前端跑通即可。随着项目后期功能增多、架构变复杂，Redis 等依赖再按需引入。</p><table><thead><tr><th>环境</th><th>版本</th></tr></thead><tbody><tr><td>操作系统</td><td>macOS</td></tr><tr><td>JDK</td><td>8+（如 jdk-8u141）</td></tr><tr><td>MySQL</td><td>8.0+</td></tr><tr><td>Node.js</td><td>14+</td></tr><tr><td>IDE</td><td>IntelliJ IDEA &#x2F; VS Code</td></tr></tbody></table><br/><h2 id="四、后端项目开发"><a href="#四、后端项目开发" class="headerlink" title="四、后端项目开发"></a>四、后端项目开发</h2><h3 id="1、创建数据库表"><a href="#1、创建数据库表" class="headerlink" title="1、创建数据库表"></a>1、创建数据库表</h3><p>开发后端之前，先把业务里要持久化的东西想清楚。音乐网站至少需要这几类数据：</p><ul><li><strong>用户侧</strong>：前台用户（<code>user</code>）、后台管理员（<code>admin</code>）</li><li><strong>内容侧</strong>：歌手（<code>singer</code>）、歌曲（<code>song</code>）、歌单（<code>song_sheet</code>）</li><li><strong>展示侧</strong>：首页轮播（<code>banner</code>）</li><li><strong>互动侧</strong>：收藏（<code>collect</code>）、评论（<code>comment</code>）</li></ul><p>表之间的关系可以这样理解：</p><ul><li>歌手和歌曲是一对多：一首歌对应一个歌手，所以在 <code>song</code> 里放 <code>singer_id</code></li><li>歌单和歌曲是多对多：一首歌可以出现在多个歌单里，所以用 <code>song_sheet_song</code> 做中间表</li><li>收藏和评论都可能是针对「歌曲」或「歌单」，所以在 <code>collect</code>、<code>comment</code> 里同时保留 <code>song_id</code>、<code>song_sheet_id</code>，再用 <code>type</code> 区分目标类型</li></ul><p><img src="/2019/03/04/music/00831rSTly1gcn0yy1tccj32bf0u01kx.jpg" alt="音乐网站数据库表结构（图有点旧了）"></p><h3 id="2、创建项目"><a href="#2、创建项目" class="headerlink" title="2、创建项目"></a>2、创建项目</h3><p>在 IntelliJ IDEA 中新建项目，选择 <strong>Spring Initializr</strong>，并按需勾选依赖：<strong>Web → Spring Web Starter</strong>，以及 <strong>SQL → MySQL Driver</strong>、<strong>MyBatis Framework</strong>。一路 Next 完成后，将得到如下项目结构（典型三层架构）：</p><p><img src="/2019/03/04/music/00831rSTly1gcn02rv93zj31c00u01kx.jpg" alt="Spring Initializr 创建后的项目结构"></p><h3 id="3、配置文件"><a href="#3、配置文件" class="headerlink" title="3、配置文件"></a>3、配置文件</h3><h4 id="3-1、application-properties"><a href="#3-1、application-properties" class="headerlink" title="3.1、application.properties"></a>3.1、application.properties</h4><p>在 <code>src/main/resources/application.properties</code> 中配置数据库连接，起步阶段只需以下几项：</p><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 服务端口</span></span><br><span class="line"><span class="attr">server.port</span>=<span class="string">8888</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"># 数据源（按本地 MySQL 账号密码修改）</span></span><br><span class="line"><span class="attr">spring.datasource.url</span>=<span class="string">jdbc:mysql://localhost:3306/yin_music?serverTimezone=Asia/Shanghai&amp;useSSL=false&amp;characterEncoding=utf8</span></span><br><span class="line"><span class="attr">spring.datasource.username</span>=<span class="string">root</span></span><br><span class="line"><span class="attr">spring.datasource.password</span>=<span class="string">123456</span></span><br><span class="line"><span class="attr">spring.datasource.driver-class-name</span>=<span class="string">com.mysql.cj.jdbc.Driver</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment"># MyBatis</span></span><br><span class="line"><span class="attr">mybatis.mapperLocations</span>=<span class="string">classpath:mapper/*.xml</span></span><br><span class="line"><span class="attr">mybatis.typeAliasesPackage</span>=<span class="string">com.example.demo.domain</span></span><br></pre></td></tr></table></figure><h4 id="3-2、generatorConfig-xml"><a href="#3-2、generatorConfig-xml" class="headerlink" title="3.2、generatorConfig.xml"></a>3.2、generatorConfig.xml</h4><p><a href="https://mybatis.org/generator/">MyBatis Generator（MBG）</a> 可以根据已有数据库表，自动生成 <strong>实体类</strong>、<strong>Mapper 接口</strong> 和 <strong>Mapper.xml</strong>。项目起步阶段通常只需执行一次；生成完成后，建议在 <code>pom.xml</code> 里注释掉 MBG 插件，避免每次构建重复覆盖代码。</p><p>在 <code>src/main/resources</code> 下新建 <code>generatorConfig.xml</code>，需要配置三块内容：</p><ol><li><strong>JDBC 连接</strong>：库地址、账号、密码（与 <code>application.properties</code> 保持一致）</li><li><strong>生成路径</strong>：实体类、Mapper 接口、Mapper.xml 分别输出到哪个包&#x2F;目录</li><li><strong>目标表</strong>：用 <code>&lt;table&gt;</code> 列出要生成的表名</li></ol><p>示例（包名、表名按项目修改）：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">generatorConfiguration</span></span></span><br><span class="line"><span class="meta">  <span class="keyword">PUBLIC</span> <span class="string">&quot;-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN&quot;</span></span></span><br><span class="line"><span class="meta">  <span class="string">&quot;http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">generatorConfiguration</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">context</span> <span class="attr">id</span>=<span class="string">&quot;default&quot;</span> <span class="attr">targetRuntime</span>=<span class="string">&quot;MyBatis3&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">jdbcConnection</span> <span class="attr">driverClass</span>=<span class="string">&quot;com.mysql.cj.jdbc.Driver&quot;</span></span></span><br><span class="line"><span class="tag">      <span class="attr">connectionURL</span>=<span class="string">&quot;jdbc:mysql://localhost:3306/yin_music?serverTimezone=Asia/Shanghai<span class="symbol">&amp;amp;</span>useSSL=false<span class="symbol">&amp;amp;</span>characterEncoding=utf8&quot;</span></span></span><br><span class="line"><span class="tag">      <span class="attr">userId</span>=<span class="string">&quot;root&quot;</span> <span class="attr">password</span>=<span class="string">&quot;123456&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">javaModelGenerator</span> <span class="attr">targetPackage</span>=<span class="string">&quot;com.example.demo.domain&quot;</span> <span class="attr">targetProject</span>=<span class="string">&quot;src/main/java&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">sqlMapGenerator</span> <span class="attr">targetPackage</span>=<span class="string">&quot;mapper&quot;</span> <span class="attr">targetProject</span>=<span class="string">&quot;src/main/resources&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">javaClientGenerator</span> <span class="attr">targetPackage</span>=<span class="string">&quot;com.example.demo.dao&quot;</span> <span class="attr">targetProject</span>=<span class="string">&quot;src/main/java&quot;</span> <span class="attr">type</span>=<span class="string">&quot;XMLMAPPER&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">table</span> <span class="attr">tableName</span>=<span class="string">&quot;consumer&quot;</span> <span class="attr">domainObjectName</span>=<span class="string">&quot;Consumer&quot;</span></span></span><br><span class="line"><span class="tag">      <span class="attr">enableCountByExample</span>=<span class="string">&quot;false&quot;</span> <span class="attr">enableUpdateByExample</span>=<span class="string">&quot;false&quot;</span></span></span><br><span class="line"><span class="tag">      <span class="attr">enableDeleteByExample</span>=<span class="string">&quot;false&quot;</span> <span class="attr">enableSelectByExample</span>=<span class="string">&quot;false&quot;</span></span></span><br><span class="line"><span class="tag">      <span class="attr">selectByExampleQueryId</span>=<span class="string">&quot;false&quot;</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 其余表同理追加 &lt;table&gt; --&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">context</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">generatorConfiguration</span>&gt;</span></span><br></pre></td></tr></table></figure><h4 id="3-3、pom-xml"><a href="#3-3、pom-xml" class="headerlink" title="3.3、pom.xml"></a>3.3、pom.xml</h4><p><code>pom.xml</code> 负责两块：<strong>依赖</strong>（<code>dependencies</code>）和 <strong>插件</strong>（<code>plugins</code>）。用 Spring Initializr 建项目时，Web、MySQL、MyBatis 等基础依赖已经带上了，再按需补充下面几项：</p><table><thead><tr><th>依赖</th><th>说明</th></tr></thead><tbody><tr><td>mybatis-spring-boot-starter</td><td>MyBatis 与 Spring Boot 集成</td></tr><tr><td>mysql-connector-java</td><td>MySQL JDBC 驱动</td></tr><tr><td>fastjson</td><td>JSON 与 JavaBean 互转</td></tr></tbody></table><p>MBG 插件配合上一节的 <code>generatorConfig.xml</code>，在 <code>plugins</code> 中加入：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis.generator<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-generator-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.4.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">configurationFile</span>&gt;</span>src/main/resources/generatorConfig.xml<span class="tag">&lt;/<span class="name">configurationFile</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">overwrite</span>&gt;</span>true<span class="tag">&lt;/<span class="name">overwrite</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br></pre></td></tr></table></figure><p>执行 <code>./mvnw mybatis-generator:generate</code> 生成 <strong>dao</strong>、<strong>domain</strong>、<strong>mapper</strong> 及对应文件：</p><p><img src="/2019/03/04/music/00831rSTly1gcmzn2nmzvj31c00u0k3d.jpg" alt="MyBatis Generator 生成代码后的项目结构"></p><p>生成完成后<strong>注释掉</strong> MBG 插件，避免后续构建重复覆盖已有代码。</p><h3 id="4、测试（以新建一个用户为例）"><a href="#4、测试（以新建一个用户为例）" class="headerlink" title="4、测试（以新建一个用户为例）"></a>4、测试（以新建一个用户为例）</h3><p>MBG 生成代码后，先写一条「插入用户」的链路，确认数据库连接和 MyBatis 映射都正常。下面用 MBG 生成的 <code>Consumer</code> 为例，包名与第三节配置保持一致（与 <a href="https://github.com/Yin-Hongwei/music-website">项目源码</a> 最新代码可能不同，以教程为准）。</p><h4 id="4-1、新建-Service-层"><a href="#4-1、新建-Service-层" class="headerlink" title="4.1、新建 Service 层"></a>4.1、新建 Service 层</h4><p>后端采用 <strong>Controller → Service → Mapper</strong> 分层（第五节会展开）。MBG 只生成 Mapper，Service 需手动创建。这里先补上 Service，让本节测试与后续接口走同一条链路；当前 <code>addUser</code> 只是转发到 Mapper，等业务复杂后再在 Service 里加校验、转换等逻辑。</p><p>在 <code>src/main/java/com/example/demo/service</code> 下新建：</p><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">service/</span><br><span class="line">├── ConsumerService.java</span><br><span class="line">└── impl/</span><br><span class="line">    └── ConsumerServiceImpl.java</span><br></pre></td></tr></table></figure><p><strong>ConsumerService 接口</strong> — 声明 <code>addUser</code> 方法：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">ConsumerService</span> &#123;</span><br><span class="line">    <span class="type">boolean</span> <span class="title function_">addUser</span><span class="params">(Consumer consumer)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/2019/03/04/music/00831rSTly1gcn3as4enxj31c00u0wpz.jpg" alt="ConsumerService 接口代码"></p><p><strong>ConsumerServiceImpl 实现类</strong> — 调用 Mapper 写入数据库：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ConsumerServiceImpl</span> <span class="keyword">implements</span> <span class="title class_">ConsumerService</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> ConsumerMapper consumerMapper;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">addUser</span><span class="params">(Consumer consumer)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> consumerMapper.addUser(consumer) &gt; <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/2019/03/04/music/00831rSTly1gcn3a22ilrj31c00u016m.jpg" alt="ConsumerServiceImpl 实现类代码"></p><h4 id="4-2、补充-Mapper"><a href="#4-2、补充-Mapper" class="headerlink" title="4.2、补充 Mapper"></a>4.2、补充 Mapper</h4><p>MBG <strong>默认</strong>会为每张表生成 <code>insert</code>、<code>insertSelective</code> 等通用 CRUD 方法（接口 + XML 已有，无需手写）。本节为与 4.1 的 <code>addUser</code> 命名一致，<strong>额外</strong>在接口和 XML 里补一条同名映射——SQL 复制默认生成的 <code>insertSelective</code> 即可，只改 <code>id</code>。</p><p><strong>ConsumerMapper 接口</strong>（<code>src/main/java/com/example/demo/dao/ConsumerMapper.java</code>）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">addUser</span><span class="params">(Consumer record)</span>;</span><br></pre></td></tr></table></figure><p><img src="/2019/03/04/music/00831rSTly1gcn3dnt4h1j31tf0u018e.jpg" alt="ConsumerMapper 接口 addUser 方法"></p><p><strong>ConsumerMapper.xml</strong>（<code>src/main/resources/mapper/ConsumerMapper.xml</code>）— 复制 MBG 默认生成的 <code>insertSelective</code> 节点，把 <code>id</code> 改为 <code>addUser</code>：</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">&quot;addUser&quot;</span> <span class="attr">parameterType</span>=<span class="string">&quot;com.example.demo.domain.Consumer&quot;</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 字段与 MBG 默认生成的 insertSelective 保持一致 --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br></pre></td></tr></table></figure><p><img src="/2019/03/04/music/00831rSTly1gcn1jzgw6cj32bs0jgayt.jpg" alt="ConsumerMapper.xml 中 addUser SQL 映射"></p><h4 id="4-3、启动类扫描-Mapper"><a href="#4-3、启动类扫描-Mapper" class="headerlink" title="4.3、启动类扫描 Mapper"></a>4.3、启动类扫描 Mapper</h4><p>MBG 生成的 Mapper 接口不会自动注册到 Spring 容器，需要在启动类上用 <code>@MapperScan</code> 指定扫描包（与 3.2 中 <code>javaClientGenerator</code> 的 <code>targetPackage</code> 一致），否则注入 <code>ConsumerMapper</code> 时会报找不到 Bean。</p><p>在 <code>src/main/java/com/example/demo/DemoApplication.java</code> 上添加：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@MapperScan(&quot;com.example.demo.dao&quot;)</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DemoApplication</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        SpringApplication.run(DemoApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-4、写单元测试"><a href="#4-4、写单元测试" class="headerlink" title="4.4、写单元测试"></a>4.4、写单元测试</h4><p>前面几步就绪后，用 <code>@SpringBootTest</code> 启动 Spring 容器，走一遍 <strong>Service → Mapper → 数据库</strong> 的完整链路（会连真实 MySQL，属于集成测试）。</p><p>在 <code>src/test/java/com/example/demo/DemoApplicationTests.java</code> 中编写：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootTest</span></span><br><span class="line"><span class="meta">@RunWith(SpringRunner.class)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DemoApplicationTests</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> ConsumerService consumerService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addUser</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">Consumer</span> <span class="variable">consumer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Consumer</span>();</span><br><span class="line">        consumer.setUsername(<span class="string">&quot;yin&quot;</span>);</span><br><span class="line">        consumer.setPassword(<span class="string">&quot;123456&quot;</span>);</span><br><span class="line">        consumerService.addUser(consumer);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>右键运行 <code>addUser</code> 测试方法：控制台无报错，且 <code>consumer</code> 表里能看到新记录，说明后端链路已跑通，可以开始写接口。</p><p><img src="/2019/03/04/music/00831rSTly1gcn1leku2lj31bv0u0npd.jpg" alt="新建用户单元测试运行结果"></p><h3 id="5、开发接口（以返回所有用户信息为例）"><a href="#5、开发接口（以返回所有用户信息为例）" class="headerlink" title="5、开发接口（以返回所有用户信息为例）"></a>5、开发接口（以返回所有用户信息为例）</h3><p>第四节验证了「写入」，这里从「读取」入手，走一遍完整的 HTTP 请求链路。包名仍与第三节、第四节保持一致。</p><h4 id="5-1、分层说明"><a href="#5-1、分层说明" class="headerlink" title="5.1、分层说明"></a>5.1、分层说明</h4><p>后端按四层组织，请求自上而下、响应自下而上：</p><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">浏览器 → Controller → Service → Mapper（+ XML）→ 数据库</span><br></pre></td></tr></table></figure><table><thead><tr><th>层次</th><th>目录</th><th>职责</th></tr></thead><tbody><tr><td>实体类</td><td><code>domain</code></td><td>对应数据库表，MBG 已生成</td></tr><tr><td>Mapper &#x2F; DAO</td><td><code>dao</code> + <code>mapper</code></td><td>接口声明数据操作，XML 编写 SQL</td></tr><tr><td>Service</td><td><code>service</code> + <code>impl</code></td><td>业务逻辑，调用 Mapper</td></tr><tr><td>Controller</td><td><code>controller</code></td><td>接收 HTTP 请求，调用 Service 并返回结果</td></tr></tbody></table><h4 id="5-2、实战：返回所有用户"><a href="#5-2、实战：返回所有用户" class="headerlink" title="5.2、实战：返回所有用户"></a>5.2、实战：返回所有用户</h4><p>目标：访问 <code>http://localhost:8888/allUser</code> 拿到全部用户。按 <strong>Controller → Service → Mapper</strong> 逐层补充 <code>allUser</code> 方法。</p><p><strong>Controller 层</strong> — 监听 <code>/allUser</code>，交给 Service 处理：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ConsumerController</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> ConsumerService consumerService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping(&quot;/allUser&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">allUser</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> consumerService.allUser();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/2019/03/04/music/00831rSTly1gcn1wesspvj31c00u0gyp.jpg" alt="ConsumerController 返回所有用户信息"></p><p><strong>Service 层</strong> — 接口声明方法，实现类调用 Mapper：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ConsumerService.java</span></span><br><span class="line">List&lt;Consumer&gt; <span class="title function_">allUser</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ConsumerServiceImpl.java</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> List&lt;Consumer&gt; <span class="title function_">allUser</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> consumerMapper.allUser();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="/2019/03/04/music/00831rSTly1gcn2ddiz3vj31lo0jgwou.jpg" alt="ConsumerService 接口 allUser 方法"></p><p><img src="/2019/03/04/music/00831rSTly1gcn213s1vaj31c00u0ws3.jpg" alt="ConsumerServiceImpl 实现 allUser 方法"></p><p><strong>Mapper 层</strong> — 接口 + XML 查询数据库（SQL 写法与 4.2 类似，复制 MBG 默认生成的 <code>select</code> 并改 <code>id</code> 亦可）：</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// ConsumerMapper.java</span></span><br><span class="line">List&lt;Consumer&gt; <span class="title function_">allUser</span><span class="params">()</span>;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- ConsumerMapper.xml --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">&quot;allUser&quot;</span> <span class="attr">resultMap</span>=<span class="string">&quot;BaseResultMap&quot;</span>&gt;</span></span><br><span class="line">  select * from consumer</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure><p><img src="/2019/03/04/music/00831rSTly1gcn2bvylbhj31i00o4149.jpg" alt="ConsumerMapper 接口 allUser 方法"></p><p><img src="/2019/03/04/music/00831rSTly1gcn271thzzj32700ngtvn.jpg" alt="ConsumerMapper.xml 中 allUser SQL 映射"></p><p><strong>启动验证</strong> — 运行项目并在浏览器访问：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./mvnw spring-boot:run</span><br></pre></td></tr></table></figure><p>浏览器打开 <a href="http://localhost:8888/allUser">http://localhost:8888/allUser</a> 查看 JSON 结果。</p><p><img src="/2019/03/04/music/00831rSTly1gcn2tz9lhtj325q0ma7br.jpg" alt="Spring Boot 项目启动日志"></p><p><img src="/2019/03/04/music/00831rSTly1gcn2sihzijj327y0j6ai5.jpg" alt="浏览器访问 &#x2F;allUser 接口返回结果"></p><h4 id="5-3、延伸"><a href="#5-3、延伸" class="headerlink" title="5.3、延伸"></a>5.3、延伸</h4><p>其余接口按同样分层逐层添加方法即可。联调前端时还需注意两点：<strong>跨域</strong>（见 <code>config</code> 目录）和<strong>静态资源路径</strong>（歌曲、图片等文件的存放与访问）。</p><h4 id="5-4、小结"><a href="#5-4、小结" class="headerlink" title="5.4、小结"></a>5.4、小结</h4><p>一次 HTTP 请求的完整路径：<strong>Controller 接收请求 → Service 处理业务 → Mapper 访问数据库 → 结果沿原路返回前端</strong>。</p><h3 id="6、最终项目结构"><a href="#6、最终项目结构" class="headerlink" title="6、最终项目结构"></a>6、最终项目结构</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">.</span><br><span class="line">├── avatorImages // 用户头像</span><br><span class="line">├── img</span><br><span class="line">│   ├── singerPic // 歌手图片</span><br><span class="line">│   ├── songListPic // 歌单图片</span><br><span class="line">│   └── songPic // 歌曲图片</span><br><span class="line">├── song // 存放歌曲</span><br><span class="line">├── src</span><br><span class="line">│   ├── main</span><br><span class="line">│   │   ├── java</span><br><span class="line">│   │   │   └── com.example.yin</span><br><span class="line">│   │   │       ├── config // 配置（跨域）</span><br><span class="line">│   │   │       ├── controller // 控制层，接收请求返回响应</span><br><span class="line">│   │   │       ├── dao // 数据操作层</span><br><span class="line">│   │   │       ├── domain // 实体类</span><br><span class="line">│   │   │       ├── constant</span><br><span class="line">│   │   │       ├── service</span><br><span class="line">│   │   │       │   └── impl // Service 层的接口</span><br><span class="line">│   │   │       └── HwMusicApplicationTests.java // 项目入口</span><br><span class="line">│   │   └── resources</span><br><span class="line">│   │       ├── mapper // mapper.xml文件，操作数据库</span><br><span class="line">│   │       ├── static // 存放静态资源</span><br><span class="line">│   │       ├── templates</span><br><span class="line">│   │       ├── application.properties // 连接数据库</span><br><span class="line">│   │       └── generatorConfig.xml // MyBatis Generator 自动生成代码</span><br><span class="line">│   └── <span class="built_in">test</span></span><br><span class="line">│       └── java</span><br><span class="line">│           └── com.example.demo // 测试用的</span><br><span class="line">├── pom.xml // 添加相关依赖和插件</span><br><span class="line">└── target</span><br></pre></td></tr></table></figure><br/><h2 id="五、客户端项目开发"><a href="#五、客户端项目开发" class="headerlink" title="五、客户端项目开发"></a>五、客户端项目开发</h2><p>前台项目位于仓库 <code>music-client/</code>，基于 <strong>Vue 3 + TypeScript + Pinia + Vue Router + Axios + Element Plus</strong> 实现。</p><h3 id="1、创建项目"><a href="#1、创建项目" class="headerlink" title="1、创建项目"></a>1、创建项目</h3><p>客户端的创建比后端简单，使用 Vue CLI 脚手架即可。进入 <code>music-website</code> 目录后，可参照如下方式初始化：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install -g @vue/cli</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在 music-website 目录下创建</span></span><br><span class="line">vue create music-client</span><br><span class="line"><span class="comment"># 建议选择：Vue 3 · TypeScript · Router · Pinia · ESLint</span></span><br></pre></td></tr></table></figure><p>依赖安装完成后进入项目目录启动：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> music-client</span><br><span class="line">npm install</span><br><span class="line">npm run serve</span><br></pre></td></tr></table></figure><p>启动成功后，直接点击控制台输出的本地地址即可访问前端页面。</p><p><img src="/2019/03/04/music/00831rSTly1gcn4octo9zj31ed0u0q9o.jpg" alt="Vue 客户端项目运行效果"></p><p>如果是新手，建议先阅读 <a href="https://cn.vuejs.org/guide/introduction.html">Vue 3 官方文档</a>，了解组件、路由、组合式 API 等基础概念后再动手。</p><h3 id="2、开发思路"><a href="#2、开发思路" class="headerlink" title="2、开发思路"></a>2、开发思路</h3><p>代码按职责拆分到不同目录，便于后期维护：</p><ul><li><strong>pages</strong>：页面级视图，负责路由对应的整页展示（首页、搜索、歌手、歌单、个人中心等）</li><li><strong>components</strong>：可复用组件，按 <code>auth</code>（认证布局）、<code>business</code>（业务组件）、<code>layouts</code>（页头页脚、播放栏）、<code>player</code>（音频播放）分类</li><li><strong>api</strong>：按模块封装后端接口（歌曲、歌手、歌单、评论、用户等），统一走 <code>utils/request.ts</code> 中的 Axios 实例</li><li><strong>store</strong>：Pinia 状态管理，拆分 <code>user</code>（用户）、<code>player</code>（播放器）、<code>song</code>（当前歌曲）、<code>search</code>（搜索）、<code>configure</code>（全局 UI 状态）等模块</li><li><strong>composables</strong>：抽取跨组件复用的逻辑，如导航、搜索关键词、全局操作等</li><li><strong>router</strong>：路由配置，<code>YinAppLayout.vue</code> 作为带页头&#x2F;页脚&#x2F;播放栏的主布局，子路由挂载各页面</li></ul><p>开发流程建议：先搭页面骨架（可写死数据），再通过 <code>api</code> 层请求后端数据；前端请求后端接口需在 <code>vue.config.js</code> 中通过 <code>NODE_HOST</code> 配置后端地址（如 <code>http://localhost:8888</code>）；遇到跨域问题可在后端配置 CORS，或在 <code>vue.config.js</code> 中配置 devServer 代理。</p><blockquote><p>前台组件以自研为主，第三方 UI 用得较少，适合练习组件封装与状态管理。</p></blockquote><h3 id="3、最终项目结构"><a href="#3、最终项目结构" class="headerlink" title="3、最终项目结构"></a>3、最终项目结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">music-client/</span><br><span class="line">├── public/                 # 静态资源（index.html 等）</span><br><span class="line">├── src/</span><br><span class="line">│   ├── api/                # 接口封装（auth、song、singer、songSheet、comment、user…）</span><br><span class="line">│   ├── assets/             # 样式、图标、图片</span><br><span class="line">│   │   ├── css/            # global.scss、var.scss 等</span><br><span class="line">│   │   ├── icons/          # iconfont</span><br><span class="line">│   │   └── images/</span><br><span class="line">│   ├── components/</span><br><span class="line">│   │   ├── auth/           # AuthLayout.vue 认证页布局</span><br><span class="line">│   │   ├── business/       # Comment、SongList、CoverCardGrid 等业务组件</span><br><span class="line">│   │   ├── layouts/        # YinAppLayout、YinHeader、YinFooter、YinPlayerBar、YinPlayQueue</span><br><span class="line">│   │   └── player/         # YinAudio 音频播放</span><br><span class="line">│   ├── composables/        # useAppActions、useNavigation、useSearchKeyword</span><br><span class="line">│   ├── enums/              # 路由名、导航标签、地区等枚举</span><br><span class="line">│   ├── pages/              # 页面视图</span><br><span class="line">│   │   ├── auth/           # SignIn、SignUp、ForgotPassword</span><br><span class="line">│   │   ├── error/          # 404</span><br><span class="line">│   │   ├── home/           # Home 首页</span><br><span class="line">│   │   ├── lyric/          # Lyric 歌词页</span><br><span class="line">│   │   ├── search/         # Search、SearchSong、SearchSongList</span><br><span class="line">│   │   ├── singer/         # Singer、SingerDetail</span><br><span class="line">│   │   ├── song-sheet/     # SongSheet、SongSheetDetail</span><br><span class="line">│   │   └── user/           # Personal、PersonalData、Upload、Setting、Password</span><br><span class="line">│   ├── router/             # 路由配置</span><br><span class="line">│   ├── store/              # Pinia：configure、player、search、song、user</span><br><span class="line">│   ├── utils/              # request、validate、api-response-data</span><br><span class="line">│   ├── App.vue</span><br><span class="line">│   └── main.ts             # 入口，注册 Pinia、Router、Element Plus</span><br><span class="line">├── babel.config.js</span><br><span class="line">├── package.json</span><br><span class="line">├── tsconfig.json</span><br><span class="line">└── vue.config.js           # 构建配置，含后端 API 地址</span><br></pre></td></tr></table></figure><br/><h2 id="六、管理端项目开发"><a href="#六、管理端项目开发" class="headerlink" title="六、管理端项目开发"></a>六、管理端项目开发</h2><p>后台项目位于 <code>music-manage/</code>，同样基于 <strong>Vue 3 + TypeScript</strong>，使用 <strong>Element Plus</strong> 快速搭建表格、表单、弹窗等管理界面，整体分层与前台一致（<code>api</code> &#x2F; <code>views</code> &#x2F; <code>components</code> &#x2F; <code>stores</code> &#x2F; <code>router</code>）。</p><h3 id="1、创建与启动"><a href="#1、创建与启动" class="headerlink" title="1、创建与启动"></a>1、创建与启动</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> music-manage</span><br><span class="line">npm install</span><br><span class="line">npm run serve</span><br></pre></td></tr></table></figure><p>启动成功后，直接点击控制台输出的本地地址即可访问。</p><h3 id="2、开发思路-1"><a href="#2、开发思路-1" class="headerlink" title="2、开发思路"></a>2、开发思路</h3><p>管理端以 CRUD 为主：每个 <code>views</code> 页面对应一类资源（Banner、用户、歌曲、歌手、歌单、评论、收藏等），列表展示与增删改查通过 Element Plus 的 Table、Form、Dialog 等组件完成。<code>api</code> 层按资源拆分接口；<code>components/layouts</code> 提供侧边栏（YinAside）、顶栏（YinHeader）和整体布局（YinLayout）；<code>stores/app.ts</code> 用 Pinia 管理侧边栏折叠等全局状态；<code>mixins/mixin.ts</code> 保留部分跨页面公共方法。</p><h3 id="3、最终项目结构-1"><a href="#3、最终项目结构-1" class="headerlink" title="3、最终项目结构"></a>3、最终项目结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">music-manage/</span><br><span class="line">├── public/</span><br><span class="line">├── src/</span><br><span class="line">│   ├── api/                # admin、banner、collection、comment、song、singer、songSheet、user…</span><br><span class="line">│   ├── assets/             # 样式与图片</span><br><span class="line">│   ├── components/</span><br><span class="line">│   │   ├── dialog/         # YinDelDialog 删除确认弹窗</span><br><span class="line">│   │   └── layouts/        # YinAside、YinHeader、YinLayout</span><br><span class="line">│   ├── enums/</span><br><span class="line">│   ├── mixins/             # mixin.ts 公共方法</span><br><span class="line">│   ├── router/</span><br><span class="line">│   ├── stores/             # app.ts 全局状态</span><br><span class="line">│   ├── utils/              # request、emitter、routeQuery</span><br><span class="line">│   ├── views/</span><br><span class="line">│   │   ├── BannerPage.vue      # Banner 管理</span><br><span class="line">│   │   ├── CollectPage.vue     # 收藏管理</span><br><span class="line">│   │   ├── CommentPage.vue     # 评论管理</span><br><span class="line">│   │   ├── InfoPage.vue        # 后台首页 / 信息概览</span><br><span class="line">│   │   ├── ListSongPage.vue    # 歌单-歌曲关联</span><br><span class="line">│   │   ├── Login.vue           # 管理员登录</span><br><span class="line">│   │   ├── singer/</span><br><span class="line">│   │   │   ├── SingerPage.vue  # 歌手管理</span><br><span class="line">│   │   │   └── SongPage.vue    # 歌曲管理</span><br><span class="line">│   │   ├── songSheet/</span><br><span class="line">│   │   │   └── SongSheetPage.vue  # 歌单管理</span><br><span class="line">│   │   └── user/</span><br><span class="line">│   │       └── ConsumerPage.vue   # 用户管理</span><br><span class="line">│   ├── App.vue</span><br><span class="line">│   └── main.ts</span><br><span class="line">├── babel.config.js</span><br><span class="line">├── package.json</span><br><span class="line">├── tsconfig.json</span><br><span class="line">└── vue.config.js</span><br></pre></td></tr></table></figure><br/><h2 id="七、贡献者"><a href="#七、贡献者" class="headerlink" title="七、贡献者"></a>七、贡献者</h2><p>感谢所有为本仓库提交过代码与改进建议的贡献者。</p><a href="https://github.com/Yin-Hongwei/music-website/graphs/contributors">  <img src="https://contrib.rocks/image?repo=Yin-Hongwei/music-website" alt="Contributors" /></a><br/><br/><h2 id="八、最后"><a href="#八、最后" class="headerlink" title="八、最后"></a>八、最后</h2><p>希望本文对大家有所启发，如有问题欢迎交流。</p>]]></content>
      
      
      
        <tags>
            
            <tag> Vue </tag>
            
            <tag> SpringBoot </tag>
            
            <tag> MyBatis </tag>
            
            <tag> 项目实战 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JavaScript 正则表达式</title>
      <link href="/2018/10/15/JavaScript%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/"/>
      <url>/2018/10/15/JavaScript%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
      
        <content type="html"><![CDATA[<h2 id="一、概念"><a href="#一、概念" class="headerlink" title="一、概念"></a>一、概念</h2><p>正则表达式是用于匹配字符串中字符组合的模式。</p><span id="more"></span><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> expression = <span class="regexp">/ pattern /</span> flag;</span><br><span class="line"></span><br><span class="line"><span class="comment">// pattern：一个字符串，指定了正则表达式的模式或其他正则表达式。</span></span><br><span class="line"><span class="comment">// flag：1）g：全局模式；2）i：不区分大小写模式；3）m：多行模式</span></span><br></pre></td></tr></table></figure><h2 id="二、种类"><a href="#二、种类" class="headerlink" title="二、种类"></a>二、种类</h2><h3 id="1、简单字符"><a href="#1、简单字符" class="headerlink" title="1、简单字符"></a>1、简单字符</h3><p>字符本身</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">/hello/g</span><br></pre></td></tr></table></figure><h3 id="2、方括号"><a href="#2、方括号" class="headerlink" title="2、方括号"></a>2、方括号</h3><p>用<code>[]</code>指代一个字符，里面的字符表示这个字符的选择范围</p><p>1）范围类</p><p><code>-</code>表示一个范围内的字符</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[<span class="number">0</span>-<span class="number">9</span>] &lt;=&gt; [<span class="number">0123456789</span>]</span><br></pre></td></tr></table></figure><p>2）组合类</p><p>范围类的组合</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">[<span class="number">0</span>-9a-fA-F] <span class="comment">// 十六进制 </span></span><br><span class="line"><span class="comment">// 组合之间不能用空格</span></span><br></pre></td></tr></table></figure><p>3）负向类</p><p>表示不在方括号之间的任何字符</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[^<span class="number">0</span>-<span class="number">9</span>]</span><br></pre></td></tr></table></figure><h3 id="3、量词"><a href="#3、量词" class="headerlink" title="3、量词"></a>3、量词</h3><p>表示前面字符出现的次数。</p><p><strong>贪婪量词</strong>：找到匹配的最大串，找到最后才停止</p><p><strong>惰性量词</strong>：找到匹配的最小串，找到就停止</p><table><thead><tr><th align="center">贪婪量词</th><th align="center">惰性量词</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center"><code>{0,1}</code> 或 <code>?</code></td><td align="center"><code>??</code></td><td align="center">至少 0 次，至多 1 次</td></tr><tr><td align="center"><code>{0,}</code> 或 <code>*</code></td><td align="center"><code>*?</code></td><td align="center">至少 0 次</td></tr><tr><td align="center"><code>{1,}</code> 或 <code>+</code></td><td align="center"><code>+?</code></td><td align="center">至少 1 次</td></tr><tr><td align="center"><code>{n}</code></td><td align="center"><code>{n}?</code></td><td align="center">出现 n 次</td></tr><tr><td align="center"><code>{n,m}</code></td><td align="center"><code>{n,m}?</code></td><td align="center">至少 n 次，至多 m 次</td></tr><tr><td align="center"><code>{n,}</code></td><td align="center"><code>{n,}?</code></td><td align="center">至少 n 次，至多不限</td></tr></tbody></table><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">/[<span class="number">0</span>-<span class="number">9</span>]&#123;<span class="number">4</span>&#125;/g</span><br></pre></td></tr></table></figure><h3 id="4、通配符"><a href="#4、通配符" class="headerlink" title="4、通配符"></a>4、通配符</h3><table><thead><tr><th align="center">类别</th><th align="left">描述</th></tr></thead><tbody><tr><td align="center"><code>.</code></td><td align="left"><code>[^\n\r]</code>，除了回车换行以外，都可以匹配</td></tr><tr><td align="center"><code>\d</code></td><td align="left"><code>[0-9]</code>，数字</td></tr><tr><td align="center"><code>\D</code></td><td align="left"><code>[^0-9]</code>，非数字</td></tr><tr><td align="center"><code>\w</code></td><td align="left"><code>[a-zA-Z_0-9]</code>，数字字母下划线</td></tr><tr><td align="center"><code>\W</code></td><td align="left"><code>[^a-zA-Z_0-9]</code>，非数字字母下划线</td></tr><tr><td align="center"><code>\s</code></td><td align="left"><code>[\t\n\x0B\f\r]</code>，空格或者空白</td></tr><tr><td align="center"><code>\S</code></td><td align="left">非空格或者非空白</td></tr><tr><td align="center"><code>\b</code></td><td align="left">表示边界（两个字符中间的位置）一边是 <code>\w</code>，另一边是 <code>\W</code></td></tr><tr><td align="center"><code>\B</code></td><td align="left">不是边界</td></tr><tr><td align="center"><code>^</code></td><td align="left">表示开始</td></tr><tr><td align="center"><code>$</code></td><td align="left">表示结束</td></tr></tbody></table><h3 id="5、分组"><a href="#5、分组" class="headerlink" title="5、分组"></a>5、分组</h3><p>一个正则表达式，不但可以对整个匹配进行操作，还可以对其中<code>()</code>中的子串进行匹配，即分组。</p><p>1）(pattern) 匹配 pattern 同时捕获结果，自动设定好组。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">\<span class="number">1</span></span><br><span class="line"><span class="title class_">RegExp</span>.<span class="property">$1</span> <span class="comment">// 反向引用</span></span><br></pre></td></tr></table></figure><p>2）(?&lt;name&gt;pattern) 匹配 pattern 同时捕获结果，设定 name 为组名。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line">\k\&lt;name&gt; <span class="comment">// 反向引用</span></span><br></pre></td></tr></table></figure><p>例：</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;word excel excel hello world world!&#x27;</span>;</span><br><span class="line"></span><br><span class="line">str.<span class="title function_">match</span>(<span class="regexp">/(\b[a-zA-Z]+\b)\s+\1/g</span>); <span class="comment">// [&quot;excel excel&quot;, &quot;world world&quot;]</span></span><br><span class="line"></span><br><span class="line">str.<span class="title function_">match</span>(<span class="regexp">/(?&lt;n1&gt;\b[a-zA-Z]+\b)\s+\k&lt;n1&gt;/</span>);</span><br><span class="line"></span><br><span class="line"><span class="title class_">RegExp</span>.<span class="property">$1</span>; <span class="comment">// &quot;excel&quot;</span></span><br></pre></td></tr></table></figure><h3 id="6、零宽（负向）先行-后行断言"><a href="#6、零宽（负向）先行-后行断言" class="headerlink" title="6、零宽（负向）先行\后行断言"></a>6、零宽（负向）先行\后行断言</h3><p>开头是：(?&lt;&#x3D;什么)  开头不是：(?&lt;!&#x3D;什么)</p><p>结尾是：(?&#x3D;什么)    结尾不是：(?!&#x3D;什么)</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">str = <span class="string">&#x27;r;e;a;a;a;rcaaa=bbb=;&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 寻找两个字符，两字符后面以分号结尾</span></span><br><span class="line">str.<span class="title function_">match</span>(<span class="regexp">/.&#123;2&#125;(?=;)/g</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 寻找四个字符，以ea开头</span></span><br><span class="line">str.<span class="title function_">match</span>(<span class="regexp">/(?&lt;=ea).&#123;4&#125;/g</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 寻找三个字母，不以分号结尾 </span></span><br><span class="line">str.<span class="title function_">match</span>(<span class="regexp">/[a-z]&#123;3&#125;(?!;)/g</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 寻找不易re开头的三个字母</span></span><br><span class="line">str.<span class="title function_">match</span>(<span class="regexp">/(?&lt;!re)a&#123;3&#125;/</span>);</span><br></pre></td></tr></table></figure><h3 id="7、-（或）"><a href="#7、-（或）" class="headerlink" title="7、|（或）"></a>7、|（或）</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">str = <span class="string">&#x27;var    aaadf1;&#x27;</span>;</span><br><span class="line">str2 = <span class="string">&#x27;function    f()&#123;&#125;&#x27;</span>;</span><br><span class="line">str.<span class="title function_">match</span>(<span class="regexp">/^var\s+[a-zA-Z]\w*;|^function\s+[a-zA-Z]\w*\s*\(\)\&#123;\&#125;/</span>);</span><br></pre></td></tr></table></figure><h2 id="三、创建方式"><a href="#三、创建方式" class="headerlink" title="三、创建方式"></a>三、创建方式</h2><h3 id="1、正则表达式字面量"><a href="#1、正则表达式字面量" class="headerlink" title="1、正则表达式字面量"></a>1、正则表达式字面量</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> pattern = <span class="regexp">/[bc]at/i</span>;</span><br></pre></td></tr></table></figure><h3 id="2、正则表达式类-RegExp"><a href="#2、正则表达式类-RegExp" class="headerlink" title="2、正则表达式类 RegExp"></a>2、正则表达式类 RegExp</h3><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> pattern = <span class="keyword">new</span> <span class="title class_">RegExp</span>(<span class="string">&quot;[bc]at&quot;</span>, <span class="string">&quot;i&quot;</span>);</span><br><span class="line"><span class="comment">// 注意双重转义</span></span><br></pre></td></tr></table></figure><p>正则表达式字面量始终共享同一个 RegExp 实例，构造函数创建的每一个新的 RegExp 实例都是一个新实例。</p><p>RegExp 构造函数上有一些属性，并且这些属性跟着执行的最后一次正则表达式操作而变化。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> text = <span class="string">&#x27;this has been a short summer&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> pattern = <span class="regexp">/(.)hort/g</span>;</span><br><span class="line"><span class="keyword">if</span> (pattern.<span class="title function_">test</span>(text)) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">RegExp</span>.<span class="property">input</span>); <span class="comment">// this has been a short summer</span></span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">RegExp</span>.<span class="property">leftContext</span>); <span class="comment">// this has been a </span></span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">RegExp</span>.<span class="property">rightContext</span>); <span class="comment">//  summer</span></span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">RegExp</span>.<span class="property">lastMatch</span>); <span class="comment">// short</span></span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">RegExp</span>.<span class="property">lastParen</span>); <span class="comment">// s</span></span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">RegExp</span>.<span class="property">multiline</span>); <span class="comment">// false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>RegExp.$1,…RegExp.$9，存储捕获的组。</p><h4 id="1）RegExp-实例属性"><a href="#1）RegExp-实例属性" class="headerlink" title="1）RegExp 实例属性"></a>1）RegExp 实例属性</h4><p>通过 global、ignoreCase、multiline、lastIndex、source 这几个属性获取一个正则表达式相关的信息。</p><h2 id="四、使用"><a href="#四、使用" class="headerlink" title="四、使用"></a>四、使用</h2><h3 id="1、RegExp-实例方法"><a href="#1、RegExp-实例方法" class="headerlink" title="1、RegExp 实例方法"></a>1、RegExp 实例方法</h3><h4 id="1）test-string"><a href="#1）test-string" class="headerlink" title="1）test(string)"></a>1）test(string)</h4><p>表示当前正则是否匹配指定的字符串，返回布尔值。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> regExp = <span class="keyword">new</span> <span class="title class_">RegExp</span>(<span class="string">&#x27;[0-9]+&#x27;</span>,<span class="string">&#x27;g&#x27;</span>);  </span><br><span class="line"><span class="comment">// 或</span></span><br><span class="line"><span class="keyword">var</span> regExp2 = <span class="regexp">/[0-9]+/g</span>; <span class="comment">// 有g，匹配所有的字符串；没有g，匹配完第一个然后停止。</span></span><br><span class="line"><span class="keyword">var</span> str = <span class="number">123</span>;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(regExp2.<span class="title function_">test</span>(str)); <span class="comment">//true</span></span><br></pre></td></tr></table></figure><p>在指向知道目标字符串和某个模式是否匹配，但不需要知道其文本内容的情况下使用。</p><h4 id="2）exce-string"><a href="#2）exce-string" class="headerlink" title="2）exce(string)"></a>2）exce(string)</h4><p>表示当前正则是否匹配指定的字符串，返回匹配的数组（找不到返回null）</p><blockquote><p>备注：该方法是为捕获组而专门设计的，该方法接收应用模式的字符串，返回一个数组且额外带有index，input属性。index 表示匹配项在字符串中的位置；input 是应用正则表达式的字符串。且<strong>数组第一项</strong>是和模式匹配的整个字符串；<strong>其他项</strong>是和模式中捕获的组匹配的字符串。</p></blockquote><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> test = <span class="string">&quot;mom and dad and baby&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> pattern = <span class="regexp">/mom( and dad( and baby)?)?/gi</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> matches = pattern.<span class="title function_">exec</span>(test);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches[<span class="number">0</span>]) <span class="comment">// &quot;mom and dad and baby&quot;</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches[<span class="number">1</span>]) <span class="comment">// &quot; and dad and baby&quot;</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches[<span class="number">2</span>]) <span class="comment">// &quot; and baby&quot;</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches.<span class="property">index</span>) <span class="comment">// 0</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches.<span class="property">input</span>) <span class="comment">// &quot;mom and dad and baby&quot;</span></span><br></pre></td></tr></table></figure><ul><li><p>模式中设置 g：在同一字符串上多次调用 exec() 始终返回第一个匹配项的信息。</p></li><li><p>模式中不设置 g：在同一字符串上多次调用 exec() 会在字符串中继续查找新的匹配项</p></li></ul><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> text = <span class="string">&#x27;cat, bat, sat, fat&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> pattern1 = <span class="regexp">/.at/</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> matches = pattern1.<span class="title function_">exec</span>(text);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches.<span class="property">index</span>); <span class="comment">// 0</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches[<span class="number">0</span>]); <span class="comment">//cat</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(pattern1.<span class="property">lastIndex</span>); <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line">matches = pattern1.<span class="title function_">exec</span>(text); </span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches.<span class="property">index</span>); <span class="comment">// 0</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches[<span class="number">0</span>]); <span class="comment">//cat</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(pattern1.<span class="property">lastIndex</span>); <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> pattern2 = <span class="regexp">/.at/gi</span></span><br><span class="line"><span class="keyword">var</span> matches1 = pattern2.<span class="title function_">exec</span>(text); </span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches1.<span class="property">index</span>); <span class="comment">// 0</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(matches1[<span class="number">0</span>]); <span class="comment">//cat</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(pattern2.<span class="property">lastIndex</span>); <span class="comment">// 3</span></span><br><span class="line"><span class="comment">// 可被用于循环匹配统计字符串中字符出现的次数</span></span><br></pre></td></tr></table></figure><h3 id="2、String-支持的正则函数"><a href="#2、String-支持的正则函数" class="headerlink" title="2、String 支持的正则函数"></a>2、String 支持的正则函数</h3><h4 id="1）match-regexp-：返回数组"><a href="#1）match-regexp-：返回数组" class="headerlink" title="1）match(regexp)：返回数组"></a>1）match(regexp)：返回数组</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">str.<span class="title function_">match</span>(<span class="regexp">/[0-9]+/g</span>);</span><br></pre></td></tr></table></figure><p>没有g，第一个匹配的最大串，其他是分组的子串，index:最大串开始的位置， input原始的串</p><p>有g，返回数组，所有匹配的最大串，但是没有分组的子串，index和input无效。</p><h4 id="2）replace-regexp-substr-replacement-：返回字符串"><a href="#2）replace-regexp-substr-replacement-：返回字符串" class="headerlink" title="2）replace(regexp&#x2F;substr, replacement)：返回字符串"></a>2）replace(regexp&#x2F;substr, replacement)：返回字符串</h4><p>regexp 表示正则表达式。</p><p>substr表示子串。</p><p>replacement 表示替换进去的串，或者<strong>函数</strong>。$1 ~ $99 分组，$&amp;代表子串； $&#96; 子串左侧； $‘ 子串右侧。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 把小写的 word 变成大写的 WORD</span></span><br><span class="line">str = <span class="string">&#x27;word&#x27;</span></span><br><span class="line">str.<span class="title function_">replace</span>(<span class="regexp">/\bword\b/</span>, <span class="string">&#x27;WORD&#x27;</span>);</span><br><span class="line">str.<span class="title function_">replace</span>(<span class="regexp">/\bword\b/g</span>, <span class="string">&#x27;WORD&#x27;</span>);</span><br><span class="line">str.<span class="title function_">replace</span>(<span class="string">&#x27;word&#x27;</span>, <span class="string">&#x27;WORD&#x27;</span>);</span><br></pre></td></tr></table></figure><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 匹配 单词1-单词2 模式，然后变成单词2-单词1</span></span><br><span class="line">str = <span class="string">&#x27;aaaa-bbbb@cc-ddd&#x27;</span>;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(str.<span class="title function_">replace</span>(<span class="regexp">/([a-z]+)-([a-z]+)/g</span>, replacer));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数的第一项是匹配的内容。</span></span><br><span class="line"><span class="comment">// 接下来的参数是与模式中的子表达式匹配的字符串，可以有 0 个或多个这样的参数。</span></span><br><span class="line"><span class="comment">// 接下来的参数是一个整数，声明了匹配在 stringObject 中出现的位置。</span></span><br><span class="line"><span class="comment">// 最后一个参数是 stringObject 本身。</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">replacer</span>(<span class="params">match, p1, p2, offset, str</span>)&#123;</span><br><span class="line">    <span class="comment">//console.log(match, p1, p2, offset, str);</span></span><br><span class="line">    <span class="keyword">var</span> sRet = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; p2.<span class="property">length</span>; i++)&#123;</span><br><span class="line">        sRet += <span class="title class_">String</span>.<span class="title function_">fromCharCode</span>(p2.<span class="title function_">charCodeAt</span>(i) + <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    sRet += <span class="string">&#x27;=&#x27;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; p1.<span class="property">length</span>; i++)&#123;</span><br><span class="line">        sRet += <span class="title class_">String</span>.<span class="title function_">fromCharCode</span>(p1.<span class="title function_">charCodeAt</span>(i) + <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sRet;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="comment">// aaaa-bbbb aaaa bbbb 0 aaaa-bbbb@cc-ddd</span></span><br><span class="line"><span class="comment">// cc-ddd cc ddd 10 aaaa-bbbb@cc-ddd</span></span><br><span class="line"><span class="comment">// cccc=bbbb@eee=dd</span></span><br></pre></td></tr></table></figure><h4 id="3）search-regExp"><a href="#3）search-regExp" class="headerlink" title="3）search(regExp)"></a>3）search(regExp)</h4><p>直接量和对象，返回整数，开始的位置，-1表示没有找到。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">str = <span class="string">&#x27;I word site excel word OK chrome match OK excel word&#x27;</span>;</span><br><span class="line"><span class="comment">//找word</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(str.<span class="title function_">search</span>(<span class="regexp">/\bword\b/</span>));</span><br></pre></td></tr></table></figure><blockquote><p>注意：search 不支持全局，忽略参数 g，同时忽略 regexp 的 lastIndex</p></blockquote><h4 id="4）split-separator-howmany"><a href="#4）split-separator-howmany" class="headerlink" title="4）split(separator, howmany)"></a>4）split(separator, howmany)</h4><p>把一个字符串分隔成数组。separator 正则，字符串，可选；howmany 是最大长度。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var str = &#x27;a,b.c&#x27;</span><br><span class="line">str.split(/,|\./) // [a, b, c]</span><br></pre></td></tr></table></figure><h2 id="五、练习"><a href="#五、练习" class="headerlink" title="五、练习"></a>五、练习</h2><h4 id="1、如何将浮点数点左边的数每三位添加一个逗号，如-12005009-11-转化为-12-005-009-11"><a href="#1、如何将浮点数点左边的数每三位添加一个逗号，如-12005009-11-转化为-12-005-009-11" class="headerlink" title="1、如何将浮点数点左边的数每三位添加一个逗号，如 12005009.11 转化为 12,005,009.11"></a>1、如何将浮点数点左边的数每三位添加一个逗号，如 12005009.11 转化为 12,005,009.11</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">commafy</span>(<span class="params">num</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    num &amp;&amp; num.<span class="title function_">toString</span>().<span class="title function_">replace</span>(<span class="regexp">/(\d)(?=(\d&#123;3&#125;)+\.)/g</span>, <span class="keyword">function</span> (<span class="params">match, $1</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> $1 + <span class="string">&quot;,&quot;</span>;</span><br><span class="line">    &#125;)</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      
        <tags>
            
            <tag> JavaScript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JavaScript Event Loop</title>
      <link href="/2018/08/17/JavaScript%20Event%20Loop/"/>
      <url>/2018/08/17/JavaScript%20Event%20Loop/</url>
      
        <content type="html"><![CDATA[<p>JavaScript 是单线程语言，异步能力依赖 Event Loop 调度宏任务与微任务。理解这套机制，是读懂 <code>setTimeout</code>、<code>Promise</code>、<code>async/await</code> 等 API 执行顺序的基础。</p><span id="more"></span><p>本文先建立 Event Loop 的基本概念，再以浏览器环境为主线讲解运行机制与示例，最后补充 Node.js 中的差异。</p><h2 id="一、什么是-Event-Loop"><a href="#一、什么是-Event-Loop" class="headerlink" title="一、什么是 Event Loop"></a>一、什么是 Event Loop</h2><p><strong>Event Loop（事件循环）</strong> 是 JavaScript 运行时用来协调 <strong>同步代码</strong>、<strong>异步回调</strong> 和 <strong>调用栈</strong> 的调度机制。</p><p>可以把它理解为一个持续运转的循环，不断重复以下判断：</p><ol><li>调用栈里还有代码在执行吗？有，就继续跑同步代码</li><li>调用栈空了，任务队列里有待执行的回调吗？有，就取出一个放入调用栈执行</li><li>都没有，就等待新的任务入队</li></ol><p>以上是简化模型，便于建立直觉。浏览器中会把「任务队列」进一步拆成 <strong>宏任务</strong> 与 <strong>微任务</strong> 两类，调度规则见下一节。</p><p>JavaScript <strong>引擎</strong>只负责执行同步代码；<code>setTimeout</code>、网络 I&#x2F;O 等异步工作由<strong>宿主环境</strong>（浏览器或 Node.js）完成，并把回调放进任务队列。Event Loop 的作用，就是把引擎和宿主环境衔接起来——让异步回调在合适的时机回到主线程执行。</p><h2 id="二、浏览器中的-Event-Loop"><a href="#二、浏览器中的-Event-Loop" class="headerlink" title="二、浏览器中的 Event Loop"></a>二、浏览器中的 Event Loop</h2><p>以下以前端（浏览器）环境为主讲解。Node.js 的差异见第三节。</p><h3 id="单线程模型"><a href="#单线程模型" class="headerlink" title="单线程模型"></a>单线程模型</h3><p>JavaScript 最初被设计为浏览器脚本语言，需要直接操作 DOM。若允许多线程同时读写同一节点，浏览器将无法确定以哪个线程的结果为准。</p><p>因此，主线程同一时刻只执行一段同步代码。耗时或异步工作交给浏览器完成，回调先入队等待；当前同步代码跑完、调用栈清空后，Event Loop 再按规则取出回调继续执行。</p><h3 id="运行机制"><a href="#运行机制" class="headerlink" title="运行机制"></a>运行机制</h3><p>浏览器中，每轮事件循环大致遵循以下步骤：</p><ol><li>从 <strong>宏任务队列</strong> 取出一个任务执行（首次加载时，<code>&lt;script&gt;</code> 整体就是一个宏任务）</li><li>执行过程中，同步代码压入 <strong>调用栈</strong> 依次运行；遇到异步 API，将回调注册到对应队列</li><li>当前宏任务的同步代码执行完毕后，清空 <strong>微任务队列</strong>（可能连续执行多轮，直到队列为空）</li><li>必要时进行 UI 渲染</li><li>回到第 1 步，处理下一个宏任务</li></ol><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">┌───────────────────────────┐</span><br><span class="line">│  取宏任务（macrotask）     │</span><br><span class="line">└─────────────┬─────────────┘</span><br><span class="line">              ▼</span><br><span class="line">┌───────────────────────────┐</span><br><span class="line">│  执行同步代码（调用栈）     │</span><br><span class="line">└─────────────┬─────────────┘</span><br><span class="line">              ▼</span><br><span class="line">┌───────────────────────────┐</span><br><span class="line">│  清空微任务队列            │</span><br><span class="line">│  （microtask）             │</span><br><span class="line">└─────────────┬─────────────┘</span><br><span class="line">              ▼</span><br><span class="line">┌───────────────────────────┐</span><br><span class="line">│  UI 渲染（可选）           │</span><br><span class="line">└─────────────┬─────────────┘</span><br><span class="line">              ▼</span><br><span class="line">         下一轮循环</span><br></pre></td></tr></table></figure><p><strong>核心规则：每轮先跑完当前宏任务的同步代码，再清空全部微任务，然后才进入下一个宏任务。</strong></p><h3 id="调用栈与任务队列"><a href="#调用栈与任务队列" class="headerlink" title="调用栈与任务队列"></a>调用栈与任务队列</h3><p><strong>调用栈（Call Stack）</strong> 记录当前正在执行的函数：进入函数时入栈，返回时出栈。同一时刻只有一个调用栈，保证同步代码按顺序执行。</p><p><strong>任务队列</strong> 存放等待执行的回调。宏任务队列和微任务队列都是 FIFO（先进先出）。不是调用栈去”读取”队列，而是 Event Loop 在调用栈清空后，从队列中取出任务放入调用栈执行。</p><h3 id="宏任务与微任务"><a href="#宏任务与微任务" class="headerlink" title="宏任务与微任务"></a>宏任务与微任务</h3><table><thead><tr><th>类型</th><th>发起方</th><th>每轮处理</th><th>浏览器常见来源</th></tr></thead><tbody><tr><td>宏任务</td><td>宿主环境</td><td>取一个</td><td><code>script</code>、<code>setTimeout</code>、<code>setInterval</code>、I&#x2F;O、<code>MessageChannel</code>、<code>postMessage</code></td></tr><tr><td>微任务</td><td>JS 引擎</td><td>清空全部</td><td><code>Promise.then/catch/finally</code>、<code>MutationObserver</code>、<code>queueMicrotask</code></td></tr></tbody></table><p>几条容易混淆的结论：</p><ul><li><code>setTimeout</code> &#x2F; <code>setInterval</code> 的<strong>回调</strong>是宏任务；延迟时间只保证”最早何时进入队列”，不保证精确执行时刻</li><li><code>Promise</code> 构造函数中的代码<strong>同步执行</strong>；<code>.then</code> &#x2F; <code>.catch</code> &#x2F; <code>.finally</code> 的回调才是微任务</li><li>微任务执行过程中产生的新微任务，会在当前轮继续清空，不会等到下一轮</li><li><code>requestAnimationFrame</code> 回调在<strong>下一帧渲染前</strong>执行，通常排在当前微任务清空之后；不要把它和微任务混为一谈</li></ul><h3 id="执行顺序示例"><a href="#执行顺序示例" class="headerlink" title="执行顺序示例"></a>执行顺序示例</h3><h4 id="同步代码先于宏任务回调"><a href="#同步代码先于宏任务回调" class="headerlink" title="同步代码先于宏任务回调"></a>同步代码先于宏任务回调</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;script start&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;setTimeout&#x27;</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;script end&#x27;</span>)</span><br></pre></td></tr></table></figure><p>输出：<code>script start</code> → <code>script end</code> → <code>setTimeout</code></p><p>整段 <code>&lt;script&gt;</code> 作为第一个宏任务：<code>setTimeout</code> 只负责把回调注册到宏任务队列，同步代码全部跑完后，才取出该回调执行。</p><h4 id="微任务先于下一个宏任务"><a href="#微任务先于下一个宏任务" class="headerlink" title="微任务先于下一个宏任务"></a>微任务先于下一个宏任务</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;script start&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="keyword">function</span> (<span class="params">resolve</span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;promise1&#x27;</span>)       <span class="comment">// 同步</span></span><br><span class="line">  <span class="title function_">resolve</span>()</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;promise1 end&#x27;</span>)   <span class="comment">// 同步</span></span><br><span class="line">&#125;).<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;promise2&#x27;</span>)       <span class="comment">// 微任务</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;setTimeout&#x27;</span>)     <span class="comment">// 宏任务</span></span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;script end&#x27;</span>)</span><br></pre></td></tr></table></figure><p>输出：<code>script start</code> → <code>promise1</code> → <code>promise1 end</code> → <code>script end</code> → <code>promise2</code> → <code>setTimeout</code></p><p>当前宏任务的同步代码结束后，先清空微任务（<code>promise2</code>），再进入下一个宏任务（<code>setTimeout</code>）。</p><h4 id="微任务链在当前轮清空"><a href="#微任务链在当前轮清空" class="headerlink" title="微任务链在当前轮清空"></a>微任务链在当前轮清空</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;start&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;timeout&#x27;</span>), <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="title class_">Promise</span>.<span class="title function_">resolve</span>()</span><br><span class="line">  .<span class="title function_">then</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;promise1&#x27;</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Promise</span>.<span class="title function_">resolve</span>()</span><br><span class="line">  &#125;)</span><br><span class="line">  .<span class="title function_">then</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;promise2&#x27;</span>))</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;end&#x27;</span>)</span><br></pre></td></tr></table></figure><p>输出：<code>start</code> → <code>end</code> → <code>promise1</code> → <code>promise2</code> → <code>timeout</code></p><p><code>promise1</code> 执行时注册的 <code>promise2</code> 仍是微任务，会在当前轮微任务清空阶段继续执行，不会等到 <code>setTimeout</code>。</p><h3 id="async-await"><a href="#async-await" class="headerlink" title="async&#x2F;await"></a>async&#x2F;await</h3><p><code>async/await</code> 是 Promise 的语法糖。理解它在 Event Loop 中的行为，需要把它还原成「同步代码 + Promise 微任务」。</p><h4 id="await-的执行时机"><a href="#await-的执行时机" class="headerlink" title="await 的执行时机"></a>await 的执行时机</h4><p>常见误解：<code>await</code> 会阻塞线程，等右侧表达式完全结束后再继续。</p><p>实际上：</p><ol><li><code>await</code> 右侧表达式会<strong>先同步求值</strong></li><li>若结果是 Promise，则 <code>await</code> 之后的代码相当于注册到该 Promise 的 <code>then</code> 中，即<strong>微任务</strong></li><li><code>async</code> 函数在第一个 <code>await</code> 处<strong>让出执行权</strong>，外层同步代码继续运行</li></ol><p>因此，<code>await</code> 不会阻塞主线程，只是把后续代码推迟到微任务阶段。</p><h4 id="综合示例"><a href="#综合示例" class="headerlink" title="综合示例"></a>综合示例</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">async1</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;async1 start&#x27;</span>)</span><br><span class="line">  <span class="keyword">await</span> <span class="title function_">async2</span>()</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;async1 end&#x27;</span>)   <span class="comment">// 微任务</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">async2</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;async2&#x27;</span>)        <span class="comment">// 同步（await 右侧先执行）</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;script start&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;setTimeout&#x27;</span>)</span><br><span class="line">&#125;, <span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="title function_">async1</span>()</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;promise1&#x27;</span>)</span><br><span class="line">  <span class="title function_">resolve</span>()</span><br><span class="line">&#125;).<span class="title function_">then</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;promise2&#x27;</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;script end&#x27;</span>)</span><br></pre></td></tr></table></figure><p>输出：<code>script start</code> → <code>async1 start</code> → <code>async2</code> → <code>promise1</code> → <code>script end</code> → <code>async1 end</code> → <code>promise2</code> → <code>setTimeout</code></p><p>执行过程：</p><ol><li>同步：<code>script start</code> → 进入 <code>async1</code> → <code>async1 start</code> → 调用 <code>async2</code> → <code>async2</code></li><li><code>await async2()</code> 返回已 resolve 的 Promise，将 <code>async1 end</code> 入微任务队列，<code>async1</code> 让出</li><li>同步：<code>promise1</code> → <code>script end</code></li><li>清空微任务：先 <code>async1 end</code>（await 续体），再 <code>promise2</code></li><li>宏任务：<code>setTimeout</code></li></ol><h4 id="与-Promise-的对应关系"><a href="#与-Promise-的对应关系" class="headerlink" title="与 Promise 的对应关系"></a>与 Promise 的对应关系</h4><p>可以把 <code>async/await</code> 理解为自动展开的 Promise 链：</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// async/await 写法</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">foo</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> result = <span class="keyword">await</span> <span class="title function_">bar</span>()</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(result)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等价的 Promise 写法</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">foo</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="title function_">bar</span>().<span class="title function_">then</span>(<span class="function"><span class="params">result</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(result)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>await</code> 之后的代码 &#x3D; <code>.then</code> 回调 &#x3D; 微任务，按 FIFO 与同轮其他微任务依次执行。</p><h2 id="三、Node-js-中的-Event-Loop"><a href="#三、Node-js-中的-Event-Loop" class="headerlink" title="三、Node.js 中的 Event Loop"></a>三、Node.js 中的 Event Loop</h2><p>浏览器与 Node.js 都遵循「宏任务 → 微任务」的大框架，但 Node.js 额外引入了 <code>process.nextTick</code> 和 <code>setImmediate</code>，优先级规则与浏览器不同。</p><h3 id="与浏览器的差异"><a href="#与浏览器的差异" class="headerlink" title="与浏览器的差异"></a>与浏览器的差异</h3><table><thead><tr><th>特性</th><th>浏览器</th><th>Node.js</th></tr></thead><tbody><tr><td>宏任务</td><td><code>script</code>、<code>setTimeout</code>、<code>setInterval</code>、I&#x2F;O 等</td><td>上述 + <code>setImmediate</code>、I&#x2F;O 回调</td></tr><tr><td>微任务</td><td><code>Promise.then</code>、<code>queueMicrotask</code> 等</td><td>同上</td></tr><tr><td>nextTick 队列</td><td>无</td><td><code>process.nextTick</code>（不属于微任务规范，但优先级高于 Promise 微任务）</td></tr></tbody></table><p>Node.js 中每轮循环的简化顺序：</p><ol><li>执行一个宏任务（timers &#x2F; I&#x2F;O &#x2F; check 等阶段）</li><li>清空 <code>process.nextTick</code> 队列</li><li>清空 Promise 等微任务队列</li><li>进入下一轮</li></ol><blockquote><p><code>process.nextTick</code> 不属于微任务规范，但行为上在每轮宏任务之后、Promise 微任务之前执行，优先级更高。</p></blockquote><h3 id="执行顺序示例-1"><a href="#执行顺序示例-1" class="headerlink" title="执行顺序示例"></a>执行顺序示例</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;1&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;2&#x27;</span>)</span><br><span class="line">  process.<span class="title function_">nextTick</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;3&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="keyword">function</span> (<span class="params">resolve</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;4&#x27;</span>)</span><br><span class="line">    <span class="title function_">resolve</span>()</span><br><span class="line">  &#125;).<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;5&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">process.<span class="title function_">nextTick</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;6&#x27;</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="keyword">function</span> (<span class="params">resolve</span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;7&#x27;</span>)</span><br><span class="line">  <span class="title function_">resolve</span>()</span><br><span class="line">&#125;).<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;8&#x27;</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;9&#x27;</span>)</span><br><span class="line">  process.<span class="title function_">nextTick</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;10&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  <span class="keyword">new</span> <span class="title class_">Promise</span>(<span class="keyword">function</span> (<span class="params">resolve</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;11&#x27;</span>)</span><br><span class="line">    <span class="title function_">resolve</span>()</span><br><span class="line">  &#125;).<span class="title function_">then</span>(<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;12&#x27;</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>输出：<code>1</code> → <code>7</code> → <code>6</code> → <code>8</code> → <code>2</code> → <code>4</code> → <code>3</code> → <code>5</code> → <code>9</code> → <code>11</code> → <code>10</code> → <code>12</code></p><table><thead><tr><th>阶段</th><th>动作</th><th>输出</th></tr></thead><tbody><tr><td>同步</td><td>主脚本</td><td><code>1</code>、<code>7</code></td></tr><tr><td>nextTick</td><td>清空 nextTick 队列</td><td><code>6</code></td></tr><tr><td>微任务</td><td>清空 Promise 队列</td><td><code>8</code></td></tr><tr><td>宏任务 1</td><td>第一个 <code>setTimeout</code> 回调</td><td><code>2</code>、<code>4</code>（Promise 构造函数同步）</td></tr><tr><td>nextTick</td><td>该宏任务产生的 nextTick</td><td><code>3</code></td></tr><tr><td>微任务</td><td>该宏任务产生的 Promise</td><td><code>5</code></td></tr><tr><td>宏任务 2</td><td>第二个 <code>setTimeout</code> 回调</td><td><code>9</code>、<code>11</code></td></tr><tr><td>nextTick</td><td></td><td><code>10</code></td></tr><tr><td>微任务</td><td></td><td><code>12</code></td></tr></tbody></table><h3 id="process-nextTick-使用注意"><a href="#process-nextTick-使用注意" class="headerlink" title="process.nextTick 使用注意"></a>process.nextTick 使用注意</h3><p><code>process.nextTick</code> 回调若递归注册自身，会饿死 I&#x2F;O 和宏任务——因为 nextTick 队列在当前轮永远优先清空。</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">spin</span>(<span class="params"></span>) &#123;</span><br><span class="line">  process.<span class="title function_">nextTick</span>(spin)  <span class="comment">// 危险：阻塞事件循环</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>适合场景：在异步操作完成前、当前调用栈退出后立刻执行的小任务（如错误处理、状态通知）。不应替代 <code>setImmediate</code> 做大量或递归调度。</p><h3 id="setImmediate-与-setTimeout-0"><a href="#setImmediate-与-setTimeout-0" class="headerlink" title="setImmediate 与 setTimeout(0)"></a>setImmediate 与 setTimeout(0)</h3><p>两者都是宏任务，但入队时机不同。在 <strong>I&#x2F;O 回调内</strong>，<code>setImmediate</code> 通常先于 <code>setTimeout(fn, 0)</code> 执行：</p><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>)</span><br><span class="line"></span><br><span class="line">fs.<span class="title function_">readFile</span>(__filename, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;timeout&#x27;</span>), <span class="number">0</span>)</span><br><span class="line">  <span class="title function_">setImmediate</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;immediate&#x27;</span>))</span><br><span class="line">&#125;)</span><br><span class="line"><span class="comment">// 输出：immediate → timeout</span></span><br></pre></td></tr></table></figure><p>在<strong>主脚本顶层</strong>（无 I&#x2F;O 回调）同时注册时，先后顺序取决于当前事件循环状态，<strong>不能假定固定顺序</strong>。</p><p>Node.js 事件循环按阶段推进（timers → pending callbacks → idle&#x2F;prepare → poll → check → close callbacks），<code>setImmediate</code> 在 check 阶段执行，<code>setTimeout</code> 在 timers 阶段执行。日常开发记住「I&#x2F;O 回调里 immediate 更早」即可，细阶段可在需要时查阅 <a href="https://nodejs.org/api/timers.html">Node.js 官方文档</a>。</p><h2 id="四、速查"><a href="#四、速查" class="headerlink" title="四、速查"></a>四、速查</h2><h3 id="浏览器"><a href="#浏览器" class="headerlink" title="浏览器"></a>浏览器</h3><table><thead><tr><th>问题</th><th>答案</th></tr></thead><tbody><tr><td>Promise 构造函数里的代码何时执行？</td><td>立即、同步</td></tr><tr><td><code>.then</code> 何时执行？</td><td>当前宏任务同步代码结束后</td></tr><tr><td>微任务和宏任务谁先？</td><td>当前宏任务同步代码结束后，先清空微任务，再取下一个宏任务</td></tr><tr><td>微任务里再产生微任务怎么办？</td><td>仍在当前轮继续清空</td></tr><tr><td><code>setTimeout(fn, 0)</code> 会立刻执行吗？</td><td>不会，回调仍是宏任务，排在当前微任务之后</td></tr><tr><td><code>requestAnimationFrame</code> 是微任务吗？</td><td>不是，在下一帧渲染前执行，通常晚于当前微任务</td></tr></tbody></table><h3 id="async-await-1"><a href="#async-await-1" class="headerlink" title="async&#x2F;await"></a>async&#x2F;await</h3><table><thead><tr><th>问题</th><th>答案</th></tr></thead><tbody><tr><td><code>await</code> 会阻塞主线程吗？</td><td>不会，只让出 <code>async</code> 函数内的后续代码</td></tr><tr><td><code>await</code> 右侧何时执行？</td><td>同步求值，立即执行</td></tr><tr><td><code>await</code> 后面的代码何时执行？</td><td>作为微任务，在当前宏任务同步代码结束后</td></tr><tr><td><code>async</code> 函数返回值是什么？</td><td>始终是 Promise</td></tr></tbody></table><h3 id="Node-js"><a href="#Node-js" class="headerlink" title="Node.js"></a>Node.js</h3><table><thead><tr><th>问题</th><th>答案</th></tr></thead><tbody><tr><td><code>process.nextTick</code> 在哪生效？</td><td>仅 Node.js</td></tr><tr><td>与 Promise 微任务谁优先？</td><td><code>nextTick</code> 优先</td></tr><tr><td><code>setImmediate</code> 与 <code>setTimeout(0)</code> 区别？</td><td>I&#x2F;O 回调内 <code>setImmediate</code> 通常更早；顶层同时注册时顺序不确定</td></tr><tr><td>浏览器能用 <code>process.nextTick</code> 吗？</td><td>不能，用 <code>queueMicrotask</code> 代替</td></tr></tbody></table>]]></content>
      
      
      
        <tags>
            
            <tag> JavaScript, Node.js </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Vue 响应式原理</title>
      <link href="/2018/08/01/vue%E5%93%8D%E5%BA%94%E5%BC%8F%E5%8E%9F%E7%90%86/"/>
      <url>/2018/08/01/vue%E5%93%8D%E5%BA%94%E5%BC%8F%E5%8E%9F%E7%90%86/</url>
      
        <content type="html"><![CDATA[<h2 id="一、前言"><a href="#一、前言" class="headerlink" title="一、前言"></a>一、前言</h2><p>vue 响应式原理是数据渲染到视图，及数据修改触发视图更新的过程。</p><span id="more"></span><h2 id="二、原理"><a href="#二、原理" class="headerlink" title="二、原理"></a>二、原理</h2><p><img src="/2018/08/01/vue%E5%93%8D%E5%BA%94%E5%BC%8F%E5%8E%9F%E7%90%86/008i3skNly1gsvyh82ifyj30xc0kudhe.jpg"></p><h3 id="1、数据监听"><a href="#1、数据监听" class="headerlink" title="1、数据监听"></a>1、数据监听</h3><p>当把一个普通的 JavaScript 对象传入 Vue 实例作为 data 选项，Vue 将遍历此对象所有的属性，并使用 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Object/defineProperty"><code>Object.defineProperty</code></a> 为这些属性设置 <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Working_with_Objects#%E5%AE%9A%E4%B9%89_getters_%E4%B8%8E_setters">getter&#x2F;setter</a>。</p><p><strong>这些 getter&#x2F;setter 对用户来说不可见，但是在内部它们让 Vue 能够追踪依赖，在属性被访问和修改时通知变更。</strong></p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">defineReactive</span>(<span class="params">obj: <span class="built_in">Object</span>, key: string, ...</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> dep = <span class="keyword">new</span> <span class="title class_">Dep</span>()</span><br><span class="line"></span><br><span class="line">  <span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(obj, key, &#123;</span><br><span class="line">    <span class="attr">enumerable</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">configurable</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">get</span>: <span class="keyword">function</span> <span class="title function_">reactiveGetter</span> (<span class="params"></span>) &#123;</span><br><span class="line">      <span class="comment">// ....</span></span><br><span class="line">      dep.<span class="title function_">depend</span>()</span><br><span class="line">      <span class="keyword">return</span> value</span><br><span class="line">      <span class="comment">// ....</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">set</span>: <span class="keyword">function</span> <span class="title function_">reactiveSetter</span> (<span class="params">newVal</span>) &#123;</span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">      val = newVal</span><br><span class="line">      dep.<span class="title function_">notify</span>()</span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2、依赖收集"><a href="#2、依赖收集" class="headerlink" title="2、依赖收集"></a>2、依赖收集</h3><p>每个组件在渲染的过程中都会创建一个对应的 <strong>watcher（订阅者）</strong> 实例，创建 watcher 实例时会去进行模版解析，会触发属性的 getter，而每个属性在最开始设置 setter&#x2F;getter 时会建立一个与之对应的<strong>收集器</strong>， getter 的调用会让该收集器记录当前的 watcher 实例，以便于之后属性变化的时候能通知到它。</p><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">class</span> <span class="title class_">Watcher</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span> (<span class="params">vm: Component, expOrFn: string | <span class="built_in">Function</span>, cb: <span class="built_in">Function</span>, ...</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">cb</span> = cb</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">getter</span> = expOrFn</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">value</span> = <span class="variable language_">this</span>.<span class="title function_">get</span>()</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">get</span> () &#123;</span><br><span class="line">  <span class="title class_">Dep</span>.<span class="property">target</span> = target</span><br><span class="line">    value = <span class="variable language_">this</span>.<span class="property">getter</span>.<span class="title function_">call</span>(<span class="variable language_">this</span>.<span class="property">vm</span>, <span class="variable language_">this</span>.<span class="property">vm</span>) <span class="comment">// 渲染</span></span><br><span class="line"><span class="title class_">Dep</span>.<span class="property">target</span> = <span class="literal">null</span></span><br><span class="line">    <span class="keyword">return</span> value</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">addDep</span> (<span class="attr">dep</span>: <span class="title class_">Dep</span>) &#123;</span><br><span class="line">dep.<span class="title function_">addSub</span>(<span class="variable language_">this</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">update</span> () &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">run</span>()</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">run</span> () &#123;</span><br><span class="line">    <span class="keyword">const</span> oldValue = <span class="variable language_">this</span>.<span class="property">value</span></span><br><span class="line">    <span class="keyword">const</span> value = <span class="variable language_">this</span>.<span class="title function_">get</span>()</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">cb</span>.<span class="title function_">call</span>(<span class="variable language_">this</span>.<span class="property">vm</span>, value, oldValue)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 收集器</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">class</span> <span class="title class_">Dep</span> &#123;</span><br><span class="line">  <span class="keyword">static</span> <span class="attr">target</span>: ?<span class="title class_">Watcher</span>;</span><br><span class="line">  <span class="attr">id</span>: number;</span><br><span class="line">  <span class="attr">subs</span>: <span class="title class_">Array</span>&lt;<span class="title class_">Watcher</span>&gt;;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">constructor</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">id</span> = uid++</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">subs</span> = []</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">addSub</span> (<span class="attr">sub</span>: <span class="title class_">Watcher</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">subs</span>.<span class="title function_">push</span>(sub)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">removeSub</span> (<span class="attr">sub</span>: <span class="title class_">Watcher</span>) &#123;</span><br><span class="line">    <span class="title function_">remove</span>(<span class="variable language_">this</span>.<span class="property">subs</span>, sub)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">depend</span> () &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="title class_">Dep</span>.<span class="property">target</span>) &#123;</span><br><span class="line">      <span class="title class_">Dep</span>.<span class="property">target</span>.<span class="title function_">addDep</span>(<span class="variable language_">this</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">notify</span> () &#123;</span><br><span class="line">    <span class="keyword">const</span> subs = <span class="variable language_">this</span>.<span class="property">subs</span>.<span class="title function_">slice</span>()</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, l = subs.<span class="property">length</span>; i &lt; l; i++) &#123;</span><br><span class="line">      subs[i].<span class="title function_">update</span>() <span class="comment">// // 调用订阅者的update方法，通知变化</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3、派发更新"><a href="#3、派发更新" class="headerlink" title="3、派发更新"></a>3、派发更新</h3><p>当属性被修改时，会触发属性的 <code>setter</code> ，然后该属性的收集器会遍历当前的所有 <strong>watcher</strong> 实例并发送通知，<strong>watcher</strong> 实例会去执行自己的 <code>update()</code> 方法，触发组件的重新渲染，再次去触发属性的 getter。</p><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p><a href="https://cn.vuejs.org/v2/guide/reactivity.html">https://cn.vuejs.org/v2/guide/reactivity.html</a></p>]]></content>
      
      
      
        <tags>
            
            <tag> Vue </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>设计作品</title>
      <link href="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/"/>
      <url>/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/</url>
      
        <content type="html"><![CDATA[<p>以前部分设计作品，做个纪念。</p><section class="post-gallery-section"><h2 id="PhotoShop"><a href="#PhotoShop" class="headerlink" title="PhotoShop"></a>PhotoShop</h2><div class="post-gallery"><p><img src="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/00831rSTly1gda2q68cgqj30u00gv75q.jpg"></p><p><img src="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/00831rSTly1gda2ojuyd6j31bs0u077p.jpg"></p></div><span id="more"></span><h2 id="Maya-Zbrush-Unity3D"><a href="#Maya-Zbrush-Unity3D" class="headerlink" title="Maya + Zbrush + Unity3D"></a>Maya + Zbrush + Unity3D</h2><div class="post-gallery"><p><img src="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/00831rSTly1gda5r378qpj31np0u0gra.jpg"></p><p><img src="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/00831rSTly1gda2rwzen0j31hc0u0gqr.jpg"></p><p><img src="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/00831rSTly1gda2s66a3aj31570u0whb.jpg"></p><p><img src="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/00831rSTly1gda2sey8vaj31hc0tyjw8.jpg"></p><p><img src="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/00831rSTly1gda2styfm2j31fo0r6tlr.jpg"></p></div><h2 id="速写"><a href="#速写" class="headerlink" title="速写"></a>速写</h2><div class="post-gallery"><p><img src="/2018/04/02/%E8%AE%BE%E8%AE%A1%E4%BD%9C%E5%93%81/00831rSTly1gda2tfmiqjj31400u04ks.jpg"></p></div></section>]]></content>
      
      
      
        <tags>
            
            <tag> 设计 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
